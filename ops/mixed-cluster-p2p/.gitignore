# Generated network data (contains participation keys — never commit).
netroot/

# Rendered template handed to `goal network create` (transient).
.template.rendered.json

# Issue #594 soak run output: per-run JSONL captures + their summary
# sidecars. Same rationale as ops/mixed-cluster/.gitignore's TASK-87
# entry — file sizes grow with run length and the data is local-machine
# / run-specific. Attach summaries to PRs by hand.
soak-*.jsonl
soak-*.jsonl.summary.json

# Issue #594 consensus-soak.sh artifact directories (soak JSONL, node
# logs, analyzer output, summary.json). Same rationale.
soak-????????-??????/

# Python bytecode from direct script runs.
scripts/__pycache__/
