# Epic: go-algorand ↔ algod-rust test parity audit

Tracks auditing algod-rust's test suite against go-algorand's, test by
test, across the entire pinned `v5.0.0-stable` reference — independent
of any single subsystem or version delta — and closing the real gaps
found. GitHub epic issue:
[#830](https://github.com/xarmian/algod-rust/issues/830). Full scope
and audit findings: [`docs/PHASE17_PROPOSAL.md`](../PHASE17_PROPOSAL.md).
Full evidence map: [`docs/PHASE17_TEST_PARITY.md`](../PHASE17_TEST_PARITY.md).

## Why this phase exists

Phases 0–16 built and hardened algod-rust subsystem by subsystem and
version-delta by version-delta, each scoped to a specific area or a
specific upstream diff. None asked the exhaustive question this phase
asks: for every test go-algorand actually has, does algod-rust have one
proving the same behavior? A gap predating every prior phase, or
falling between two phases' scopes, would never surface any other way.

## Headline findings

Of 3,177 go-algorand tests: 472 `matched-1:1`, 443 `matched-1:many`, 200
`matched-many:1`, 721 `partial`, 430 `missing-test`, 578
`not-implemented`, 333 `out-of-scope`. **1,115 real, actionable gaps.**

The largest: **AVM transaction-group resource-availability enforcement
does not exist at all** — no cross-transaction resource sharing (v9+),
no availability check on raw addresses, no `tx.Access`-based resolution
at runtime, despite the data model being fully implemented and
statically validated.

## Sub-issues (priority-first within each group)

**Correctness bugs:**
- [x] #808 — avm: implement transaction-group resource-availability enforcement (foreign-array-style sharing + raw-address gate) — **highest priority**; holding/local-state cross-product sharing and tx.Access resolution split to #841
- [x] #809 — avm/ledger: fix app-call guard correctness bugs — merged via PR #838
- [x] #810 — avm: add per-field version gating for global/txn/gtxn/asset_params_get/acct_params_get/block — merged via PR #840; found gap in v13+ block fields split to #839
- [x] #811 — avm: implement RekeyTo/ApplicationCall minimum-AVM-version rule — merged via PR #831
- [x] #812 — validate: add per-type WellFormed() mempool validation — merged via PR #837
- [x] #813 — consensus: reconcile Falcon-512 vs Falcon-1024 parameter-set naming — resolved-by-investigation, no bug found (false alarm from audit test-name grepping)
- [x] #815 — ledger: FirstValidTime + block_field implemented via PR #834; absentee computation split to #833 (needs new block-proposal-time online-stake infrastructure)
- [x] #833 — ledger: implement block-proposal-side absentee/suspension list computation (split from #815); isAbsent pure function implemented via PR #846, block-assembly wiring split to #845 — closed as superseded by #845
- [x] #816 — rest-api: add PQ authorizer/LogicSig-curve compliance checks and stateproof-for-round polling — merged via PR #836; simulate placeholder-PQ-sig moved to #835
- [x] #822 — crypto: enforce decode-time allocation bounds in merklearray proof decoder — merged via PR #832
- [x] #839 — avm: implement missing v13+ block opcode fields (BlkBranch512, BlkSha512_256/256/512TxnCommitment) (split from #810)
- [ ] #841 — avm: implement holding/local-state cross-product resource sharing and tx.Access-list resolution (split from #808)
- [x] #937 — rest-api: implement live catchpoint-catchup mode toggling on a running node (split from #827); via PR #942 for `node start --follow` — new `LiveCatchupManager` mirrors go's `StartCatchup`/`AbortCatchup`/`SetCatchpointCatchupMode` state machine exactly, driving the existing `SyncOrchestrator` in-process via a pausable/resumable `NormalSyncControl`, live-verified against two real local `node start` processes (pause/resume/idempotency all confirmed via direct REST calls) — closed. `algod-rust participate` wiring (pausing the live agreement service — the hard part) split to #940; granular per-account/per-block progress counters split to #941
- [x] #940 — rest-api/consensus: wire live catchpoint-catchup into `algod-rust participate` (split from #937) — `ParticipateAgreementControl` implements `NormalSyncControl` by tearing the agreement `Service` + `CatchupService` pair down and reconstructing both from freshly-built bridges (`Service::shutdown`/`start` both consume `self`, so "stop completely, rebuild from scratch" is the only safe pattern), reusing a stable `Arc<Condvar>` across rebuilds so the pool-block-follower/heartbeat/state-proof-worker threads keep waking correctly; only the two actual ledger writers (agreement + catchup) are paused, not go's full seven-service list, since none of the other five write the ledger directly in algod-rust's architecture. Requires the new `participate --catchup-peer <url>` flag (algod-rust's catchup path is REST-based, unlike go's gossip-based fetch). Live-verified against two real processes: pause/resume confirmed via REST, and the resumed agreement service correctly restored its exact pre-pause round/period/step from the crash-recovery db (no equivocation risk). Also confirmed no regression via the real two-process consensus suite (`multi_node_consensus_sync.rs --ignored`, all 3 tests green) — closed via PR #944
- [x] #941 — rest-api/sync: surface granular catchpoint-catchup progress counters on `GET /v2/status` (split from #937); via PR #943 — threaded real live counts through `import_catchpoint_file_with_progress`/`CatchpointVerifyResult`/`run_download_lookback` into `LiveCatchupManager`'s per-run `CatchpointCounters` snapshot, replacing `status()`'s hardcoded zeros — closed
- [x] #845 — ledger/pool/agreement: wire isAbsent-based absentee-account computation into block assembly (split from #833); validation-side done via PR #886 (`validate_absent_online_accounts` now checks isAbsent/challenge-failure for a received block's claimed-absent list, rejecting a falsely-claimed absentee — a real security-relevant gap); proposal-side done via PR #909, closed: `SqliteLedger::absent_participation_account_candidates` wired into `SimpleBlockEvaluator::generate_block` (the node's actual block-proposal-assembly path), live-verified against the `ops/mixed-cluster/` harness (fork-clean, cert-cross-verified, zero Go-side agreement rejections, Rust-proposed blocks committed at ~expected stake share; the positive absent-list-non-empty case wasn't naturally observable in the ~70-round soak window, honestly disclosed and covered instead by direct unit/integration tests). Residual known limitation: real participation addresses aren't yet threaded through `algo-pool`'s block-assembly call chain end-to-end (same pre-existing gap issue #526's expired-list feature has) — documented, not blocking
- [x] #847 — avm: assembler treats ';' as a comment delimiter instead of a statement separator (found while working #823) — merged via PR #849
- [x] ledger/agreement: `detect_validation_groups` had no transaction-group-size upper bound at all (found while working #825) — merged via PR #857; filed #856 for unrelated pre-existing test failures found while verifying no regressions
- [x] #856 — bin/algod-rust: 3 stale `participate.rs` tests predating PR #837's tightened per-type wellFormed rules (afrz missing freeze_asset/freeze_account; a close-to-self test asserting acceptance of a scenario go-algorand rejects) — merged via PR #870
- [x] #866 — avm: `pre-sharedResources program cannot be invoked with tx.Access` version-gate (go's `sharedResourcesVersion`, `eval.go:1612-1621`) was entirely unimplemented (found while working #824 theme 1) — merged via PR #871
- [x] #877 — avm: `txn`/`gtxn`/`gtxns` lack go-algorand's pseudo-op multi-arity dispatch (`txna`/`gtxna`/`gtxnsa` sugar) (found while working #823 theme 4) — merged via PR #881
- [x] #905 — ledger: fix flaky `fork_detection_fires_on_digest_mismatch_with_cert` test race (found while working #829 slice 5) — merged via PR #906
- [x] #928 — sync: fix flaky `ranked_catchpoint_source` 404-deprioritization test (found while working #925) — root-caused via PR #929 as a genuine production divergence from go, not just a test timing bug: `check_ledger_download`'s retry budget was capped at `min(peer_count, 50)` instead of go's fixed 50-attempt `CatchupLedgerDownloadRetryAttempts`, leaving zero slack for `PeerRanker`'s tied-rank random tie-breaking to redraw an already-failed peer and exhaust the budget; verified with 25+ consecutive clean runs plus a new 100-trial regression test — merged
- [x] #925 — networking: wire vpack vote-compression codec into the `algo-p2p` libp2p gossip transport (split from #817); via PR #927 — traced go's real P2P transport and found it wraps every libp2p stream in the identical `wsPeer`/`wsPeerMsgCodec` machinery the classic transport uses (votes never travel over gossipsub even in P2P mode, only per-peer unicast streams), resolving the stateful-codec-vs-gossipsub risk; wired into `bin/algod-rust/src/commands/p2p_transport.rs`'s `spawn_ws_peer`, verified via 3 real loopback-libp2p integration tests (negotiation, functional round-trip, byte-level `VP`-tag proof) since `ops/mixed-cluster` can't exercise P2P mode — closed. Found and filed unrelated flaky-test follow-up #928
- [x] #911 — agreement: investigate 5-node convergence stall in the new multi-node service-level test harness (found while working #825/#910) — root cause: `TestLedger::ensure_digest` was a no-op (harness bug, not a production liveness bug); fixed via PR #913's `SharedCommits` cluster-wide committed-block registry; verified with 12 consecutive 5-node convergence runs; `TestAgreementFastRecoveryDownEarly`/`DownMiss` now ported at full 5-node fidelity — closed
- [x] #920 — agreement: investigate `FastRecoveryLate`/`Redo` flakiness in the 5-node harness (found while working #825/#919) — root-caused via PR #923: two genuine harness bugs fixed (single-fire cert-vote pocketing instead of a period-spanning retry loop; `AgreementCluster::wait_for_quiet`'s previously-stubbed crypto-verifier-backlog check now wired up via a new `CryptoVerifier for Arc<C>` blanket impl), and `TestPlayerAlwaysResynchsPinnedValue` ported (now matched, passes reliably — proves the pin-resync mechanic is correct, not the cause). The residual payload-propagation gap (no payload catch-up path below `Player::partitioned()`'s period-3 threshold) fixed via PR #926's `TestingNetwork::redeliver_recent_payloads`, which also found and fixed a second real bug — a test helper (`force_fast_recovery_into_value`) was prematurely healing `drop_slow_next_votes` out of sequence with go's structure. `fast_recovery_late_five_node`/`fast_recovery_redo_five_node` now land at ~95-97% reliability across 75+/19+ standalone runs — closed

**Net-new subsystems:**
- [x] #814 — stateproof: implement a signing/proving worker; core signing/proving algorithm done via PR #898 (`algo-consensus-crypto::stateproof::Prover`, `algo-ledger::stateproof_worker`); precursor voters-participant persistence gap split to #912, fixed via PR #914; live background daemon wiring done via PR #918 (`bin/algod-rust`'s `stateproof_service`, opt-in `EnableStateProofWorker`); PR #933 closed the final gap — ran `ops/mixed-cluster` with a test-only shortened `StateProofInterval=16` (new opt-in `start.sh` env vars), found+fixed 4 real bugs blocking the cycle (participate not loading consensus.json overrides, the pool blanket-rejecting `stpf` txns, no zero-signature exemption for the state-proof sender, and `try_build`'s stale-round wedge from never-called pruning), and observed **14 consecutive state-proof cycles committed on-chain** with the Rust node's own submission confirmed byte-for-byte identical to what Go's relays independently saw — closed
- [x] #912 — ledger: persist full voters-round participant snapshots for state-proof signing/proving (split from #814) — new `votersparticipants` sqlite table alongside the existing compact `votersnapshot`, retention reusing the existing recovery-window pruning arithmetic (go's `proversCacheLength` confirmed to bound only an in-memory cache, not disk retention); verified end-to-end with a real sign→build→verify round trip against retrieved data via PR #914 — closed
- [x] #817 — networking: implement vpack vote-compression codec; stateless codec done via PR #885 (verified against 7 real byte-vector fixtures captured from go-algorand's own source), stateful codec (LRU reference tables, HPACK-style proposal window, `r.rnd` delta encoding) done via PR #895 (verified against 8 further real byte-vector fixtures using the same technique). Outbound websocket wiring done via PR #915, live-verified against real go-algorand v5.0.0-stable relays in `ops/mixed-cluster` — 40-round soak with genuine bidirectional `VP`-tagged wire traffic (confirmed byte-level shrinkage on both send and receive), zero abort events, zero Go-side rejections, zero forks. Inbound (axum) websocket wiring done via PR #924 — the mixed-cluster harness's Rust node only ever dials out (no gossip listener), so live-cluster verification was structurally impossible; verified instead via 3 real-socket (genuine loopback TCP + real HTTP/WebSocket handshake) integration tests proving byte-for-byte round-trips in both directions — closed. The separate `algo-p2p` libp2p transport split to #925
- [x] #818 — networking: implement libp2p stream manager, p2pMetainfo exchange, and IdentityTracker; stream manager and IdentityTracker ported as standalone, unwired modules via PR #893 (`p2pMetainfo` found to already be ported, parity doc simply hadn't been updated); connection-limit/pubsub-parameter derivation (`deriveConnLimits`/`deriveAlgorandGossipSubParams`) via PR #902 — closed. Live wiring into `P2pHost`'s event loop remains open as out-of-original-scope follow-up work
- [x] #819 — sync: implement catchup peer-selection/ranking layer; ranking algorithm (`PeerRanker`/`ClassBasedPeerSelector`) done via PR #891, closing 21 of 26 previously-not-implemented rows — closed as superseded by #901. Wiring into live block/catchpoint fetch paths split to #901 (needs live mixed-cluster verification)
- [x] #901 — sync: wire catchup peer-selection/ranking algorithm into live block/catchpoint fetch paths (split from #819); `GossipBlockSource` (block fetch) and new `RankedCatchpointSource` (catchpoint fetch) both wired via PR #916, live-verified against `ops/mixed-cluster` — a graceful restart-rejoin scenario shows 190 ranked-selection log lines during the real catch-up window (`RankedCatchpointSource` has no live-cluster coverage since this harness's Rust node never runs catchpoint sync) — closed. Staged `CatchpointCatchupService`-equivalent pipeline + `SynchronizingTime()` metric split to #917
- [x] #917 — sync: staged `CatchpointCatchupService`-equivalent pipeline with peer-ranked stages and `SynchronizingTime()` metric (split from #901); `check_ledger_download` pre-flight HEAD-probe stage (go's `checkLedgerDownload`) and `CatchupService::synchronizing_time()` both done via PR #922 — closed. Go's fuller persisted/resumable `CatchpointCatchupState` machine deliberately not ported since algod-rust's existing `SyncOrchestrator` phase state machine already owns the equivalent block-lookback/switch phases
- [x] #829 — avm: implement static stack-type-tracking pass in the TEAL assembler; slice 1 (straight-line/branch-free tracking) via PR #883; slice 2 (branch-merge type unification) via PR #892; slice 3 (scratch-slot tracking) via PR #899; slice 4 (`#pragma typetrack` toggling) via PR #903; slice 5 (`popn`/`dupn`/`cover`/`uncover` arity tracking) via PR #904; slice 6 (`match`, `txn`/`gtxn`/`gtxns`/pseudo-ops, `pushbytess`/`pushints`) via PR #908; slice 7 (`itxn_field` field-type refinement, closing `TestTxTypes`; bounds-refined `[min,max]` `StackType`s re-verified and formally closed as investigated-and-deliberately-deferred — confirmed to never affect any accept/reject verdict) via PR #921 — full originally-scoped opcode list now modeled, closed
- [x] #821 — pool/networking: implement application-call excessive-rate-limiter (ERL) subsystem; algorithm ported via PR #890 (`crates/core/algo-pool/src/app_rate_limiter.rs`, all 11 `TestAppRateLimiter_*` cases, sharded/LRU sliding-window + client-mapper hashing bit-for-bit); PR #907 investigated and rejected the REST-submission wiring point as a non-faithful port; PR #931 confirmed #860's pull-side peer-fairness redesign doesn't translate (peer-fairness has no analogue for "which app IDs get evaluated first"); a third pass (PR #938) found the wiring point both prior passes missed — `TxTagHandler`, the inbound gossip `TX`-tag handler on both WS-gossip and libp2p transports, genuinely unsolicited peer-pushed traffic matching go's exact `processIncomingTxn` semantics — wired with real config knobs at go's v5.0.0-stable defaults, tested — closed
- [x] #860 — networking: port go-algorand's `ElasticRateLimiter`/`REDCongestionManager` for incoming-tx backlog admission; algorithm ported via PR #897 (`crates/core/algo-pool/src/elastic_rate_limiter.rs`, all 10 `rateLimit_test.go` cases); REST-submission wiring investigated and rejected (PR #907, same finding as #821); PR #931 implements the pull-side redesign this issue's title called for — `TxSyncPeerLimiter` applies the algorithm to which peer's pull request this node services first under load (the mirror image of go's push-based gate), wired into `TxSyncService`'s production HTTP path, tested for peer-isolation fairness and congestion hysteresis — closed
- [x] #820 — tools: add algokey pq CLI, autonomous heartbeat service, and libgoal app-call resource resolution; heartbeat service done via PR #882 (also fixed a real bug: `hb` transactions were blanket-rejected from the pool, which would have silently discarded the new service's own submissions); algokey pq CLI done via PR #884 (22/30 upstream tests matched); libgoal resource-resolution algorithm done via PR #887 — closed. Full `app call`/`app method` ABI CLI subcommand split to #888 (needs a materially larger ARC-4/ABI-encoding subsystem that doesn't exist in `goal-rust` yet)
- [ ] #828 — util: consider porting db Accessor/Migration framework, pagedqueue, and rateLimit ElasticRateLimiter
- [x] #888 — tools: goal-rust `app call`/`app method` ABI CLI subcommand (split from #820); slice 1 (PR #896) adds the ARC-4 ABI encoding subsystem — new `crates/core/algo-abi` crate (extracted from `algo-rest-api`'s private `abi.rs`), method-selector computation, `Method`/Contract-JSON signature parsing, ABI value decode, JSON display formatting, and go's more-than-15-arguments tuple-bundling rule — verified against real ARC-4 vectors from go-algorand's own e2e ABI fixture and `TestParseMethodArgJSONtoByteSlice`; slice 2 (PR #900) wires it into live `goal-rust app call`/`app method` CLI subcommands — CLI-value parsing, resource resolution via #887, transaction construction/signing/submission, ARC-4 return-value decoding — closed. `app create`/`update`/`delete`/`optin`/`closeout`/`clear`/`read`/`info`/`box info`/`box list` remain unimplemented as smaller independent follow-ups
- [x] #835 — ledger/simulation: implement placeholder PQ signature validation in the simulator (split from #816)
- [ ] #951 — node: implement real follower-node ledger ownership, distinct from the REST-polling `follow` conformance validator (found while auditing `docs/phase17/parity_daemon_node.md`) — unblocks porting `TestDevModeWarning`/`TestFastCatchupResume` and the `_Follower` resource-path tests as genuine parity tests rather than `MockNode`-only surface tests; explicitly not a re-litigation of issue #751's separate "keep the two-entry-point CLI architecture" decision
- [ ] #953 — node/config: per-resource data-dir overrides (`HotDataDir`/`ColdDataDir`), listener port-0-fallback/port-8080-busy-retry, and REST request body-size limit (found while auditing `docs/phase17/parity_daemon_node.md`) — the body-size limit is a real DoS-hardening gap, not just a missing test
- [ ] #955 — networking: server-side catchpoint-serving (`rpcs.LedgerService`-equivalent, both classic-gossip and P2P transports) and block-service peer-redirect-to-archival-peer fallback (found while auditing `docs/phase17/parity_daemon_node.md`) — coordinates with #957 (ledger-core area's `GetCatchpointStream`/path-nesting work, the building block this issue's endpoint would call)

**Test-gap sweeps:**
- [ ] #823 — testing: close AVM/assembler missing-test gaps; theme 1 partial (match opcode execution-level tests, also fixed a real op_match bug) via PR #848 — assembler-level TestAssembleMatch/TestDisassembleBad* sub-items are the only remaining scope; theme 2 (JSON parser edge cases) via PR #851; theme 3 (box budget, all scenarios) via PR #852 + PR #878; theme 4 (assembler diagnostics, all scenarios — found+fixed 2 real gaps: intc/bytec out-of-range acceptance, disassembler silently printing unresolvable field bytes; split TestAssembleTxna's real multi-arity-dispatch gap to #877) via PR #854 + PR #878; theme 5 (EC/pairing, all scenarios — found+fixed a real consensus-affecting bug: BLS12-381 ec_map_to never cleared the cofactor, producing points outside the prime-order subgroup) via PR #853 + PR #878
- [x] #824 — testing: close ledger missing-test gaps; PQ-rekeying theme's core security gap (authorizer-vs-AuthAddr check was entirely unenforced) fixed via PR #850; theme 1 (app-call lifecycle, found+fixed a real cross-product resource-availability bug for top-level group siblings) via PR #867 (also split off #866, a real tx.Access version-gate bug, fixed via PR #871); theme 2 (account/resource lookups) via PR #872; theme 3 (catchpoint/archival, found+fixed a real resource-count-mismatch validation gap in catchpoint import) via PR #875; theme 5 (msgpack edge cases, found+fixed a real AccountTotals omitempty encoding bug) via PR #864; theme 6 (tracker/commit robustness, found+fixed 2 real bugs: missing lastCatchpoint persistence, and an offline-account stake/key leak into agreement's OnlineAccountData) via PR #869 — all 6 themes done, closed
- [ ] #825 — testing: close agreement missing-test gaps; theme 4's `TestProposalCarriesOversizedTxnGroup` fixed via PR #857 (a real bug, not just a missing test), `TestProposalManagerRejectsUnknownEvent` via PR #858, `TestSampleIndexIsValid`/`TestLowerBound` constant invariants via PR #859, `TestSortProposalValueLess`/`TestPseudonodeNonEnqueuedTasks` via PR #894 — theme 4 and its standalone-gaps section now fully done; theme 2 DONE (all 14 credential-arrival-history scenarios) via PR #868 + PR #880 — 2 real bugs found+fixed (`Vote.validated_at` and `Proposal.received_at` were both always `Duration::ZERO`, silently defeating the dynamic-filter-timeout feature and zeroing `BlockAcceptedEvent.ReceivedAt` telemetry); theme 1 partial (6 named regressions + 2 network-hardening scenarios) via PR #876; theme 3 partial (`TestAgreementServiceStartDeadline`, no divergence found) via PR #889 — PR #910 built the previously-missing multi-node service-level test harness (`testingNetwork`/`testingClock`/`activityMonitor`-equivalent, `crates/core/algo-agreement/tests/simulate/`) and ported `TestAgreementFastRecoveryDownEarly`/`DownMiss` at 2-node scale; porting go's actual 5-node scenarios surfaced a real 5-node convergence stall, root-caused and fixed via #911/PR #913 (a harness gap, not a production bug) — `DownEarly`/`DownMiss` now pass at full 5-node fidelity. PR #919 extended the harness with payload pocketing + two-group partition delivery filters and landed `late_cert_bug_five_node`/`large_periods_five_node` (no divergence found); attempting `Late`/`Redo` surfaced genuine reproducible flakiness in the pinned-value-resync-on-period-bump mechanic — split to #920, root-caused and fixed (two harness bugs plus a payload-catchup gap; `TestPlayerAlwaysResynchsPinnedValue` ported, confirmed unrelated) — `late_five_node`/`redo_five_node` now land at ~95-97% reliability via PR #926, closed. PR #932 re-investigated the remaining scenarios rather than trusting stale notes and found `SynchronousFutureUpgrade` was narrower than previously documented — landed via a per-round consensus-version selector, no divergence found — plus a new `crown` delivery-filter primitive for a future `RecoverBothVAndBotQuorums` port. `TestAgreementCertificateDoesNotStallSingleRelay` was attempted via a new `make_relays` star/relay-topology primitive (a message is delivered only if its source or recipient is a designated relay, mirroring go's `makeRelays`), landed as a standalone unit-tested primitive, plus reuse of the `pocket_all_cert_votes`/`replay_all` mechanic `late_cert_bug_five_node` already proved reliable — but the full scenario test itself was NOT landed in that pass: it showed a genuine-looking residual ~10-20% flake (relay's `next_round()` never advances across 40 retry attempts despite repeated re-injection of identical evidence) below this harness's ~95%+ bar, even after finding and fixing two real harness bugs (`TestLedger::ensure_block`/`ensure_validated_block` publishing to `SharedCommits` AFTER advancing `next_rnd`, a genuine race; and a cert-vote-pocketing loop that needed same-period grouping, not just a raw count). A follow-up pass reconstructed the scenario from scratch (the prior pass's code was never committed) and root-caused it via `VoteAggregator::filter_vote` instrumentation: a HARNESS bug, not a `Service`/`Player` divergence — the healing step armed `make_relays` without first clearing the earlier `partition`, and since those two delivery filters are independent (both must pass), the still-partitioned relay silently received nothing. Fixed by calling `repair_all()` before `make_relays()`; landed as `certificate_does_not_stall_single_relay_five_node`, verified across 15+ standalone runs plus 3 full-suite-concurrent runs — closed. Remaining open: `RecoverBothVAndBotQuorums` (needs `nextVoteRanges`-equivalent timing beyond `crown` alone), `8ba23942` (needs coservice-count quiescence tracking + a suspendable block validator), and theme 1's remainder
- [x] #826 — testing: close crypto missing-test gaps; theme 1 (tamper vectors) via PR #855, theme 2 (KAT coverage, found real go-derived + independently cross-verified vectors) via PR #863, theme 3 (randomized-encoding property tests, shared driver) via PR #873 — closed
- [ ] #827 — testing: close REST/kmd/multi-node-cluster negative-path missing-test gaps; theme 1 (REST negative-path coverage) via PR #861; themes 2/3/5 via PR #874 (found+fixed a real msgpack decode bug: 10 `Option` fields missing `#[serde(default)]`); theme 4 (multi-node cluster) via PR #930 built a real, Docker-free multi-node harness — two real `algod-rust participate` child processes on loopback (extending the `tx_propagation_two_binary.rs` pattern), a leader self-certifying its own chain via real VRF/OTS keys and a follower syncing purely over classic-websocket gossip/catchup — closing `TestInitialSync` (matched-1:1) and `TestSyncingFullNode` (partial, steady-state shape only, not its partition-recovery drill or 10-node scale); PR #934 extended the same harness with a `consensus.json`-override-driven `test0`→`test1` protocol pair (mirroring go's `configurableConsensus`), closing `TestSimpleUpgrade` (matched-1:1) and along the way finding+fixing two real bugs (`validate_protocol_version` ignoring the live `consensus.json`-override registry, and `apply_block_impl` never advancing the ledger's tracked live-protocol field past genesis after an on-chain upgrade). PR #936 found and fixed a real security-relevant gap — `raw_transaction`/`add_participation_key`/etc. REST handlers never checked `NodeInterface::is_follower_mode()` at all, so a follower node would have accepted operations it should reject — closing `TestErrors`. Same PR investigated the rest: `TestDevModeWarning`/`TestFastCatchupResume` correctly re-classified `out-of-scope`/`not-implemented` rather than force-tested, since algod-rust deliberately has no in-process ledger-owning follower-node type (a recorded architectural decision, issue #751/PR #772) — `TestFastCatchupResume`'s live-catchup-toggle half split to new issue #937; `TestNodeSetCatchpointCatchupMode` confirmed to need the same live-toggle capability (also #937); P2P-specific tests (`TestNodeP2PRelays`/`TestNodeP2P_NetProtoVersions`/`TestNodeHybridTopology`) confirmed to need a real multi-node P2P integration-test harness `algo-p2p` doesn't have yet, consistent with that transport's known immaturity — remain open

- [ ] #950 — testing: close e2e REST-API/CLI negative-path and lookup-edge-case missing-test gaps (found while auditing `docs/phase17/parity_e2e.md`) — 8 themes (account/asset/app-index edge cases, grouped-transaction submission, send/broadcast negative paths, lease-conflict regressions, misc REST/CLI edge cases, kmd multisig edge cases, pre-state-proof-mandate keyreg acceptance, kmd MDK-past-imported-keys), non-overlapping with #827 theme 1's already-closed 9-item checklist
- [ ] #958 — testing: close daemon/rpcs missing-test gaps (found while auditing `docs/phase17/parity_daemon_node.md`) — 9 themes (`/v2/status` consensus-upgrade fields, `wait-for-block-after` shutdown/timeout, catchup/simulate handler gaps, tx-sync edge cases, block-service shutdown, node-orchestration misc, P2P dedicated scenarios, connection-limiter soft-limit test, `NetPrioResponse` msgpack roundtrip)
- [ ] #961 — testing: close msgpack self-roundtrip test gaps for `StateSchema`/`ParticipationUpdates`/`BlockHeader`/`LightBlockHeader`/`hashable.Message`/`stateproofmsg.Message`/`NetPrioResponse` (found while auditing `docs/phase17/parity_data_misc.md` and `parity_daemon_node.md`) — coverage-only sweep in the spirit of issue #827 theme 5
- [ ] #963 — testing: close algo-pool missing-test gaps — close-account-through-pool scenarios and per-category pool-error-classification tests (found while auditing `docs/phase17/parity_data_misc.md`)

## Reproducing this audit

`scripts/list_go_tests.sh` and `scripts/list_rust_tests.sh` regenerate
the raw test inventories; re-run both and rebuild
`docs/phase17/parity_*.md` after any future go-algorand version bump.
