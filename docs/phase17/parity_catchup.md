# Phase 17 Test Parity — catchup

| go-algorand test | algod-rust test(s) | status | notes |
|---|---|---|---|
| [TestCatchpointServicePeerRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/catchpointService_test.go#L90) | [single_peer_failure_is_ranked_without_panicking](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L447), [check_ledger_download_probe_deprioritizes_a_404_peer_before_any_real_download](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L505), [check_ledger_download_fails_when_every_peer_lacks_the_catchpoint](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L560) | matched-1:many | Issue #901 wired `make_catchpoint_peer_selector` into `RankedCatchpointSource` (`crates/node/algo-rest-client/src/ranked_catchpoint_source.rs`), used by `AlgodSyncBackend::download_catchpoint` — a peer that fails is ranked and deprioritized without panicking, matching go's crash-safety assertion. Issue #917 added a `check_ledger_download` pre-flight probe stage (a HEAD request via `CatchpointDownloader::probe_availability`, mirroring go's `checkLedgerDownload`/`headLedger`) that runs ahead of the real transfer and ranks an unavailable-catchpoint peer `PEER_RANK_NO_CATCHPOINT_FOR_ROUND` before any real download is attempted — the same two-stage (`checkLedgerDownload` then the real fetch) sequencing go's `CatchpointCatchupService.Start()`/`run()` uses, sharing peer-selector state across both stages. algod-rust still has no persisted, resumable `CatchpointCatchupState` machine (`processStageLedgerDownload`/`processStageLatestBlockDownload`/`processStageBlocksDownload`/`processStageSwitch` remain unported — algod-rust's `SyncOrchestrator` phase state machine, `crates/core/algo-ledger/src/sync/state_machine.rs`, already owns the equivalent block-lookback/switch phases, so porting go's version would duplicate rather than replace it), which is why this stays a test-behavior match rather than an architecture-level one. |
| [TestProcessStageBlocksDownloadNilCert](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/catchpointService_test.go#L129) | — | out-of-scope | Regression guard that `StoreBlock` never receives a nil certificate after a block is already in the ledger, specific to go's persisted `processStageBlocksDownload` stage. Issue #917 added a `check_ledger_download` pre-flight probe stage (see `TestCatchpointServicePeerRank` above) but deliberately did not port go's fuller resumable `CatchpointCatchupState` machine that `processStageBlocksDownload` belongs to — algod-rust's `SyncOrchestrator` (`crates/core/algo-ledger/src/sync/state_machine.rs`) already owns the equivalent block-lookback-after-catchpoint phase with its own certificate handling, so there is no `processStageBlocksDownload`-shaped call site to regress here. |
| [TestClassBasedPeerSelector_makeClassBasedPeerSelector](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L48) | [class_based_selector_preserves_construction_order](../../crates/node/algo-network/src/peer_ranker.rs#L1367) | matched-1:1 | Issue #819: `ClassBasedPeerSelector` (`peer_ranker.rs`) ports go's `classBasedPeerSelector`/`wrappedPeerSelector` — per-peer-class tolerance factors and priority-ordered selector list. Issue #901 wired it into `GossipBlockSource` (block fetch) and `RankedCatchpointSource` (catchpoint fetch). |
| [TestClassBasedPeerSelector_rankPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L79) | [class_based_selector_tracks_failures_only_for_the_owning_class](../../crates/node/algo-network/src/peer_ranker.rs#L1428) | matched-1:1 | |
| [TestClassBasedPeerSelector_peerDownloadDurationToRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L173) | [class_based_peer_download_duration_to_rank_uses_first_matching_class](../../crates/node/algo-network/src/peer_ranker.rs#L1509) | matched-1:1 | |
| [TestClassBasedPeerSelector_getNextPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L227) | [disabled_classes_are_skipped_and_reset_together_once_all_disabled](../../crates/node/algo-network/src/peer_ranker.rs#L1589) | matched-1:1 | |
| [TestClassBasedPeerSelector_integration](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L414) | [catchpoint_selector_prefers_relays_then_falls_back_to_archival_on_failures](../../crates/node/algo-network/src/peer_ranker.rs#L1690) | matched-1:1 | |
| [TestNoPeersAvailable](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L45) | — | out-of-scope | Despite its name, this test does not exercise a "zero peers available" scenario at all: it passes `&lf` itself (the `ledgerFetcher` struct) as an opaque `Peer` to `downloadLedger` and asserts `errNonHTTPPeer` — a Go runtime type-switch guard against a peer that isn't an `HTTPPeer`. (Previously doc-mismapped to `gossip_sync_backend_gossip_only_no_peers_fails`/`handoff_selects_http_only_when_no_peers`, which test an unrelated "literally zero peers configured" scenario in `catchpoint_sync.rs`'s block-source policy selection — not this test's actual subject.) `CatchpointDownloader` (`crates/node/algo-rest-client/src/catchpoint_download.rs`) is constructed directly from a `base_url: &str` (or, via `with_p2p_transport`, a statically-typed `HttpPeerTransport`), not a dynamically-typed `Peer` interface, so there is no wrong-peer-type call site to guard against — the same reasoning `TestUGetBlockUnsupported` (below) is already marked out-of-scope for. |
| [TestNonParsableAddress](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L54) | [probe_availability_rejects_non_parsable_base_url](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L847), [download_rejects_non_parsable_base_url_without_retrying](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L861) | matched-1:many | Issue #976: `CatchpointDownloader::new`/`with_config` don't parse the base URL eagerly (they only trim a trailing slash), so the equivalent rejection point to go's `getPeerLedger` is the first network call — `reqwest`'s request builder fails to parse a malformed URL and `.send()` surfaces it as `Err`, never a panic, and `is_retryable` correctly treats that failure as permanent (not retried). |
| [TestLedgerFetcherErrorResponseHandling](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L63) | [read_chunk_with_stall_check_times_out_on_a_stalled_response](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1254), [download_retries_on_mid_stream_connection_drop](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1019), [download_maps_404_to_not_found_without_retrying](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1108), [download_maps_persistent_server_error_to_rest_client_error](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1147), [download_rejects_response_with_missing_content_type](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1302), [download_rejects_response_with_duplicate_content_type_headers](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1344), [download_rejects_response_with_wrong_content_type_value](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1391), [download_succeeds_with_a_single_correct_content_type_header](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1436) | matched-1:many | Issue #1203: `CatchpointDownloader::get_with_retry`/`p2p_download_attempt` now validate the response's `Content-Type` header before the body is read, mirroring go's `getPeerLedger` count-then-value check (`catchup/ledgerFetcher.go:143-154`) against `algo_network::LEDGER_RESPONSE_CONTENT_TYPE`. All five of go's table cases (404, 500, no Content-Type, too-many Content-Types, invalid Content-Type) now have a direct algod-rust equivalent, plus stall-timeout and mid-stream-drop retry cases go's table doesn't cover. The P2P transport path gets the same missing/wrong-value checks ([download_rejects_missing_content_type_over_p2p_transport](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1484)); its raw-stream header parser folds duplicate header lines into one `HashMap` entry, so the "too many Content-Types" case specifically isn't representable over that transport — a structural limitation of the P2P raw-header parser, not a coverage gap in the `reqwest` path. `probe_availability`'s HEAD pre-flight deliberately does not get this check, matching go's `headLedger`, which never inspects Content-Type. |
| [TestLedgerFetcher](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L130) | [download_retries_on_mid_stream_connection_drop](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1019), [download_maps_404_to_not_found_without_retrying](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1108), [download_maps_persistent_server_error_to_rest_client_error](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1147) | matched-1:many | algod-rust's `CatchpointDownloadClient::download` is the ledgerFetcher analog (explicitly ported from `catchup/ledgerFetcher.go`, see doc comments at the top of the file). Now covers the successful mid-stream-retry path plus the 404/500 status-code branches go's `headLedger`/`downloadLedger` exercise; the non-HTTP-peer and unparsable-URL cases are covered separately (see `TestNonParsableAddress` above). |
| [TestLedgerFetcherP2P](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L187) | [probe_availability_succeeds_over_p2p_transport](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1581), [download_succeeds_over_p2p_transport](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L1617), [catchpoint_downloader_probes_and_downloads_over_real_p2p_transport](../../bin/algod-rust/src/commands/p2p_transport.rs#L2713), [build_algod_sync_backend_wires_in_connected_p2p_peers](../../bin/algod-rust/src/commands/catchpoint_sync.rs) | matched-1:many | Issue #1127: mirroring go's finding (`TestLedgerFetcherP2P` doesn't test a separate binary P2P protocol — it proves the *same* HTTP-shaped `ledgerFetcher.headLedger`/`downloadLedger` code works against a P2P-derived `network.HTTPPeer`), `CatchpointDownloader` gained a pluggable `HttpPeerTransport` seam (`crates/node/algo-rest-client/src/http_over_stream.rs`) and a `with_p2p_transport` constructor so both the HEAD probe and the full streamed download run over an abstract duplex-stream transport instead of only `reqwest`. `bin/algod-rust`'s `P2pHttpPeerTransport` (`p2p_transport.rs`) implements that trait over the real `algo_p2p`/`/algorand-http/1.0.0` transport (`P2pTransport::open_http_stream`), proven end-to-end against a real libp2p pair in `catchpoint_downloader_probes_and_downloads_over_real_p2p_transport`. `RankedCatchpointSource::push_p2p_peer` extends #901's existing `ClassBasedPeerSelector` ranking to P2P-sourced peers unchanged. Issue #1130 closed the remaining gap: `catchpoint_sync.rs`'s `AlgodSyncBackend::with_catchpoint_peers_and_p2p` threads a live, connected `P2pTransport`'s peer set into `RankedCatchpointSource` at node-startup time (wired through `participate.rs`'s live-catchup path; `WsOnly` mode is unaffected), mirroring #901's `create_peer_selector`-into-block-fetch pattern. The peer set is a snapshot taken when the sync backend is constructed (not continuously live mid-run) — a peer that connects mid-catchup is picked up on the next attempt rather than the current one, a documented simplification. |
| [TestPeerSelector_PeerAddress](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L66) | — | out-of-scope | Go's `peerAddress()` extracts a string address from a live `network.Peer` (`HTTPPeer`/`UnicastPeer`). Issue #819's `PeerRanker` (`peer_ranker.rs`) is a standalone algorithm module with no dependency on algod-rust's live network stack, so peers are already identified by a caller-supplied `String` id (`PeerSelectorPeer::peer_id`) — there is no address-extraction step to port. |
| [TestPeerSelector_DownloadDurationToRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L80) | [download_duration_to_rank_matches_go_fixture](../../crates/node/algo-network/src/peer_ranker.rs#L897) | matched-1:1 | Issue #819: `download_duration_to_rank` in `peer_ranker.rs` ports go's `downloadDurationToRank` bit-for-bit (same clamp-then-map formula), pinned against the same fixture values as the go test. |
| [TestPeerSelector_RankPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L121) | [rank_peer_moves_peers_between_pools_and_tracks_membership](../../crates/node/algo-network/src/peer_ranker.rs#L973) | matched-1:1 | |
| [TestPeerSelector_PeerDownloadRanking](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L180) | [peer_download_duration_to_rank_uses_the_peers_class_bucket](../../crates/node/algo-network/src/peer_ranker.rs#L1053) | matched-1:1 | |
| [TestPeerSelector_FindMissingPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L232) | [find_peer_returns_negative_indices_when_missing](../../crates/node/algo-network/src/peer_ranker.rs#L1034) | matched-1:1 | |
| [TestPeerSelector_HistoricData](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L247) | [historic_data_biases_the_selector_toward_the_fastest_peer](../../crates/node/algo-network/src/peer_ranker.rs#L1383), [ranker_never_exceeds_class_upper_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1240), [ranker_never_goes_under_class_lower_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1263) | matched-1:many | Issue #1504 batch 1: `historic_data_biases_the_selector_toward_the_fastest_peer` ports go's 1000-iteration long-run test verbatim (same archival/relay peer classes, same per-peer download durations, same assertions that `a3` is selected at least as often as `a1`/`a2` and the lower-priority relay peers get zero selections). Go's `peerSelectorTestRandVal` helper SHA-512/256-hashes the loop counter and decodes a `binary.ReadUvarint` from a 0x00-prefixed digest copy; because that leading 0x00 byte's continuation bit is clear, `ReadUvarint` always stops there and returns 0, so go's "random" multiplier is provably always exactly `1.0` on every iteration — verified by reading `catchup/peerSelector_test.go` and reproduced directly with the resulting fixed multiplier rather than re-deriving the (constant) hash. The real randomness in both go's test and `PeerRanker` is which same-rank pool member `getNextPeer`/`get_next_peer` draws via a true RNG, which this port also exercises. `ranker_never_exceeds_class_upper_bound`/`ranker_never_goes_under_class_lower_bound` remain as the pre-existing deterministic bound-invariant coverage. |
| [TestPeerSelector_PeersDownloadError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L321) | [peers_download_error_evicts_failing_archival_peers_after_ramp_up](../../crates/node/algo-network/src/peer_ranker.rs#L1446), [repeated_download_failures_evict_the_peer_and_the_next_class_takes_over](../../crates/node/algo-network/src/peer_ranker.rs#L1285) | matched-1:many | Issue #1504 batch 1: `peers_download_error_evicts_failing_archival_peers_after_ramp_up` ports go's test verbatim — 500 iterations of successful downloads, then archival-class (`a1`/`a2`/`a3`) requests are forced to fail every subsequent iteration while relay-class (`b1`/`b2`) requests keep succeeding, and the post-loop pool-structure assertions (which pool holds the surviving `b`-peers, which pool is pinned at `PEER_RANK_DOWNLOAD_FAILED` with all 3 evicted archival peers) are reproduced exactly against `ranker.pools`. See `TestPeerSelector_HistoricData`'s row for why go's per-iteration "random" multiplier is provably a fixed `1.0` and this port uses that fixed value directly. |
| [TestPeerSelector_Penalty](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L397) | [penalty_prevents_the_fastest_peer_from_fully_dominating_selection](../../crates/node/algo-network/src/peer_ranker.rs#L1508), [penalty_never_demotes_and_reset_never_promotes_past_a_demotion](../../crates/node/algo-network/src/peer_ranker.rs#L1201) | matched-1:many | Issue #1504 batch 1: `penalty_prevents_the_fastest_peer_from_fully_dominating_selection` ports go's test verbatim (fixed, non-random per-peer durations for `a1`/`a2`/`a3`; asserts the mid-speed peer `a2` still gets selected at least 50 times in 1000 iterations rather than being fully starved by the faster `a3` (which gets at least 2x `a2`'s share), plus the same zero-relay-selection checks). `penalty_never_demotes_and_reset_never_promotes_past_a_demotion` remains as the pre-existing deterministic penalty-formula coverage. |
| [TestPeerSelector_PeerDownloadDurationToRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L456) | [peer_download_duration_to_rank_covers_all_four_class_buckets](../../crates/node/algo-network/src/peer_ranker.rs#L1101) | matched-1:1 | Exercises all four `PeerClassKind` rank buckets in one ranker, mirroring go's four-peer-class fixture. |
| [TestPeerSelector_LowerUpperBounds](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L500) | [lower_upper_bounds_match_go_fixture](../../crates/node/algo-network/src/peer_ranker.rs#L934) | matched-1:1 | |
| [TestPeerSelector_FullResetRequestPenalty](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L523) | [full_reset_clears_all_gaps](../../crates/node/algo-network/src/peer_ranker.rs#L1224) | matched-1:1 | |
| [TestPeerSelector_PenaltyBounds](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L538) | [penalty_never_demotes_and_reset_never_promotes_past_a_demotion](../../crates/node/algo-network/src/peer_ranker.rs#L1201) | matched-1:1 | |
| [TestPeerSelector_ClassUpperBound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L564) | [ranker_never_exceeds_class_upper_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1240) | matched-1:1 | |
| [TestPeerSelector_ClassLowerBound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L599) | [ranker_never_goes_under_class_lower_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1263) | matched-1:1 | |
| [TestPeerSelector_EvictionAndUpgrade](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L629) | [repeated_download_failures_evict_the_peer_and_the_next_class_takes_over](../../crates/node/algo-network/src/peer_ranker.rs#L1285) | matched-1:1 | |
| [TestPeerSelector_RefreshAvailablePeers](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L666) | [refresh_adds_new_peers_and_removes_dropped_ones](../../crates/node/algo-network/src/peer_ranker.rs#L1322) | matched-1:1 | |
| — (new algod-rust-only regression, issue #901) | [ranked_selector_prefers_the_reliable_peer_after_the_unreliable_one_fails](../../crates/node/algo-rest-client/src/gossip_block_source.rs#L714) | matched-1:1 | No single go test asserts this end-to-end (go's wiring long predates a dedicated regression); pins that `GossipBlockSource`'s live block-fetch peer selection — not just the standalone `peer_ranker.rs` algorithm — actually deprioritizes a peer that fails in favor of a reliable one on subsequent fetches. |
| — (new algod-rust-only regression, issue #901) | [ranked_source_prefers_the_reliable_peer_after_the_unreliable_one_fails](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L280) | matched-1:1 | Same wiring-level assertion as above, for the catchpoint-file download path (`RankedCatchpointSource`). |
| [TestServiceFetchBlocksSameRange](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L135) | [sync_range_static_entry_point_fetches_arbitrary_range](../../crates/core/algo-ledger/src/catchup_service.rs#L2714), [sync_now_fetches_arbitrary_range_without_certificate](../../crates/core/algo-ledger/src/catchup_service.rs#L2745) | matched-1:many | Closed (#1504 batch 3): added `CatchupService::sync_range` — a public, directly-callable, certificate-less, arbitrary-range range-sync entry point (built from the existing `sync_pass_inner` fetch/apply loop) — and `CatchupService::sync_now`, an instance method that triggers an immediate synchronous pass on a *running* service via a new one-shot trigger channel the worker thread selects on alongside `cert_rx`/`shutdown_rx` (mirroring go's `syncNow` channel in `periodicSync`), and blocks for the result. This mirrors go's `syncer.testStart(); syncer.sync()` in `TestServiceFetchBlocksSameRange` directly: the local ledger fetches from `next_round()` up through the peer's last available round with no certificate required per round, driven entirely by the fetcher-supplied certificate (existing `authenticate_block` path). |
| [TestSyncRound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L207) | [get_sync_round_success](../../crates/node/algo-rest-api/tests/integration.rs#L8124), [get_sync_round_not_set](../../crates/node/algo-rest-api/tests/integration.rs#L8143), [set_sync_round_success](../../crates/node/algo-rest-api/tests/integration.rs#L8195), [set_sync_round_invalid](../../crates/node/algo-rest-api/tests/integration.rs#L8211), [unset_sync_round_success](../../crates/node/algo-rest-api/tests/integration.rs#L8228) | matched-1:many | Go's `SetDisableSyncRound`/`GetDisableSyncRound`/`UnsetDisableSyncRound` behavior is exposed and tested via algod-rust's REST v2 `/v2/ledger/sync` endpoints rather than an internal catchup-service unit test. |
| [TestPeriodicSync](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L297) | [periodic_sync_fetches_blocks_in_parallel](../../crates/core/algo-ledger/src/catchup_service.rs#L1892), [periodic_sync_with_parallelism_one_stays_serial](../../crates/core/algo-ledger/src/catchup_service.rs#L1927) | matched-1:many | |
| [TestServiceFetchBlocksOneBlock](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L361) | [happy_path_fetch_and_commit](../../crates/core/algo-ledger/src/catchup_service.rs#L1451) | matched-1:1 | |
| [TestAbruptWrites](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L419) | [abrupt_writes_race_with_concurrent_ledger_commits](../../crates/core/algo-ledger/src/catchup_service.rs#L1473), [skips_already_committed_round](../../crates/core/algo-ledger/src/catchup_service.rs#L1330) | matched-1:many | #1504 batch 2: `abrupt_writes_race_with_concurrent_ledger_commits` closes the concurrency gap the previous note flagged — a background thread commits blocks for rounds `1..=20` directly to the shared `MockCatchupLedger` (simulating agreement making independent progress, as go's test's `go func()` loop does against `local.AddBlock`) while `CatchupService` concurrently fetches and commits certificates for the same rounds through a `DelayedRoundServingBlockFetcher` that injects a per-round stagger to widen the race window. Asserts the ledger reaches the final round with no panic/deadlock, mirroring go's `require.Equal(t, remote.LastRound(), local.LastRound())`. |
| [TestServiceFetchBlocksMultiBlocks](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L479) | [periodic_sync_fetches_blocks_in_parallel](../../crates/core/algo-ledger/src/catchup_service.rs#L1892) | matched-1:1 | |
| [TestServiceFetchBlocksMalformed](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L536) | [digest_mismatch_triggers_retry](../../crates/core/algo-ledger/src/catchup_service.rs#L1513), [contents_mismatch_triggers_retry](../../crates/core/algo-ledger/src/catchup_service.rs#L1958) | matched-1:many | |
| [TestOnSwitchToUnSupportedProtocol1](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L580) | [sync_pass_interrupted_in_initial_loop](../../crates/core/algo-ledger/src/catchup_service.rs#L2094) | matched-1:1 | Issue #976: interruption on the very first round of a pass — direct, synchronous call into `sync_pass_inner` (rather than the background-thread `CatchupService::start`) so the outcome (no block ever committed) is asserted without polling. |
| [TestOnSwitchToUnSupportedProtocol2](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L599) | [sync_pass_interrupted_mid_loop](../../crates/core/algo-ledger/src/catchup_service.rs#L2120) | matched-1:1 | Issue #976: interruption partway through a pass, after several supported rounds were already committed — sync stops exactly at the last supported round. |
| [TestOnSwitchToUnSupportedProtocol3](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L621) | [sync_pass_interrupted_with_short_notice](../../crates/core/algo-ledger/src/catchup_service.rs#L2151) | matched-1:1 | Issue #976: short-notice interruption — a worker may prefetch the unsupported round concurrently with the last supported one (`parallel_blocks=2`), but the strict in-order apply loop guarantees the boundary is still honored exactly. |
| [TestOnSwitchToUnSupportedProtocol4](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L647) | [sync_pass_interrupted_restart_at_switch_round](../../crates/core/algo-ledger/src/catchup_service.rs#L2182) | matched-1:1 | Issue #976: restart-at-switch-round variant — the ledger already sits at the switch boundary when a fresh pass starts, so it must decline to fetch anything at all. `sync_cert_bails_out_for_unsupported_round` (`catchup_service.rs#L2210`) adds the same guard's coverage through the certificate-driven path (`sync_cert`/`fetchRound`'s equivalent). |
| [TestCatchupUnmatchedCertificate](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L912) | [fork_detection_fires_on_digest_mismatch_with_cert](../../crates/core/algo-ledger/src/catchup_service.rs#L1692), [no_fork_detection_without_fetched_cert](../../crates/core/algo-ledger/src/catchup_service.rs#L1748), [no_fork_when_fetched_cert_matches_agreement](../../crates/core/algo-ledger/src/catchup_service.rs#L1785) | matched-1:many | go's `syncCert` (called directly here) is algod-rust's `sync_cert`; the fork-detection tests exercise the same code path with unmatched/matched certs. |
| [TestCreatePeerSelector](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L958) | [create_peer_selector_matches_go_topology](../../crates/node/algo-network/src/peer_ranker.rs#L1728) | matched-1:1 | Issue #901 ported `create_peer_selector` (`peer_ranker.rs`), mirroring go's `createPeerSelector` topology exactly: `ConnectedOut`, `PhonebookRelays`, `PhonebookArchivalNodes`, `ConnectedIn` classes with tolerance factors 3/3/10/3. It is wired into `GossipBlockSource`'s block-fetch peer selection (`crates/node/algo-rest-client/src/gossip_block_source.rs`), replacing the previous round-robin. |
| [TestServiceStartStop](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L980) | [stop_without_certs_is_clean](../../crates/core/algo-ledger/src/catchup_service.rs#L1256), [drop_triggers_stop](../../crates/core/algo-ledger/src/catchup_service.rs#L1267) | matched-1:many | |
| [TestSynchronizingTime](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L993) | [synchronizing_time_is_zero_before_any_sync_and_nonzero_during_one](../../crates/core/algo-ledger/src/catchup_service.rs#L2095), [synchronizing_time_is_zero_immediately_after_stop](../../crates/core/algo-ledger/src/catchup_service.rs#L2142) | matched-1:many | Issue #917 added `CatchupService::synchronizing_time()`, mirroring go's `Service.SynchronizingTime()`/`syncStartNS`: zero when no periodic sync pass is in flight, else the elapsed time since the current pass ([`sync_pass`], go's `sync()`) started. Unlike go's test (which pokes `syncStartNS` directly, same package), algod-rust's tests drive the real state transition through a slow fetcher since the field is private, exercising `sync_pass`'s own start/reset logic rather than just the getter. |
| [TestDownloadBlocksToSupportStateProofs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1006) | [lookback_for_stateproofs_support_matches_go_test_oracle](../../crates/core/algo-ledger/src/voters_tracker.rs#L514) | matched-1:1 | Issue #976: `lookback_for_stateproofs_support` (`voters_tracker.rs#L174`) ports go's `lookbackForStateproofsSupport` formula bit-for-bit (built on the already-ported `get_oldest_expected_state_proof`/`voters_round_for_state_proof_round`) and is pinned against the same three-case oracle go's test uses. Wired into `SyncOrchestrator::run_download_lookback` (`crates/core/algo-ledger/src/sync/mod.rs`), which now downloads `max(MAX_TXN_LIFE, lookback_for_stateproofs_support(..))` blocks instead of always the fixed `MAX_TXN_LIFE` window, via the new `catchpoint::download_lookback_blocks_with_lookback`. |
| [TestServiceLedgerUnavailable](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1046) | [sync_pass_inner_stops_early_when_ledger_behind_committing_deltas](../../crates/core/algo-ledger/src/catchup_service.rs#L2788) | matched-1:1 | Closed (#1504 batch 3): added `CatchupLedger::is_behind_committing_deltas` (default `false`) — a ledger-side commit-backpressure signal mirroring go's `Ledger.IsBehindCommittingDeltas()` — and a check in `sync_pass_inner` right after a round successfully commits, mirroring go's `pipelinedFetch` guard (`catchup/service.go`) that returns `errCatchupBehindDeltas`. When the ledger reports it is behind, the pass stops with `SyncExitReason::BehindDeltas` after committing whatever it already has in hand. The new test's mock ledger reports "behind" once round 3 commits, against a fetcher that has 50 rounds available; the pass commits exactly rounds 1..=3 (partial-but-bounded progress, not zero and not the full 50) and returns the typed `BehindDeltas` reason — pinning the same property go's test asserts. |
| [TestServiceNoBlockForRound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1094) | [sync_cert_gives_up_after_retry_limit_exceeded](../../crates/core/algo-ledger/src/catchup_service.rs#L2828) | matched-1:1 | Closed (#1504 batch 3): added `CatchupService::CATCHUP_RETRY_LIMIT = 500` (matching go's `catchupRetryLimit`, `catchup/service.go:47`) and a check in `sync_cert`'s retry loop, mirroring go's `if i > catchupRetryLimit { ... return errFetchRetryLimit }` at `catchup/service.go:314` — once exceeded, the loop logs a single warning and returns `SyncExitReason::NoBlock` instead of retrying forever. The retry-attempt cap is parameterized (`sync_cert_with_retry_limit`, with `sync_cert` calling it with the production `500`) so the new test can pin the exhaustion path itself with a small limit (3) against a fetcher that always fails, asserting both the typed exit reason and the exact attempt count, without waiting through hundreds of real backoff delays. |
| [TestPipelinedFetchExitReason](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1145) | [sync_pass_inner_and_sync_cert_report_typed_exit_reasons](../../crates/core/algo-ledger/src/catchup_service.rs#L2865) | matched-1:1 | Closed (#1504 batch 3): added `SyncExitReason` (`Done`/`NoPeer`/`NoBlock`/`Stopping`/`BehindDeltas`/`Unsupported`), mirroring go's typed `pipelinedFetch`/`fetchAndWrite` exit errors (`errCatchupNoPeer`, `errFetchNoBlock`/`errFetchRetryLimit`, `errCatchupStopping`, `errCatchupBehindDeltas`, and normal completion). `sync_pass`/`sync_pass_inner`/`sync_cert`/`sync_cert_with_retry_limit` now return it instead of `()`, threaded through every existing stop/return point (shutdown, unsupported round, fetch failure, digest mismatch, retry-limit exhaustion, ledger-behind-deltas, and success). The new test exercises a representative subset of the variants directly across both `sync_pass_inner` (periodic path) and `sync_cert` (certificate-driven path): `Done`, `Unsupported`, `Stopping`, `NoBlock`, and `BehindDeltas`, asserting the exact typed value returned in each scenario rather than only the log line/side effect, closing the gap go's test exercises via `pipelinedFetch`'s direct return value. |
| [TestUGetBlockWs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L42) | [successful_single_peer_fetch](../../crates/node/algo-rest-client/src/gossip_block_source.rs#L697), [single_peer_no_block_for_next_round_returns_error](../../crates/node/algo-rest-client/src/gossip_block_source.rs#L719), [parse_success_response](../../crates/node/algo-network/src/block_fetcher.rs#L364), [make_request_topics_has_correct_keys](../../crates/node/algo-network/src/block_fetcher.rs#L324) | matched-1:many | Added `single_peer_no_block_for_next_round_returns_error` (`gossip_block_source.rs`), a direct port of go's exact two-call sequence: a single `MockPeer` serves round `next` successfully (asserted equal, matching go's block-equality check), then round `next + 1` — which the peer has no data for — fails with an error surfacing the peer's "not available" response, mirroring go's `noBlockForRoundError{round: next+1, latest: next}` assertion. This closes the gap the previous note flagged (no end-to-end fetch-over-a-live-unicast-peer test): `MockPeer` implements the real `UnicastPeer` trait and the test drives the actual `make_block_request_topics` -> `UnicastPeer::request` -> `parse_block_response` -> `decode_block_cert` pipeline end-to-end, the same pipeline `successful_single_peer_fetch` (pre-existing, now linked here) already exercised for the success half alone. |
| [TestUGetBlockHTTP](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L87) | [fetch_block_succeeds_then_reports_no_block_for_next_round](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L408), [block_url_round_zero](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L324), [accessors](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L362) | matched-1:many | Added `fetch_block_succeeds_then_reports_no_block_for_next_round` (`http_block_fetcher.rs`), a direct port of go's real-server test: a `wiremock` `MockServer` serves round `next` with the real block content type and body bytes (asserted byte-equal), then 404s round `next + 1` with the `X-Latest-Round` header set to `next` — mirroring go's `noBlockForRoundError{round: next+1, latest: next}` assertion — and the fetch surfaces `HttpBlockFetchError::BlockNotAvailable { latest_round: Some(next) }`. This closes the gap the previous note flagged (URL/accessor unit tests only, no fetch-against-a-live-server integration test), driving `HttpBlockFetcher::fetch_block` through a real `reqwest` request against a live listener exactly as go's test drives `fetchBlock` against a real `rpcs.MakeBlockService` HTTP handler. |
| [TestUGetBlockUnsupported](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L135) | — | out-of-scope | Guards a Go runtime type-switch fallback (`fetchBlock` erroring when the peer is neither `HTTPPeer` nor `UnicastPeer`). algod-rust's fetchers are statically typed per transport (`HttpBlockFetcherSource`, gossip `block_fetcher.rs`), so this failure mode cannot occur — there is no dynamic peer-type dispatch to fall through. |
| [TestProcessBlockBytesErrors](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L149) | [decode_block_cert_wrong_cert_round](../../crates/node/algo-rest-client/src/gossip_block_source.rs#L765), [decode_block_cert_wrong_block_round](../../crates/node/algo-rest-client/src/gossip_block_source.rs#L782), [parse_missing_block_data](../../crates/node/algo-network/src/block_fetcher.rs#L446), [parse_missing_cert_data](../../crates/node/algo-network/src/block_fetcher.rs#L453), [parse_error_response_without_latest](../../crates/node/algo-network/src/block_fetcher.rs#L396) | matched-1:many | Closed (#1504 batch 3): `decode_block_cert` (`crates/node/algo-rest-client/src/gossip_block_source.rs`) now takes an `expected_round: Round` parameter and checks both the decoded block's round and the decoded certificate's round against it, in the same order as go's `processBlockBytes` (block first, then cert), returning new typed `AlgoError::WrongBlockFromPeer`/`AlgoError::WrongCertFromPeer` variants mirroring go's `errWrongBlockFromPeer`/`errWrongCertFromPeer` — in addition to (not instead of) `sync_cert`'s later digest-mismatch retry safety net in `catchup_service.rs`, which still guards a peer that answers with the correct round but the wrong block content. `decode_block_cert_invalid_block` already covered `errCannotDecodeBlock`'s equivalent. All existing call sites that build a mock certificate for these tests were fixed to encode the round they're actually testing against (they previously all shared one `Certificate::default()` — round 0 — which the new round check now correctly rejects wherever the requested round wasn't also 0). |
| [TestRequestBlockBytesErrors](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L181) | [parse_empty_topics](../../crates/node/algo-network/src/block_fetcher.rs#L460), [decode_round_uvarint_empty](../../crates/node/algo-network/src/block_fetcher.rs#L478), [unicast_request_peer_closing](../../crates/node/algo-network/src/ws_peer.rs#L5729), [unicast_request_response_channel_closed](../../crates/node/algo-network/src/ws_peer.rs#L5664) | matched-1:many | Re-verified (#1504 batch 2): go's `requestBlockBytes` wraps a pre-cancelled `context.Context` in `fetchBlock`, asserting the request aborts with `errWsFetcherRequestFailed{cause: "context canceled"}` rather than hanging or blocking on a response that will never arrive. algod-rust's true structural analog is one layer down from `universalFetcher_test.go`'s mock-transport level: `PeerHandle::request`/`request_with_timeout` (`ws_peer.rs`, the production implementation `GossipBlockSource::fetch_from_peer` calls through `UnicastPeer`) races a pending request against a `closing: CancellationToken` in `request_with_timeout_via_tracker`'s `tokio::select!` (`ws_peer.rs#L1664`), returning `PeerError::ConnectionClosed` when it fires — the same "an in-flight/about-to-send request is aborted by an external cancellation signal and returns a well-typed error instead of hanging" property go's test proves, just expressed as a peer-closing `CancellationToken` (algod-rust's cancellation primitive throughout the network layer) rather than a `context.Context`. `unicast_request_peer_closing` pre-cancels before calling `request()` (mirroring go's pre-cancelled-context setup exactly) and `unicast_request_response_channel_closed` cancels mid-flight after the request is already pending, covering both timings go's single test collapses into one pre-cancelled case. |
| [TestGetBlockBytesHTTPErrors](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L248) | [error_display_unexpected_status](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L436), [error_display_invalid_content_type](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L426), [error_display_response_too_large](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L448) | matched-1:many | |
| [TestErrorTypes](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L297) | [error_display_block_not_available](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L398), [error_display_block_not_available_no_latest](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L408), [error_display_service_unavailable](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L416) | matched-1:many | Covers `Display` formatting for algod-rust's error variants; go additionally covers ws-fetcher-specific error types (`errWrongCertFromPeer`, `errWsFetcherRequestFailed`) which have no direct HTTP-fetcher-side equivalent. |
| [TestMaxBlockRequestSize](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L323) | [max_block_request_size_matches_uni_ens_block_req_tag_limit](../../crates/node/algo-network/src/block_fetcher.rs#L370), [make_request_topics_has_correct_keys](../../crates/node/algo-network/src/block_fetcher.rs#L324), [uvarint_round_trip](../../crates/node/algo-network/src/block_fetcher.rs#L299) | matched-1:1 | Issue #1504 batch 1: `max_block_request_size_matches_uni_ens_block_req_tag_limit` ports go's test — builds the worst-case block-request topics (max-u64 round + max-u64 nonce, the largest possible uvarint encodings) and asserts the serialized size equals `Tag::UniEnsBlockReq.max_message_size()` (`crate::tag::Tag`, 67 bytes), matching go's assertion against `protocol.UniEnsBlockReqTag.MaxMessageSize()`. |

## Gaps summary

- **Peer selection and ranking algorithm (issue #819, standalone module) — wired into live fetch paths (issue #901):** `crates/node/algo-network/src/peer_ranker.rs` is a direct port of go's `peerSelector`/`classBasedPeerSelector` (`catchup/peerSelector.go`, `catchup/classBasedPeerSelector.go`) — per-peer download-duration ranking (`PeerRanker`), penalty/eviction/upgrade bookkeeping (`HistoricStats`), and per-peer-class tolerance factors (`ClassBasedPeerSelector`/`WrappedPeerSelector`). This closes 21 of the 26 `peerSelector_test.go`/`classBasedPeerSelector_test.go` rows (5 of the 21 remain `partial`, where go's test drives a long randomized run rather than a deterministic assertion; `TestPeerSelector_PeerAddress` is not portable by design since this module has no live-network dependency to extract an address from). Issue #901 added `create_peer_selector` (mirroring go's `createPeerSelector` 4-class topology) and wired it into `GossipBlockSource`'s block-fetch peer selection (`crates/node/algo-rest-client/src/gossip_block_source.rs`), replacing round-robin, and added `RankedCatchpointSource` (`crates/node/algo-rest-client/src/ranked_catchpoint_source.rs`), wired into `AlgodSyncBackend::download_catchpoint` (`bin/algod-rust/src/commands/catchpoint_sync.rs`), replacing the single-source-only `CatchpointDownloader` call. Both feed observed download duration/failure back into the ranker after every fetch, mirroring go's `peerSelector.rankPeer`/`peerDownloadDurationToRank` call sites. `TestCreatePeerSelector` now matches; issue #917 added a `check_ledger_download` pre-flight probe stage to `RankedCatchpointSource` (mirroring go's `checkLedgerDownload`/`headLedger`), so `TestCatchpointServicePeerRank` is now `matched-1:many` (see above) — algod-rust still has no persisted, resumable `CatchpointCatchupState` machine, so this remains a test-behavior match rather than a full architecture port.
- **No staged `CatchpointCatchupService` with peer coordination:** go's `catchup/catchpointService.go` staged download pipeline (`processStageLatestBlockDownload`, `processStageBlocksDownload`, and the separate up-front `checkLedgerDownload` HEAD-probe stage) has no direct counterpart. `RankedCatchpointSource` (issue #901) ranks peers directly around the download attempt itself rather than a distinct probe stage — a peer that fails is deprioritized on retry, but there is no discrete multi-stage pipeline with per-stage peer coordination.
- **Architectural difference, not a gap:** algod-rust's `CatchupService` (`crates/core/algo-ledger/src/catchup_service.rs`) intentionally mirrors only go's certificate-driven `syncCert`/`fetchRound` path per its own doc comment, rather than the full peer-selector-backed range-sync pipeline in `catchup/service.go`; several "partial" rows above stem from this narrower scope rather than an unaddressed bug. Likewise `TestUGetBlockUnsupported` is out-of-scope because algod-rust's statically-typed per-transport fetchers (`HttpBlockFetcherSource`, `block_fetcher.rs`) have no dynamic peer-type fallback to fail on.
- **`TestSyncRound`** is matched not inside a catchup-service unit test but via algod-rust's REST v2 `/v2/ledger/sync` endpoint tests (`get_sync_round_*`, `set_sync_round_*`, `unset_sync_round_success` in `algo-rest-api`), since algod-rust exposes disable/enable-sync-round control at the API layer.
- **Production infrastructure `sync_cert`/`CatchupLedger` don't yet have (#1504 batch 2 audit):** three of `catchup_service.rs`'s remaining `partial` rows trace to the same root cause — go's `Service`/`CatchupLedger` interface exposes strictly more state and structure than algod-rust's cert-driven `CatchupService`/`CatchupLedger` trait does, and closing them needs new production surface rather than a test. (1) No `catchupRetryLimit`-equivalent bound: `sync_cert`'s retry loop (confirmed via `grep -n "catchupRetryLimit\|RETRY_LIMIT"`) retries indefinitely on persistent fetch failure rather than giving up after go's fixed 500 attempts — `persistent_failure_retries_until_shutdown` pins the current (unbounded) behavior as intentional. (2) No commit-backpressure signal: `CatchupLedger` (confirmed via its full method list) has no `behind_deltas`/`writing_catchpoint`-style method a ledger implementation could use to make `sync_cert`/`sync_pass` pause, unlike go's `mockedLedger.behindDeltas`-driven backpressure check in `fetchAndWrite`. (3) No typed exit-reason: `sync_cert`/`sync_pass`/`sync_pass_inner` all return `()` (confirmed via `grep -n "enum.*Exit\|ExitReason"` finding no match), so there is nothing analogous to go's `pipelinedFetch`'s typed `error` return (`errCatchupNoPeer`/`errFetchRetryLimit`/`errCatchupBehindDeltas`/etc.) for a caller or test to assert on. A fourth, `decode_block_cert` (`gossip_block_source.rs`), takes no expected-round parameter and performs no round-mismatch check at all, unlike go's `processBlockBytes`'s `errWrongCertFromPeer`/`errWrongBlockFromPeer`; algod-rust instead catches a wrong-round/wrong-block peer response later, at commit time, via `sync_cert`'s full block-digest comparison against the agreement certificate.
