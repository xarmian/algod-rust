# Phase 17 Test Parity — catchup

| go-algorand test | algod-rust test(s) | status | notes |
|---|---|---|---|
| [TestCatchpointServicePeerRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/catchpointService_test.go#L90) | [single_peer_failure_is_ranked_without_panicking](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L447), [check_ledger_download_probe_deprioritizes_a_404_peer_before_any_real_download](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L505), [check_ledger_download_fails_when_every_peer_lacks_the_catchpoint](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L560) | matched-1:many | Issue #901 wired `make_catchpoint_peer_selector` into `RankedCatchpointSource` (`crates/node/algo-rest-client/src/ranked_catchpoint_source.rs`), used by `AlgodSyncBackend::download_catchpoint` — a peer that fails is ranked and deprioritized without panicking, matching go's crash-safety assertion. Issue #917 added a `check_ledger_download` pre-flight probe stage (a HEAD request via `CatchpointDownloader::probe_availability`, mirroring go's `checkLedgerDownload`/`headLedger`) that runs ahead of the real transfer and ranks an unavailable-catchpoint peer `PEER_RANK_NO_CATCHPOINT_FOR_ROUND` before any real download is attempted — the same two-stage (`checkLedgerDownload` then the real fetch) sequencing go's `CatchpointCatchupService.Start()`/`run()` uses, sharing peer-selector state across both stages. algod-rust still has no persisted, resumable `CatchpointCatchupState` machine (`processStageLedgerDownload`/`processStageLatestBlockDownload`/`processStageBlocksDownload`/`processStageSwitch` remain unported — algod-rust's `SyncOrchestrator` phase state machine, `crates/core/algo-ledger/src/sync/state_machine.rs`, already owns the equivalent block-lookback/switch phases, so porting go's version would duplicate rather than replace it), which is why this stays a test-behavior match rather than an architecture-level one. |
| [TestProcessStageBlocksDownloadNilCert](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/catchpointService_test.go#L129) | — | not-implemented | Regression guard that `StoreBlock` never receives a nil certificate after a block is already in the ledger, specific to go's persisted `processStageBlocksDownload` stage. Issue #917 added a `check_ledger_download` pre-flight probe stage (see `TestCatchpointServicePeerRank` above) but deliberately did not port go's fuller resumable `CatchpointCatchupState` machine that `processStageBlocksDownload` belongs to — algod-rust's `SyncOrchestrator` (`crates/core/algo-ledger/src/sync/state_machine.rs`) already owns the equivalent block-lookback-after-catchpoint phase with its own certificate handling, so there is no `processStageBlocksDownload`-shaped call site to regress here. |
| [TestClassBasedPeerSelector_makeClassBasedPeerSelector](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L48) | [class_based_selector_preserves_construction_order](../../crates/node/algo-network/src/peer_ranker.rs#L1367) | matched-1:1 | Issue #819: `ClassBasedPeerSelector` (`peer_ranker.rs`) ports go's `classBasedPeerSelector`/`wrappedPeerSelector` — per-peer-class tolerance factors and priority-ordered selector list. Issue #901 wired it into `GossipBlockSource` (block fetch) and `RankedCatchpointSource` (catchpoint fetch). |
| [TestClassBasedPeerSelector_rankPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L79) | [class_based_selector_tracks_failures_only_for_the_owning_class](../../crates/node/algo-network/src/peer_ranker.rs#L1428) | matched-1:1 | |
| [TestClassBasedPeerSelector_peerDownloadDurationToRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L173) | [class_based_peer_download_duration_to_rank_uses_first_matching_class](../../crates/node/algo-network/src/peer_ranker.rs#L1509) | matched-1:1 | |
| [TestClassBasedPeerSelector_getNextPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L227) | [disabled_classes_are_skipped_and_reset_together_once_all_disabled](../../crates/node/algo-network/src/peer_ranker.rs#L1589) | matched-1:1 | |
| [TestClassBasedPeerSelector_integration](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/classBasedPeerSelector_test.go#L414) | [catchpoint_selector_prefers_relays_then_falls_back_to_archival_on_failures](../../crates/node/algo-network/src/peer_ranker.rs#L1690) | matched-1:1 | |
| [TestNoPeersAvailable](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L45) | [gossip_sync_backend_gossip_only_no_peers_fails](../../bin/algod-rust/src/commands/catchpoint_sync.rs#L1155), [handoff_selects_http_only_when_no_peers](../../bin/algod-rust/src/commands/catchpoint_sync.rs#L1223) | partial | Rust checks "no peers available" at the sync-backend/handoff layer rather than inside a dedicated `ledgerFetcher`, since algod-rust has no discrete ledger-fetcher-with-peer-selector component. |
| [TestNonParsableAddress](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L54) | [probe_availability_rejects_non_parsable_base_url](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L847), [download_rejects_non_parsable_base_url_without_retrying](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L861) | matched-1:many | Issue #976: `CatchpointDownloader::new`/`with_config` don't parse the base URL eagerly (they only trim a trailing slash), so the equivalent rejection point to go's `getPeerLedger` is the first network call — `reqwest`'s request builder fails to parse a malformed URL and `.send()` surfaces it as `Err`, never a panic, and `is_retryable` correctly treats that failure as permanent (not retried). |
| [TestLedgerFetcherErrorResponseHandling](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L63) | [read_chunk_with_stall_check_times_out_on_a_stalled_response](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L702), [download_retries_on_mid_stream_connection_drop](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L583) | partial | Covers stall-timeout and mid-stream-drop retry, not the full HTTP-status/error-response matrix go's ledgerFetcher test exercises. |
| [TestLedgerFetcher](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L130) | [download_retries_on_mid_stream_connection_drop](../../crates/node/algo-rest-client/src/catchpoint_download.rs#L583) | partial | algod-rust's `CatchpointDownloadClient::download` is the ledgerFetcher analog (explicitly ported from `catchup/ledgerFetcher.go`, see doc comments at the top of the file), but test coverage of the full success-path download-and-verify flow is narrower than go's. |
| [TestLedgerFetcherP2P](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/ledgerFetcher_test.go#L187) | — | not-implemented | algod-rust's catchpoint download is HTTP-only (`catchpoint_download.rs`); no libp2p/P2P transport variant of the ledger/catchpoint-file fetch exists. |
| [TestPeerSelector_PeerAddress](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L66) | — | not-implemented | Go's `peerAddress()` extracts a string address from a live `network.Peer` (`HTTPPeer`/`UnicastPeer`). Issue #819's `PeerRanker` (`peer_ranker.rs`) is a standalone algorithm module with no dependency on algod-rust's live network stack, so peers are already identified by a caller-supplied `String` id (`PeerSelectorPeer::peer_id`) — there is no address-extraction step to port. |
| [TestPeerSelector_DownloadDurationToRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L80) | [download_duration_to_rank_matches_go_fixture](../../crates/node/algo-network/src/peer_ranker.rs#L897) | matched-1:1 | Issue #819: `download_duration_to_rank` in `peer_ranker.rs` ports go's `downloadDurationToRank` bit-for-bit (same clamp-then-map formula), pinned against the same fixture values as the go test. |
| [TestPeerSelector_RankPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L121) | [rank_peer_moves_peers_between_pools_and_tracks_membership](../../crates/node/algo-network/src/peer_ranker.rs#L973) | matched-1:1 | |
| [TestPeerSelector_PeerDownloadRanking](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L180) | [peer_download_duration_to_rank_uses_the_peers_class_bucket](../../crates/node/algo-network/src/peer_ranker.rs#L1053) | matched-1:1 | |
| [TestPeerSelector_FindMissingPeer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L232) | [find_peer_returns_negative_indices_when_missing](../../crates/node/algo-network/src/peer_ranker.rs#L1034) | matched-1:1 | |
| [TestPeerSelector_HistoricData](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L247) | [ranker_never_exceeds_class_upper_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1240), [ranker_never_goes_under_class_lower_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1263) | partial | Go's version drives 1000 iterations through a SHA-based deterministic PRNG asserting the best-performing peer (`a3`) is selected at least as often as its slower siblings and failing peers get zero selections. algod-rust's `peer_ranker.rs` tests pin the same underlying `historicStats.push`/class-bound invariants deterministically (bounds never violated across repeated pushes) rather than porting the randomized long-run distribution assertion verbatim. |
| [TestPeerSelector_PeersDownloadError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L321) | [repeated_download_failures_evict_the_peer_and_the_next_class_takes_over](../../crates/node/algo-network/src/peer_ranker.rs#L1285) | partial | Same randomized-distribution style as `TestPeerSelector_HistoricData`; algod-rust asserts the underlying eviction/pool-membership mechanics deterministically instead. |
| [TestPeerSelector_Penalty](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L397) | [penalty_never_demotes_and_reset_never_promotes_past_a_demotion](../../crates/node/algo-network/src/peer_ranker.rs#L1201) | partial | Same randomized-distribution style; algod-rust pins the penalty formula's bound/monotonicity invariants deterministically. |
| [TestPeerSelector_PeerDownloadDurationToRank](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L456) | [peer_download_duration_to_rank_covers_all_four_class_buckets](../../crates/node/algo-network/src/peer_ranker.rs#L1101) | matched-1:1 | Exercises all four `PeerClassKind` rank buckets in one ranker, mirroring go's four-peer-class fixture. |
| [TestPeerSelector_LowerUpperBounds](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L500) | [lower_upper_bounds_match_go_fixture](../../crates/node/algo-network/src/peer_ranker.rs#L934) | matched-1:1 | |
| [TestPeerSelector_FullResetRequestPenalty](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L523) | [full_reset_clears_all_gaps](../../crates/node/algo-network/src/peer_ranker.rs#L1224) | matched-1:1 | |
| [TestPeerSelector_PenaltyBounds](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L538) | [penalty_never_demotes_and_reset_never_promotes_past_a_demotion](../../crates/node/algo-network/src/peer_ranker.rs#L1201) | matched-1:1 | |
| [TestPeerSelector_ClassUpperBound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L564) | [ranker_never_exceeds_class_upper_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1240) | matched-1:1 | |
| [TestPeerSelector_ClassLowerBound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L599) | [ranker_never_goes_under_class_lower_bound](../../crates/node/algo-network/src/peer_ranker.rs#L1263) | matched-1:1 | |
| [TestPeerSelector_EvictionAndUpgrade](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L629) | [repeated_download_failures_evict_the_peer_and_the_next_class_takes_over](../../crates/node/algo-network/src/peer_ranker.rs#L1285) | matched-1:1 | |
| [TestPeerSelector_RefreshAvailablePeers](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/peerSelector_test.go#L666) | [refresh_adds_new_peers_and_removes_dropped_ones](../../crates/node/algo-network/src/peer_ranker.rs#L1322) | matched-1:1 | |
| — (new algod-rust-only regression, issue #901) | [ranked_selector_prefers_the_reliable_peer_after_the_unreliable_one_fails](../../crates/node/algo-rest-client/src/gossip_block_source.rs#L714) | matched-1:1 | No single go test asserts this end-to-end (go's wiring long predates a dedicated regression); pins that `GossipBlockSource`'s live block-fetch peer selection — not just the standalone `peer_ranker.rs` algorithm — actually deprioritizes a peer that fails in favor of a reliable one on subsequent fetches. |
| — (new algod-rust-only regression, issue #901) | [ranked_source_prefers_the_reliable_peer_after_the_unreliable_one_fails](../../crates/node/algo-rest-client/src/ranked_catchpoint_source.rs#L280) | matched-1:1 | Same wiring-level assertion as above, for the catchpoint-file download path (`RankedCatchpointSource`). |
| [TestServiceFetchBlocksSameRange](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L135) | [periodic_sync_catches_up_without_any_certificate](../../crates/core/algo-ledger/src/catchup_service.rs#L1188), [happy_path_fetch_and_commit](../../crates/core/algo-ledger/src/catchup_service.rs#L1451) | partial | go's `Service.sync()` fetches an arbitrary range behind a peer-selector-driven pipeline; algod-rust's `CatchupService` is certificate/round-driven (mirrors the `syncCert`/`fetchRound` path only, per its own doc comment), so range-catchup semantics are narrower. |
| [TestSyncRound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L207) | [get_sync_round_success](../../crates/node/algo-rest-api/tests/integration.rs#L8124), [get_sync_round_not_set](../../crates/node/algo-rest-api/tests/integration.rs#L8143), [set_sync_round_success](../../crates/node/algo-rest-api/tests/integration.rs#L8195), [set_sync_round_invalid](../../crates/node/algo-rest-api/tests/integration.rs#L8211), [unset_sync_round_success](../../crates/node/algo-rest-api/tests/integration.rs#L8228) | matched-1:many | Go's `SetDisableSyncRound`/`GetDisableSyncRound`/`UnsetDisableSyncRound` behavior is exposed and tested via algod-rust's REST v2 `/v2/ledger/sync` endpoints rather than an internal catchup-service unit test. |
| [TestPeriodicSync](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L297) | [periodic_sync_fetches_blocks_in_parallel](../../crates/core/algo-ledger/src/catchup_service.rs#L1892), [periodic_sync_with_parallelism_one_stays_serial](../../crates/core/algo-ledger/src/catchup_service.rs#L1927) | matched-1:many | |
| [TestServiceFetchBlocksOneBlock](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L361) | [happy_path_fetch_and_commit](../../crates/core/algo-ledger/src/catchup_service.rs#L1451) | matched-1:1 | |
| [TestAbruptWrites](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L419) | [skips_already_committed_round](../../crates/core/algo-ledger/src/catchup_service.rs#L1330) | partial | go's test races concurrent block writes against the sync loop; algod-rust's test only checks the loop skips an already-committed round, not full concurrency-safety under a racing writer. |
| [TestServiceFetchBlocksMultiBlocks](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L479) | [periodic_sync_fetches_blocks_in_parallel](../../crates/core/algo-ledger/src/catchup_service.rs#L1892) | matched-1:1 | |
| [TestServiceFetchBlocksMalformed](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L536) | [digest_mismatch_triggers_retry](../../crates/core/algo-ledger/src/catchup_service.rs#L1513), [contents_mismatch_triggers_retry](../../crates/core/algo-ledger/src/catchup_service.rs#L1958) | matched-1:many | |
| [TestOnSwitchToUnSupportedProtocol1](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L580) | [sync_pass_interrupted_in_initial_loop](../../crates/core/algo-ledger/src/catchup_service.rs#L2094) | matched-1:1 | Issue #976: interruption on the very first round of a pass — direct, synchronous call into `sync_pass_inner` (rather than the background-thread `CatchupService::start`) so the outcome (no block ever committed) is asserted without polling. |
| [TestOnSwitchToUnSupportedProtocol2](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L599) | [sync_pass_interrupted_mid_loop](../../crates/core/algo-ledger/src/catchup_service.rs#L2120) | matched-1:1 | Issue #976: interruption partway through a pass, after several supported rounds were already committed — sync stops exactly at the last supported round. |
| [TestOnSwitchToUnSupportedProtocol3](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L621) | [sync_pass_interrupted_with_short_notice](../../crates/core/algo-ledger/src/catchup_service.rs#L2151) | matched-1:1 | Issue #976: short-notice interruption — a worker may prefetch the unsupported round concurrently with the last supported one (`parallel_blocks=2`), but the strict in-order apply loop guarantees the boundary is still honored exactly. |
| [TestOnSwitchToUnSupportedProtocol4](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L647) | [sync_pass_interrupted_restart_at_switch_round](../../crates/core/algo-ledger/src/catchup_service.rs#L2182) | matched-1:1 | Issue #976: restart-at-switch-round variant — the ledger already sits at the switch boundary when a fresh pass starts, so it must decline to fetch anything at all. `sync_cert_bails_out_for_unsupported_round` (`catchup_service.rs#L2210`) adds the same guard's coverage through the certificate-driven path (`sync_cert`/`fetchRound`'s equivalent). |
| [TestCatchupUnmatchedCertificate](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L912) | [fork_detection_fires_on_digest_mismatch_with_cert](../../crates/core/algo-ledger/src/catchup_service.rs#L1692), [no_fork_detection_without_fetched_cert](../../crates/core/algo-ledger/src/catchup_service.rs#L1748), [no_fork_when_fetched_cert_matches_agreement](../../crates/core/algo-ledger/src/catchup_service.rs#L1785) | matched-1:many | go's `syncCert` (called directly here) is algod-rust's `sync_cert`; the fork-detection tests exercise the same code path with unmatched/matched certs. |
| [TestCreatePeerSelector](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L958) | [create_peer_selector_matches_go_topology](../../crates/node/algo-network/src/peer_ranker.rs#L1728) | matched-1:1 | Issue #901 ported `create_peer_selector` (`peer_ranker.rs`), mirroring go's `createPeerSelector` topology exactly: `ConnectedOut`, `PhonebookRelays`, `PhonebookArchivalNodes`, `ConnectedIn` classes with tolerance factors 3/3/10/3. It is wired into `GossipBlockSource`'s block-fetch peer selection (`crates/node/algo-rest-client/src/gossip_block_source.rs`), replacing the previous round-robin. |
| [TestServiceStartStop](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L980) | [stop_without_certs_is_clean](../../crates/core/algo-ledger/src/catchup_service.rs#L1256), [drop_triggers_stop](../../crates/core/algo-ledger/src/catchup_service.rs#L1267) | matched-1:many | |
| [TestSynchronizingTime](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L993) | [synchronizing_time_is_zero_before_any_sync_and_nonzero_during_one](../../crates/core/algo-ledger/src/catchup_service.rs#L2095), [synchronizing_time_is_zero_immediately_after_stop](../../crates/core/algo-ledger/src/catchup_service.rs#L2142) | matched-1:many | Issue #917 added `CatchupService::synchronizing_time()`, mirroring go's `Service.SynchronizingTime()`/`syncStartNS`: zero when no periodic sync pass is in flight, else the elapsed time since the current pass ([`sync_pass`], go's `sync()`) started. Unlike go's test (which pokes `syncStartNS` directly, same package), algod-rust's tests drive the real state transition through a slow fetcher since the field is private, exercising `sync_pass`'s own start/reset logic rather than just the getter. |
| [TestDownloadBlocksToSupportStateProofs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1006) | [lookback_for_stateproofs_support_matches_go_test_oracle](../../crates/core/algo-ledger/src/voters_tracker.rs#L514) | matched-1:1 | Issue #976: `lookback_for_stateproofs_support` (`voters_tracker.rs#L174`) ports go's `lookbackForStateproofsSupport` formula bit-for-bit (built on the already-ported `get_oldest_expected_state_proof`/`voters_round_for_state_proof_round`) and is pinned against the same three-case oracle go's test uses. Wired into `SyncOrchestrator::run_download_lookback` (`crates/core/algo-ledger/src/sync/mod.rs`), which now downloads `max(MAX_TXN_LIFE, lookback_for_stateproofs_support(..))` blocks instead of always the fixed `MAX_TXN_LIFE` window, via the new `catchpoint::download_lookback_blocks_with_lookback`. |
| [TestServiceLedgerUnavailable](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1046) | [fetch_failure_does_not_crash](../../crates/core/algo-ledger/src/catchup_service.rs#L1295) | partial | Covers the fetch-failure-doesn't-crash case generally, not specifically a ledger that lags behind on committing deltas. |
| [TestServiceNoBlockForRound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1094) | [retry_logic_retries_on_fetch_failure](../../crates/core/algo-ledger/src/catchup_service.rs#L1592), [persistent_failure_retries_until_shutdown](../../crates/core/algo-ledger/src/catchup_service.rs#L1624) | partial | go's regression is specifically about bounding retry-driven log spam (`catchupRetryLimit`); algod-rust's tests cover retry/backoff behavior but don't assert a log-message-count bound. |
| [TestPipelinedFetchExitReason](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/service_test.go#L1145) | [persistent_failure_retries_until_shutdown](../../crates/core/algo-ledger/src/catchup_service.rs#L1624) | partial | go's test asserts the specific exit-reason value surfaced in the "finished catching up" log line; algod-rust's test covers shutdown-on-persistent-failure but not a typed/logged exit-reason. |
| [TestUGetBlockWs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L42) | [parse_success_response](../../crates/node/algo-network/src/block_fetcher.rs#L364), [make_request_topics_has_correct_keys](../../crates/node/algo-network/src/block_fetcher.rs#L324), [topic_keys_match_go_constants](../../crates/node/algo-network/src/block_fetcher.rs#L485) | partial | algod-rust's `block_fetcher.rs` implements the gossip/unicast-peer request-topics and response-parsing wire format go's `universalBlockFetcher`(ws path) uses, but there is no end-to-end fetch-over-a-live-unicast-peer test as in go. |
| [TestUGetBlockHTTP](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L87) | [block_url_round_zero](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L324), [accessors](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L362), [with_client_constructor](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L369) | partial | `http_block_fetcher.rs` is the HTTP-peer analog of `universalBlockFetcher`; covered at the URL-construction/accessor unit level rather than a full fetch-against-a-live-block-service integration test. |
| [TestUGetBlockUnsupported](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L135) | — | out-of-scope | Guards a Go runtime type-switch fallback (`fetchBlock` erroring when the peer is neither `HTTPPeer` nor `UnicastPeer`). algod-rust's fetchers are statically typed per transport (`HttpBlockFetcherSource`, gossip `block_fetcher.rs`), so this failure mode cannot occur — there is no dynamic peer-type dispatch to fall through. |
| [TestProcessBlockBytesErrors](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L149) | [parse_missing_block_data](../../crates/node/algo-network/src/block_fetcher.rs#L446), [parse_missing_cert_data](../../crates/node/algo-network/src/block_fetcher.rs#L453), [parse_error_response_without_latest](../../crates/node/algo-network/src/block_fetcher.rs#L396) | partial | Covers missing-topic/error-response parsing; go additionally asserts wrong-cert-round and wrong-block-round mismatch errors specifically, which are exercised in algod-rust at the `catchup_service.rs` digest-mismatch level (`digest_mismatch_triggers_retry`) rather than inside the fetcher parser itself. |
| [TestRequestBlockBytesErrors](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L181) | [parse_empty_topics](../../crates/node/algo-network/src/block_fetcher.rs#L460), [decode_round_uvarint_empty](../../crates/node/algo-network/src/block_fetcher.rs#L478) | partial | Covers empty/malformed-topic decode errors; no context-cancellation-during-request test as in go. |
| [TestGetBlockBytesHTTPErrors](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L248) | [error_display_unexpected_status](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L436), [error_display_invalid_content_type](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L426), [error_display_response_too_large](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L448) | matched-1:many | |
| [TestErrorTypes](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L297) | [error_display_block_not_available](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L398), [error_display_block_not_available_no_latest](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L408), [error_display_service_unavailable](../../crates/node/algo-rest-client/src/http_block_fetcher.rs#L416) | matched-1:many | Covers `Display` formatting for algod-rust's error variants; go additionally covers ws-fetcher-specific error types (`errWrongCertFromPeer`, `errWsFetcherRequestFailed`) which have no direct HTTP-fetcher-side equivalent. |
| [TestMaxBlockRequestSize](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/catchup/universalFetcher_test.go#L323) | [make_request_topics_has_correct_keys](../../crates/node/algo-network/src/block_fetcher.rs#L324), [uvarint_round_trip](../../crates/node/algo-network/src/block_fetcher.rs#L299) | partial | Covers the request-topics wire format the size constant is derived from; no dedicated constant-matches-protocol-package assertion as in go. |

## Gaps summary

- **Peer selection and ranking algorithm (issue #819, standalone module) — wired into live fetch paths (issue #901):** `crates/node/algo-network/src/peer_ranker.rs` is a direct port of go's `peerSelector`/`classBasedPeerSelector` (`catchup/peerSelector.go`, `catchup/classBasedPeerSelector.go`) — per-peer download-duration ranking (`PeerRanker`), penalty/eviction/upgrade bookkeeping (`HistoricStats`), and per-peer-class tolerance factors (`ClassBasedPeerSelector`/`WrappedPeerSelector`). This closes 21 of the 26 `peerSelector_test.go`/`classBasedPeerSelector_test.go` rows (5 of the 21 remain `partial`, where go's test drives a long randomized run rather than a deterministic assertion; `TestPeerSelector_PeerAddress` is not portable by design since this module has no live-network dependency to extract an address from). Issue #901 added `create_peer_selector` (mirroring go's `createPeerSelector` 4-class topology) and wired it into `GossipBlockSource`'s block-fetch peer selection (`crates/node/algo-rest-client/src/gossip_block_source.rs`), replacing round-robin, and added `RankedCatchpointSource` (`crates/node/algo-rest-client/src/ranked_catchpoint_source.rs`), wired into `AlgodSyncBackend::download_catchpoint` (`bin/algod-rust/src/commands/catchpoint_sync.rs`), replacing the single-source-only `CatchpointDownloader` call. Both feed observed download duration/failure back into the ranker after every fetch, mirroring go's `peerSelector.rankPeer`/`peerDownloadDurationToRank` call sites. `TestCreatePeerSelector` now matches; issue #917 added a `check_ledger_download` pre-flight probe stage to `RankedCatchpointSource` (mirroring go's `checkLedgerDownload`/`headLedger`), so `TestCatchpointServicePeerRank` is now `matched-1:many` (see above) — algod-rust still has no persisted, resumable `CatchpointCatchupState` machine, so this remains a test-behavior match rather than a full architecture port.
- **No staged `CatchpointCatchupService` with peer coordination:** go's `catchup/catchpointService.go` staged download pipeline (`processStageLatestBlockDownload`, `processStageBlocksDownload`, and the separate up-front `checkLedgerDownload` HEAD-probe stage) has no direct counterpart. `RankedCatchpointSource` (issue #901) ranks peers directly around the download attempt itself rather than a distinct probe stage — a peer that fails is deprioritized on retry, but there is no discrete multi-stage pipeline with per-stage peer coordination.
- **Architectural difference, not a gap:** algod-rust's `CatchupService` (`crates/core/algo-ledger/src/catchup_service.rs`) intentionally mirrors only go's certificate-driven `syncCert`/`fetchRound` path per its own doc comment, rather than the full peer-selector-backed range-sync pipeline in `catchup/service.go`; several "partial" rows above stem from this narrower scope rather than an unaddressed bug. Likewise `TestUGetBlockUnsupported` is out-of-scope because algod-rust's statically-typed per-transport fetchers (`HttpBlockFetcherSource`, `block_fetcher.rs`) have no dynamic peer-type fallback to fail on.
- **`TestSyncRound`** is matched not inside a catchup-service unit test but via algod-rust's REST v2 `/v2/ledger/sync` endpoint tests (`get_sync_round_*`, `set_sync_round_*`, `unset_sync_round_success` in `algo-rest-api`), since algod-rust exposes disable/enable-sync-round control at the API layer.
