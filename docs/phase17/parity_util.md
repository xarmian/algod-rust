# Phase 17 Test Parity — util (and subpackages)

| go-algorand test | algod-rust test(s) | status | notes |
|---|---|---|---|
| [TestBitset](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L22) | [round_trips_through_marshal_unmarshal](../../crates/node/algo-network/src/bloom.rs#L408), [golden_case1_small_ascii_elements](../../crates/node/algo-network/src/bloom.rs#L325) | partial | Go exhaustively walks all 1024 bit positions of the raw bitset; Rust exercises `set`/`test` only through the higher-level `Filter` API in golden/round-trip cases, not a dedicated bit-by-bit sweep. |
| [TestFilter](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L37) | [golden_case1_small_ascii_elements](../../crates/node/algo-network/src/bloom.rs#L325) | matched-1:1 | |
| [TestOptimal](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L50) | [golden_optimal_matches_go_for_ten_elements_one_percent](../../crates/node/algo-network/src/bloom.rs#L352), [golden_optimal_matches_go_for_zero_elements](../../crates/node/algo-network/src/bloom.rs#L378) | partial | Go statistically validates the actual false-positive rate across 2000/20000/200000-element filters; Rust only pins two exact `(size_bits, num_hashes)` outputs against the go reference, not a live FP-rate measurement. |
| [TestOptimalSize](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L122) | [binary_marshal_length_matches_optimal_sizing](../../crates/node/algo-network/src/bloom.rs#L451) | partial | Go checks bits-per-element for a 150000-element/1e-10 filter; Rust checks marshal length consistency for a 10-element/0.01 filter — same sizing formula, different assertion and parameters. |
| [TestIncompressible](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L136) | | not-implemented | No test verifies bloom-filter output resists `flate` compression (an entropy/randomness-quality check). |
| [TestMarshalJSON](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L158) | | not-implemented | `Filter` has no JSON (de)serialization in Rust — only `marshal_binary`/`unmarshal_binary`, since algod-rust's bloom filters travel over the binary/msgpack wire format, not JSON. |
| [TestMaxHashes](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L200) | [unmarshal_rejects_too_many_hashes](../../crates/node/algo-network/src/bloom.rs#L436) | matched-1:1 | Go exercises the bound via JSON marshal/unmarshal; Rust exercises the same `MAX_HASHES` bound via binary unmarshal — same invariant. |
| [TestEmptyFilter](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L239) | [unmarshal_rejects_short_data](../../crates/node/algo-network/src/bloom.rs#L420) | partial | Go fuzzes every truncation prefix `0..len(marshaled)` of a real filter and calls `Test` safely on whatever decodes; Rust only checks two fixed short byte-strings are rejected outright. |
| [TestBinaryMarshalLength](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L255) | [binary_marshal_length_matches_optimal_sizing](../../crates/node/algo-network/src/bloom.rs#L451) | partial | Go sweeps 6 element-counts × 6 FP-rates (36 cases); Rust checks a single case. |
| [TestBloomFilterMemoryConsumption](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L272) | | out-of-scope | Uses Go's `testing.Benchmark`/`ReportAllocs` heap-allocation accounting, which has no Rust equivalent concept. |
| [TestBloomFilterReferenceHash](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/bloom/bloom_test.go#L377) | [golden_case1_small_ascii_elements](../../crates/node/algo-network/src/bloom.rs#L325), [golden_case2_nonzero_prefix_more_elements](../../crates/node/algo-network/src/bloom.rs#L336), [golden_case3_optimal_sized_filter_txid_like_elements](../../crates/node/algo-network/src/bloom.rs#L358), [golden_case4_empty_filter](../../crates/node/algo-network/src/bloom.rs#L384), [golden_case5_digest_sized_elements](../../crates/node/algo-network/src/bloom.rs#L392) | matched-1:many | Rust's golden cases serve the identical purpose (pin exact byte output against a captured go-algorand reference) with a broader set of fixtures than the single N=3 case upstream. |
| [TestIsDefaultValue](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/codecs/json_test.go#L37) | [save_non_default_only_includes_version_and_overridden_fields](../../crates/node/algo-config/src/lib.rs#L3907) | partial | Go unit-tests the standalone `isDefaultValue` helper directly; Rust tests the same "only overridden fields get written" behavior end-to-end through `Local::save_non_default_to_path` rather than an isolated helper. |
| [TestSaveObjectToFile](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/codecs/json_test.go#L63) | [save_non_default_round_trips_back_to_the_same_config](../../crates/node/algo-config/src/lib.rs#L3930) | partial | Go tests the generic `SaveObjectToFile` (pretty vs. compact JSON) on an arbitrary struct; Rust tests the config-specific save/round-trip path only. |
| [TestWriteNonDefaultValue](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/codecs/json_test.go#L101) | [save_non_default_only_includes_version_and_overridden_fields](../../crates/node/algo-config/src/lib.rs#L3907) | matched-1:1 | `Local::save_non_default_to_path` is algod-rust's direct port of `WriteNonDefaultValues`/`SaveNonDefaultValuesToFile`, per the doc comment at `algo-config/src/lib.rs:42`. |
| [TestTimedWaitSignal](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/condvar/timedwait_test.go#L31) | | out-of-scope | Go's `sync.Cond` has no built-in timeout, so `util/condvar.TimedWait` is a hand-rolled wrapper around it; Rust's `std::sync::Condvar::wait_timeout` provides this natively, so no equivalent wrapper (or its tests) exists or is needed. |
| [TestTimedWaitBroadcast](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/condvar/timedwait_test.go#L56) | | out-of-scope | Same as above — native `Condvar::wait_timeout` covers this. |
| [TestTimedWaitTimeout](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/condvar/timedwait_test.go#L81) | | out-of-scope | Same as above — native `Condvar::wait_timeout` covers this. |
| [TestInMemoryDisposal](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L40) | | out-of-scope | Issue #828 investigation: go's `Accessor.Close`/dispose exists because `database/sql` handles are explicitly closed and the underlying named in-memory DB must be dropped by hand. `SqliteLedger`/`ClaimedWallets` hold an owned `rusqlite::Connection` (`crates/core/algo-ledger/src/sqlite.rs`, `crates/node/algo-kmd/src/sqlite.rs`) that Rust's `Drop` closes deterministically on scope exit — there is no separate disposal API to test. |
| [TestInMemoryUniqueDB](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L87) | | out-of-scope | Issue #828 investigation: go needs unique names because its in-memory DBs use `file::memory:?cache=shared`, where two `Accessor`s can otherwise collide on the same shared-cache name. `SqliteLedger::open_in_memory` (`sqlite.rs:2291`) calls plain `Connection::open_in_memory()`, which SQLite gives a private, unshared in-memory database per call — grep confirms every call site (`sqlite.rs`, 50+ test call sites) uses one connection per in-memory ledger, never two handles sharing one memory DB — so the naming-collision problem go's wrapper solves does not arise. |
| [TestDBConcurrency](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L121) | [wal_mode_allows_concurrent_read_during_uncommitted_write](../../crates/core/algo-ledger/src/sqlite.rs) | matched-1:many | Issue #828: added directly against the real WAL semantics `SqliteLedger::open_read_snapshot` (`sqlite.rs:2508`) depends on in production ("SQLite WAL allows concurrent readers") — two independent connections against the same on-disk tracker file, one holding an uncommitted write while the other reads concurrently without blocking or erroring. |
| [TestDBConcurrencyRW](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L239) | [wal_mode_allows_concurrent_read_during_uncommitted_write](../../crates/core/algo-ledger/src/sqlite.rs) | matched-1:many | Same new test — covers the same concurrent read/write coexistence property go's Accessor-mediated RW test checks. |
| [TestResettingTransactionWarnDeadline](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L363) | | out-of-scope | Issue #828 investigation: this is a diagnostic-only warn-log emitted when a transaction runs long, with no effect on transaction correctness or outcome. algod-rust does not model go's telemetry/logging pipeline generally (see `telemetry_only_fields_are_accepted_but_ignored_not_modeled`, `algo-config/src/lib.rs:4074`); porting a warn-log-only mechanism with no behavioral consequence is not a real gap. |
| [TestSetSynchronousMode](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L402) | [set_synchronous_mode_overrides_both_schemas](../../crates/core/algo-ledger/src/sqlite.rs#L6463), [set_synchronous_mode_clamps_out_of_range_values](../../crates/core/algo-ledger/src/sqlite.rs#L6471), [clamp_synchronous_mode_clamps_both_directions](../../crates/core/algo-ledger/src/sqlite.rs#L6576) | partial | Go tests `SynchronousMode` on the generic `db.Accessor`; Rust tests the same PRAGMA-clamping/override behavior but embedded directly in the ledger's own sqlite setup, not a reusable wrapper API. |
| [TestReadingWhileWriting](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L439) | [wal_mode_allows_concurrent_read_during_uncommitted_write](../../crates/core/algo-ledger/src/sqlite.rs) | matched-1:many | Issue #828: new test asserts a concurrent reader observes the pre-transaction row count while the writer's insert is still uncommitted, then observes the post-commit count on a fresh read transaction — the same snapshot-isolation property. |
| [TestLockingTableWhileWritingWAL](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L483) | [wal_mode_allows_concurrent_read_during_uncommitted_write](../../crates/core/algo-ledger/src/sqlite.rs) | matched-1:many | Same new test — asserts the concurrent read does not block/error while a write transaction is open, against a database `open_with_prefix` always creates in WAL mode (`open_split`, `sqlite.rs:2248`). |
| [TestLockingTableWhileWritingJournal](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/dbutil_test.go#L489) | | out-of-scope | Issue #828 investigation: `open_split` unconditionally sets `PRAGMA journal_mode=WAL` on every ledger connection (`sqlite.rs:378-381`) with no rollback-journal-mode code path anywhere in algod-rust to exercise; there is nothing to test. |
| [TestInitialize](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/initialize_test.go#L62) | [resources_ctype_migration_upgrades_old_shape_db](../../crates/core/algo-ledger/src/sqlite.rs), [algod_rust_meta_retire_migrates_round_into_acctrounds_and_drops_table](../../crates/core/algo-ledger/src/sqlite.rs) | out-of-scope | Issue #828 investigation: algod-rust has no version-numbered migration *chain* — instead every migration (`migrate_resources_ctype_not_null`, `migrate_off_algod_rust_meta`, `catchpoint::state_keys::migrate_legacy_keys`) is idempotent and self-describing: it inspects the actual on-disk shape (via `pragma_table_info`/row probes) on every open and no-ops if already converged, rather than trusting a stored version number. Each migration already has its own dedicated before/after-shape test (cited). This is a real architectural difference from go's version-tracked `Initialize`/`Migration` chain, but not a demonstrated behavioral gap: algod-rust ships as a single binary pinned to one go-algorand release lineage, so the "unrecognized future schema version" scenario the chain framework guards against has no reachable trigger in this codebase's deployment model. |
| [TestReadOnlyError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/initialize_test.go#L139) | | out-of-scope | Same architectural-alternative reasoning as `TestInitialize` above — no version-chain abstraction to guard read-only opens against. |
| [TestUnknownVersionError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/initialize_test.go#L149) | | out-of-scope | Same as above. |
| [TestNewDBFlag](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/initialize_test.go#L170) | | out-of-scope | Same as above. |
| [TestResumeUpgrading](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/initialize_test.go#L216) | | out-of-scope | Same as above — algod-rust's idempotent shape-detecting migrations are naturally resumable (re-running `open_with_prefix` after a crash mid-migration simply re-evaluates and re-applies any not-yet-converged shape), so there is no separate "resume" state machine to test. |
| [TestVersioning](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/db/versioning_test.go#L93) | | out-of-scope | Same as above. |
| [TestStream_ToBatchBasic](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/execpool/stream_test.go#L126) | | out-of-scope | `execpool.Stream`/`BatchProcessor` is a goroutine-pool batching abstraction built on Go's runtime scheduler; algod-rust uses Tokio tasks/channels directly at each call site instead of porting this generic utility. |
| [TestStream_NoInputYet](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/execpool/stream_test.go#L201) | | out-of-scope | Same as above. |
| [TestStream_MultipleBatchAttempts](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/execpool/stream_test.go#L232) | | out-of-scope | Same as above. |
| [TestStream_Errors](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/execpool/stream_test.go#L302) | | out-of-scope | Same as above. |
| [TestStream_PendingJobOnRestart](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/execpool/stream_test.go#L376) | | out-of-scope | Same as above. |
| [TestIsEmpty](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/io_test.go#L34) | | not-implemented | No `IsEmpty(dir)` directory-emptiness helper (or test of one) found. |
| [TestMoveFile](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/io_test.go#L76) | | not-implemented | No `MoveFile` helper (rename-with-cross-device fallback) ported. |
| [TestMoveFileAcrossFilesystems](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/io_test.go#L97) | | not-implemented | Same as above. |
| [TestMoveFileSourceDoesNotExist](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/io_test.go#L118) | | not-implemented | Same as above. |
| [TestMoveFileSourceIsASymlink](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/io_test.go#L134) | | not-implemented | Same as above. |
| [TestMoveFileSourceAndDestinationAreSame](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/io_test.go#L162) | | not-implemented | Same as above. |
| [TestMoveFileDestinationIsADirectory](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/io_test.go#L184) | | not-implemented | Same as above. |
| [TestList_RemoveFromList](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/list_test.go#L100) | | out-of-scope | `util.List[T]` is a generic intrusive doubly-linked list with a manual free-list allocator, a Go idiom for avoiding GC pressure; Rust code uses standard collections (`Vec`/`VecDeque`/`LinkedList`) directly at each use site instead of porting this generic container. |
| [TestList_AddingNewNodeWithAllocatedFreeListPtr](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/list_test.go#L116) | | out-of-scope | Same as above. |
| [TestList_AddingNewNodeWithAllocatedFreeListValue](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/list_test.go#L136) | | out-of-scope | Same as above. |
| [TestList_MultiElementListPositioning](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/list_test.go#L156) | | out-of-scope | Same as above. |
| [TestList_SingleElementListPositioning](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/list_test.go#L216) | | out-of-scope | Same as above. |
| [TestList_RemovedNodeShouldBeMovedToFreeList](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/list_test.go#L230) | | out-of-scope | Same as above. |
| [TestList_PushMoveBackRemove](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/list_test.go#L252) | | out-of-scope | Same as above. |
| [TestMetricCounter](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/counter_test.go#L35) | [votes_cast_increments_total_and_per_step](../../crates/core/algo-agreement/tests/metrics_test.rs#L117) | partial | go-algorand's `metrics.Counter` is a generic, reusable, registry-backed primitive; algod-rust has no equivalent generic type — each subsystem (agreement, REST process metrics, bench) hand-rolls its own counters. The cited test covers the same increment/read behavior for one concrete counter. |
| [TestMetricCounterFastInts](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/counter_test.go#L86) | [metrics_are_safe_across_threads](../../crates/core/algo-agreement/tests/metrics_test.rs#L429) | partial | Go tests a lock-free fast-path integer counter specifically; Rust tests thread-safety of its own (differently-implemented) metrics struct — same concern (concurrent counting correctness), different mechanism. |
| [TestMetricCounterMixed](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/counter_test.go#L138) | | not-implemented | No generic labeled/unlabeled mixed-counter primitive exists to test. |
| [TestCounterWriteMetric](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/counter_test.go#L192) | [prometheus_text_reports_real_counts](../../crates/core/algo-agreement/tests/metrics_test.rs#L394) | partial | Same "counter renders to Prometheus text" property, tested against a concrete metrics struct rather than a generic `Counter.WriteMetric`. |
| [TestGetValue](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/counter_test.go#L218) | [snapshot_json_roundtrips](../../crates/core/algo-agreement/tests/metrics_test.rs#L413) | partial | No generic `Counter.GetValue()` accessor; Rust exposes counter values via a JSON snapshot instead. |
| [TestGetValueForLabels](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/counter_test.go#L231) | | not-implemented | No generic per-label value accessor exists. |
| [TestCounterLabels](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/counter_test.go#L251) | [vote_step_labels_use_agreement_step_names](../../crates/core/algo-agreement/tests/metrics_test.rs#L133) | partial | Same "label rendering" concern for one concrete, fixed set of labels rather than a generic label-combination API. |
| [TestMetricGauge](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/gauge_test.go#L35) | [runtime_snapshot_renders_all_series_with_correct_types](../../crates/node/algo-rest-api/src/process_metrics.rs#L256) | partial | No generic `Gauge` type; Rust's runtime-metrics snapshot renders gauge-shaped values ad hoc. |
| [TestGaugeLabels](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/gauge_test.go#L96) | [netdev_snapshot_renders_per_interface_series_with_labels](../../crates/node/algo-rest-api/src/process_metrics.rs#L282) | partial | Same per-label gauge rendering, tested against the concrete netdev snapshot rather than a generic labeled-gauge primitive. |
| [TestSanitizeTelemetryName](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/metrics_test.go#L100) | | out-of-scope | Sanitizes metric names for Algorand's telemetry reporting pipeline, which algod-rust does not implement — `telemetry_only_fields_are_accepted_but_ignored_not_modeled` (algo-config/src/lib.rs:4074) confirms telemetry config fields are accepted but deliberately not modeled/wired up. |
| [TestSanitizePrometheusName](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/metrics_test.go#L121) | | not-implemented | No generic Prometheus metric-*name* sanitizer; `escape_label` (`algo-rest-api/src/process_metrics.rs:236`) escapes label *values*, a related but different function. |
| [TestOTelPrometheusExporter](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/otel_test.go#L36) | | out-of-scope | algod-rust hand-renders Prometheus text directly (`to_prometheus_text`); it does not implement an OpenTelemetry exporter/bridge at all. |
| [TestPrometheusMetrics](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/prometheus_test.go#L33) | [metrics_endpoint_returns_prometheus_text_without_auth](../../crates/node/algo-rest-api/tests/integration.rs#L9111) | partial | Go tests the generic `DefaultRegistry` Prometheus-text rendering; Rust tests the concrete `/metrics` endpoint's rendered output end-to-end. |
| [TestPrometheusHistogramMetrics](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/prometheus_test.go#L151) | [timing_stats_aggregate_across_rounds](../../crates/core/algo-agreement/tests/metrics_test.rs#L300), [min_and_max_track_varying_round_durations](../../crates/core/algo-agreement/tests/metrics_test.rs#L316) | partial | No generic `Histogram` metric type; Rust's round-timing stats aggregate min/max/mean directly in `ParticipationMetrics` rather than through a reusable histogram primitive. |
| [TestWriteAdd](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/registry_test.go#L28) | | not-implemented | No generic metrics `Registry` (dynamic add/lookup of named metrics) is ported. |
| [TestParseNodeExporterArgs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/reporter_test.go#L27) | | out-of-scope | Parses CLI args for shelling out to Prometheus's `node_exporter` binary as a telemetry-reporting sidecar — algod-rust has no such external-process integration. |
| [TestRuntimeMetrics](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/runtime_test.go#L29) | [runtime_snapshot_capture_reports_nonzero_memory_for_this_process](../../crates/node/algo-rest-api/src/process_metrics.rs#L330), [runtime_snapshot_renders_all_series_with_correct_types](../../crates/node/algo-rest-api/src/process_metrics.rs#L256) | partial | Go samples Go-runtime-specific series (GC cycles, `/memory/classes/*` heap breakdown, goroutine count) that have no Rust equivalent (no GC); Rust's runtime snapshot covers process memory/CPU series instead, per the documented mapping decision at the top of `process_metrics.rs`. |
| [TestTagCounter](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/tagcounter_test.go#L30) | | not-implemented | No generic `TagCounter` (top-N tag frequency counter) primitive is ported. |
| [TestTagCounterFilter](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/tagcounter_test.go#L85) | | not-implemented | Same generic-primitive gap as above. |
| [TestTagCounterWriteMetric](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/metrics/tagcounter_test.go#L163) | | not-implemented | Same generic-primitive gap as above. |
| [TestPagedQueueNew](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L28) | | out-of-scope | Issue #828 investigation: confirmed algod-rust has no eval-prefetcher pipeline at all — resources are loaded synchronously inside the eval path (see docs/phase17/parity_ledger_core.md's `TestEvaluatorPrefetcher`/`TestEvaluatorPrefetcherAlignment*` rows, "no prefetcher stage at all"). `PagedQueue` exists upstream solely to give the prefetcher's job queue pointer-stable entries across page growth; with no prefetcher consumer, there is nothing for a ported `PagedQueue` to serve. |
| [TestPagedQueueAppendAndLen](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L44) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueGet](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L56) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueGetAdvancingPage](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L75) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueGetZeroValueBeyondEnd](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L97) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueGetPanicOnLowIndex](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L113) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueuePtr](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L132) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueuePtrBeyondEnd](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L163) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueuePtrPanicOnLowIndex](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L178) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueAllPtrs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L192) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueAllPtrs2](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L218) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueAll](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L239) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueAllEarlyReturn](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L261) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueAll2](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L286) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueAll2EarlyReturn](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L303) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueuePageGrowth](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L325) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestPagedQueueEmptyAll](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/pagedqueue_test.go#L350) | | out-of-scope | Same as TestPagedQueueNew above — no prefetcher pipeline exists to consume a paged queue. |
| [TestNewElasticRateLimiter](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L42) | | not-implemented | Issue #828 investigation confirmed this is a real, unaddressed behavioral gap (not architecturally moot): algod-rust's incoming-tx backlog admission (`bin/algod-rust/src/node_interface_impl.rs`, `async_backlog_permits` / `reserve_async_backlog_permit`) is a single flat `tokio::sync::Semaphore` with a hard accept/reject cliff — no per-peer capacity fairness, no elastic capacity, no RED early-drop congestion avoidance. Too large to fix inline; split out to [issue #860](https://github.com/xarmian/algod-rust/issues/860) to port `ElasticRateLimiter`/`REDCongestionManager` (go-algorand `v3.14.1-beta`, commit `50d5dfde5`). |
| [TestElasticRateLimiterCongestionControlled](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L50) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestReservations](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L83) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestZeroSizeReservations](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L111) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestConsumeReleaseCapacity](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L133) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestREDCongestionManagerShouldDrop](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L181) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestREDCongestionManagerShouldntDrop](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L217) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestREDCongestionManagerTargetRate](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L249) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestREDCongestionManagerPrune](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L267) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestREDCongestionManagerStopStart](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/rateLimit_test.go#L287) | | not-implemented | Same gap — tracked in [issue #860](https://github.com/xarmian/algod-rust/issues/860). |
| [TestGetS3UploadBucket](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L30) | | out-of-scope | AWS S3 helper for uploading/downloading catchpoint and release artifacts to Algorand's hosting infrastructure; unrelated to node consensus/protocol logic and not part of algod-rust's scope. |
| [TestGetS3ReleaseBucket](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L57) | | out-of-scope | Same as above. |
| [Test_getS3Region](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L84) | | out-of-scope | Same as above. |
| [TestMakeS3SessionForUploadWithBucket](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L111) | | out-of-scope | Same as above. |
| [TestMakeS3SessionForDownloadWithBucket](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L144) | | out-of-scope | Same as above. |
| [TestGetVersionFromName](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L177) | | out-of-scope | Same as above. |
| [TestGetVersionFromNameCompare](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L202) | | out-of-scope | Same as above. |
| [TestGetPartsFromVersion](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L217) | | out-of-scope | Same as above. |
| [TestGetPartsFromVersionEndToEnd](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/s3/s3Helper_test.go#L250) | | out-of-scope | Same as above. |
| [TestMakeSet](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/set_test.go#L27) | | out-of-scope | `util.MakeSet[T]` is a thin generic wrapper over `map[T]struct{}`; Rust code uses `std::collections::HashSet` directly rather than porting a bespoke generic set type. |
| [TestSetAdd](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/set_test.go#L43) | | out-of-scope | Same as above. |
| [TestSetOps](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/set_test.go#L56) | | out-of-scope | Same as above. |
| [TestMonotonicDelta](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/timers/monotonic_test.go#L36) | [timeout_at_waits_before_firing](../../crates/core/algo-agreement/src/system_clock.rs#L183) | matched-1:1 | `SystemClock` (`system_clock.rs:23`) is the documented Rust port of `timers.Monotonic[TimeoutType]`. |
| [TestMonotonicZeroDelta](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/timers/monotonic_test.go#L62) | [timeout_at_elapsed_delta_fires_immediately](../../crates/core/algo-agreement/src/system_clock.rs#L170) | matched-1:1 | A zero (or already-elapsed) delta fires immediately in both implementations. |
| [TestMonotonicNegativeDelta](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/timers/monotonic_test.go#L76) | | out-of-scope | `std::time::Duration` is unsigned in Rust, so a "negative delta" cannot be constructed — the scenario this test guards against cannot occur. |
| [TestMonotonicZeroTwice](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/timers/monotonic_test.go#L90) | [zero_resets_the_monotonic_reference](../../crates/core/algo-agreement/src/system_clock.rs#L149), [timeout_at_after_zero_measures_from_new_zero](../../crates/core/algo-agreement/src/system_clock.rs#L204) | matched-1:many | Together these cover repeated `zero()` calls correctly rebasing subsequent timeouts. |
| [TestMonotonicEncodeDecode](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/timers/monotonic_test.go#L122) | | not-implemented | `SystemClock`'s wall-clock stamp (`zero_wall`) is kept only "for logging / future persistence" and is marked `#[allow(dead_code)]` — no `Encode`/`Decode` round-trip for persisting clock state across restarts exists. |
| [TestTimeoutTypes](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/timers/monotonic_test.go#L157) | [timeout_at_waits_before_firing](../../crates/core/algo-agreement/src/system_clock.rs#L183) | partial | `timeout_at` accepts a `TimeoutType` parameter (mirroring Go's API) but the parameter is unused (`_timeout_type`) in `SystemClock`'s implementation; no test explicitly registers two independent, concurrently-pending timeouts of different types and asserts they fire independently, though each call trivially returns its own channel. |
| [TestGetTotalMemory](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/util_test.go#L27) | | not-implemented | `GetTotalMemory` reports total host physical memory (used only by `catchpointdump`'s bench-report tool); the closest Rust analogue, [test_current_rss_bytes](../../crates/tools/algo-bench/src/metrics.rs#L165), reports current-process RSS — a different metric — so this is not counted as a match. |
| [TestUUID](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/util/uuid/uuid_test.go#L27) | | out-of-scope | `util/uuid` generates the node's telemetry session ID; algod-rust does not implement telemetry reporting at all (see `telemetry_only_fields_are_accepted_but_ignored_not_modeled`, algo-config/src/lib.rs:4074), so there is no UUID-generation call site to test. |

## Gaps summary

- **Generic `util/metrics` primitives (Counter, Gauge, TagCounter, Registry, Prometheus/OTel exporters, node_exporter arg parsing) are not ported as reusable types.** algod-rust hand-rolls Prometheus-text rendering per subsystem (`algo-agreement`'s `ParticipationMetrics`, `algo-rest-api`'s runtime/netdev process snapshots) instead of a shared `metrics.Counter`/`Gauge`/`Registry` library. Several go tests (`TestMetricCounter`, `TestCounterWriteMetric`, `TestGetValue`, `TestCounterLabels`, `TestMetricGauge`, `TestGaugeLabels`, `TestPrometheusMetrics`, `TestPrometheusHistogramMetrics`, `TestRuntimeMetrics`) have narrower partial analogues; `TestMetricCounterMixed`, `TestGetValueForLabels`, `TestSanitizePrometheusName`, `TestWriteAdd`, `TestTagCounter*` (3 tests) have none. `TestOTelPrometheusExporter`, `TestParseNodeExporterArgs`, and `TestSanitizeTelemetryName` are out-of-scope since algod-rust implements neither OpenTelemetry export nor telemetry reporting nor a `node_exporter` sidecar integration.
- **`util/db`'s generic `Accessor`/`Migration` framework has no reusable Rust equivalent** — algod-rust talks to SQLite directly via `rusqlite` inside `algo-ledger`/`algo-kmd`, with no independently-tested wrapper layer for disposal semantics, unique in-memory naming, concurrency/locking behavior (WAL vs. journal), transaction-deadline warnings, or the versioned-migration chain (`Initialize`/`Migration`/`Versioning`). Only `SetSynchronousMode` has a partial analogue (`algo-ledger/src/sqlite.rs`).
- **`util/pagedqueue` (14 tests) is entirely unimplemented** — no eval-prefetcher pipeline or pointer-stable paged-queue data structure was found anywhere in `algo-ledger`, suggesting either the prefetch-pipeline optimization itself wasn't ported, or it uses a different (untested-here) mechanism.
- **`util/rateLimit`'s `ElasticRateLimiter`/`REDCongestionManager` (10 tests) is unimplemented** — algod-rust's incoming-transaction backlog uses a plain semaphore/bounded-channel cap (`with_async_backlog_capacity`) rather than the RED active-queue-management algorithm go-algorand uses in `txHandler.go`.
- **Whole packages are out-of-scope by design**, each backed by a Rust/architecture idiom that removes the need for a port: `util/condvar` (native `Condvar::wait_timeout`), `util/execpool` (Tokio tasks/channels replace the goroutine-pool `Stream` abstraction), the top-level `List[T]` and `Set[T]` generics in `util/list_test.go`/`util/set_test.go` (standard `VecDeque`/`HashSet`), `util/s3` (AWS hosting infrastructure, not node logic), and `util/uuid` (telemetry, which isn't implemented at all).
- **`util` top-level file/OS helpers are unimplemented**: `IsEmpty`, `MoveFile` (and its 5 edge-case variants), and `GetTotalMemory` (host physical memory) have no ported equivalents; the closest Rust code (`current_rss_bytes` in `algo-bench`) measures a different quantity (process RSS, not host total memory).
- **Bloom filter core algorithm is well-matched** (`crates/node/algo-network/src/bloom.rs` golden-fixture tests captured directly from go-algorand output), but several go tests validate broader statistical/fuzz sweeps (`TestOptimal`'s FP-rate measurement across large N, `TestBinaryMarshalLength`'s 36-case matrix, `TestEmptyFilter`'s exhaustive truncation fuzz) that Rust covers only with narrower, fixed-case checks — and `TestMarshalJSON`/`TestIncompressible`/`TestBloomFilterMemoryConsumption` have no Rust equivalent at all (no JSON encoding path; no compression/memory-profiling tests).
