# Phase 17 Test Parity — config + stateproof + protocol

Scope: go-algorand test packages `config`, `protocol`, `protocol/test`, `protocol/transcode`, `stateproof`, `stateproof/verify` (v5.0.0-stable pin) against algod-rust's test suite. 119 go-algorand tests total.

| go-algorand test | algod-rust test(s) | status | notes |
|---|---|---|---|
| [TestLocal_SaveThenLoad](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L47) | [real_config_json_round_trips_through_data_dir](../../crates/node/algo-config/src/lib.rs#L3882) | matched-1:1 | |
| [TestConfig_LoadMissing](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L72) | [missing_config_json_in_data_dir_returns_defaults](../../crates/node/algo-config/src/lib.rs#L3856) | out-of-scope | Reclassified (batch 2, issue #1496): batch 1 already established this is not a behavioral divergence -- confirmed intentional, not a bug. `LoadConfigFromDisk`'s own raw contract does return an `os.IsNotExist` error on a missing file, as this unit test checks -- but the real daemon startup path (`cmd/algod/main.go:179-183`: `cfg, _, err := config.LoadConfigFromDiskWithMigrations(...); if err != nil && !os.IsNotExist(err) { log.Fatalf(...) }`) explicitly swallows exactly that error and proceeds with the defaults-merged `cfg` it already got back. So go's actual end-to-end behavior (missing `config.json` -> run with defaults) already matches algod-rust's `load_from_data_dir`, which just folds that fallback into the loader itself instead of splitting it across a raw loader + a caller-side `IsNotExist` check -- full behavioral equivalence, already demonstrated and tested (`missing_config_json_in_data_dir_returns_defaults`). The only remaining gap is architectural: this go test specifically targets the *raw* loader function's own `IsNotExist`-return contract in isolation, a function shape algod-rust deliberately doesn't have (it merged the raw-load + fallback-on-`IsNotExist` steps go splits across `LoadConfigFromDisk` + `main.go` into one `load_from_data_dir` call). There is no missing behavior to pin a test against -- only a go-side internal function boundary with no algod-rust counterpart to test in isolation, so this moves out of `partial` (no closeable behavioral gap remains) to `out-of-scope`. |
| [TestLocal_MergeConfig](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L82) | [json_partial_overlay_only_overrides_present_fields](../../crates/node/algo-config/src/lib.rs#L3745), [string_field_partial_overlay_only_overrides_present_field](../../crates/node/algo-config/src/lib.rs#L3758) | matched-1:many | |
| [TestLocal_EnrichNetworkingConfig](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L125) | [gossip_fanout_for_listen_server_bumps_default_to_relay_fanout](../../crates/node/algo-config/src/lib.rs#L4248), [gossip_fanout_for_listen_server_preserves_explicit_override](../../crates/node/algo-config/src/lib.rs#L4258) | out-of-scope | Re-verified (batch 1, issue #1496): go's `enrichNetworkingConfig` has three side effects when `NetAddress` is set -- auto-enable `EnableLedgerService`/`EnableBlockService`, lower-case `PublicAddress`, and bump `GossipFanout`. Confirmed by reading `Local::gossip_fanout_for_listen_server`'s own doc comment and the module doc's "Explicitly out of scope" note (issue #788): `enable_ledger_service`/`enable_block_service` are already documented, deliberate no-ops in algod-rust (no runtime behavior to gate -- see their own doc comments), so auto-flipping them would only change which no-op flag reads `true`, not any real behavior; `public_address` remains round-trip-only (no behavioral consumer anywhere in algod-rust yet), so lowercasing it normalizes a value nothing reads. Only the gossip-fanout bump -- the one side effect with a real behavioral consequence -- has an algod-rust equivalent, and it is ported and tested. Not a closeable test gap: there is no missing behavior to pin a test against, only two flags that stay no-ops by design. |
| [TestLoadPhonebook](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L186) | [round_trips_a_real_include_list](../../crates/node/algo-network/src/phonebook_file.rs#L123), [loads_a_hand_written_go_style_file](../../crates/node/algo-network/src/phonebook_file.rs#L135) | matched-1:many | Issue #949: `algo_network::phonebook_file::load_phonebook`/`save_phonebook` port go's `config.LoadPhonebook`/`SavePhonebookToDisk` (`{"Include": [...]}` JSON shape), wired into `Phonebook` construction in `commands::participate::run`/`commands::relay::run` via `add_persistent_peers`. |
| [TestLoadPhonebookMissing](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L203) | [missing_file_returns_empty_list_not_error](../../crates/node/algo-network/src/phonebook_file.rs#L116) | matched-1:1 | Issue #949. |
| [TestLocal_ConfigExampleIsCorrect](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L212) | [config_json_example_matches_compiled_defaults](../../crates/node/algo-config/src/lib.rs#L5395) | matched-1:1 | Issue #1086: `crates/node/algo-config/config.json.example` ships alongside a regression test that parses it and compares it (as a JSON `Value`, not via `Local`'s per-field-defaulting `Deserialize`) against `Local::default()`'s full JSON serialization — any drift in a field's default, a renamed/added/removed field, or a stale example value fails the test. |
| [TestLocal_ConfigMigrate](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L255) | [field_still_at_old_default_advances_to_new_default_across_multiple_boundaries](../../crates/node/algo-config/src/lib.rs#L3769), [field_explicitly_overridden_away_from_default_is_never_clobbered](../../crates/node/algo-config/src/lib.rs#L3782), [version_newer_than_latest_known_is_rejected_not_panicked](../../crates/node/algo-config/src/lib.rs#L3817) | matched-1:many | Rust's per-field version-tagged migration mechanism covers the same "advance stale defaults, preserve explicit overrides, reject too-new versions" behavior go's `migrate()` implements, though not via the identical named-field spot-checks go's test uses. |
| [TestLocal_ConfigMigrateFromDisk](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L304) | [default_at_version_replays_only_tags_up_to_that_version](../../crates/node/algo-config/src/lib.rs#L3699), [catchup_parallel_blocks_explicit_override_survives_migration](../../crates/node/algo-config/src/lib.rs#L3465), [max_catchpoint_download_duration_migrates_from_2h_to_12h_default](../../crates/node/algo-config/src/lib.rs#L3657), [dns_security_flags_migrates_across_version_34](../../crates/node/algo-config/src/lib.rs#L3289), [base_logger_debug_level_migrates_from_1_to_4_default](../../crates/node/algo-config/src/lib.rs#L3953), [cadaver_size_target_migrates_from_1gib_to_disabled_default](../../crates/node/algo-config/src/lib.rs#L3989) | matched-1:many | go replays real `config-v<N>.json` fixture files from disk for every historical version; algod-rust instead spot-checks the same individual field migrations (`DNSSecurityFlags`, `CadaverSizeTarget`, `MaxCatchpointDownloadDuration`, etc.) directly against its version-tag table rather than fixture files, so coverage is field-equivalent but not fixture-file-equivalent. |
| [TestLocal_ConfigInvariant](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L362) | [config_v27_fixture_matches_versioned_default](../../crates/node/algo-config/src/lib.rs#L5986) | out-of-scope | Reclassified (issue #1498/#1496 final push): re-verified `NOT_YET_PORTED` in `crates/node/algo-config/src/lib.rs` (`lib.rs#L6053`) is down to a single entry, `NetAddress`, a permanent design exclusion (issue #788, kept as a per-subcommand CLI flag). Every field-content gap this test could close is already closed. What remains is purely the test's own methodology (field-by-field comparison against `Local`'s known fields vs go's full-struct comparison, which would additionally require back-filling ~28 later-introduced fields' historical struct-tag introduction-version data purely to prove a methodology equivalence, not a behavioral one) -- there is no remaining behavioral gap to pin a test against, only a permanent test-shape difference. Moves from `partial` (implying future closeable work) to `out-of-scope` per this project's convention. go's own `config-v27.json` fixture (copied verbatim to `crates/node/algo-config/fixtures/config-v27.json`) is checked field-by-field against `Local::default_at_version(27)`. Issue #1149 triaged the 35 fields (not 34 as originally estimated) that had no `Local` field at all: 4 ported as real, behavior-wired fields (`ProposalAssemblyTime`, `TxPoolSize`, `TxPoolExponentialIncreaseFactor`, `VerifiedTranscationsCacheSize` -- see `issue_1149_retired_fields_round_trip_through_json_as_documented_no_ops` and the `tx_pool_size_migrates_through_gos_historical_defaults`/`proposal_assembly_time_migrates_from_250ms_to_500ms_default`/`verified_transcations_cache_size_migrates_from_30000_to_150000_default` tests), 15 formally retired as documented no-op fields following issue #1137's precedent (`CatchupBlockValidateMode`, `DeadlockDetection`, `DeadlockDetectionThreshold`, `EnableBlockServiceFallbackToArchiver`, `EnableCatchupFromArchiveServers`, `IsIndexerActive`, `NetworkMessageTraceServer`, `NodeExporterListenAddress`, `NodeExporterPath`, `PeerPingPeriodSeconds`, `ReconnectTime`, `RunHosted`, `StorageEngine`, `SuggestedFeeBlockHistory`, `SuggestedFeeSlidingWindowSize` -- three of these, `EnableBlockServiceFallbackToArchiver`/`EnableCatchupFromArchiveServers`/`IsIndexerActive`, are confirmed dead in go-algorand's own `config.Local` at the current pin, not just in algod-rust), and 15 deferred to 5 category-3 follow-up issues needing real subsystem work (#1187 account-updates telemetry stats, #1188 `EnableMetricReporting` reconciliation, #1189 network/operational timer knobs, #1190 tx-backlog queue-depth knobs, #1191 transaction-sync tuning knobs), plus `NetAddress` which issue #788 already permanently excluded by design (kept as a per-subcommand CLI flag). `NOT_YET_PORTED` in `crates/node/algo-config/src/lib.rs` shrank from 35 to 16 entries (the 15 deferred fields plus `NetAddress`). Issue #1189 then closed its 5-field slice: `DisableNetworking` ported as a real, behavior-wired field (skips WS-gossip listening/dialing/DNS-discovery and the libp2p P2P transport in `relay`/`participate` when set -- see `networking_active` in `bin/algod-rust/src/commands/network_common.rs` and its tests, plus `telemetry_only_no_op_fields_still_round_trip`); `EnablePingHandler` and `ParticipationKeysRefreshInterval` formally retired as documented no-ops because go-algorand's own `v5.0.0-stable` production code never consumes either field (confirmed by direct source grep -- not merely an algod-rust gap); `HeartbeatUpdateInterval`/`PeerConnectionsUpdateInterval` formally retired as documented no-ops because their only upstream consumer is the remote-telemetry-reporting pipeline issue #756 already declined to build. `NOT_YET_PORTED` shrank further from 16 to 11 entries. Issue #1188 then closed one more: `EnableMetricReporting` formally retired as a documented no-op — investigation of `daemon/algod/server.go:346-352` and `util/metrics/service.go`/`reporter.go` found it gates neither `/metrics` route mounting nor counter population (those are unconditional/`EnableRuntimeMetrics`/`EnableNetDevMetrics` respectively) but a third thing: go-algorand's `MetricService.ReporterLoop`, which POSTs to `NodeExporterListenAddress` and shells out to spawn `NodeExporterPath` — both already formally retired in #1149, so the loop this flag would gate has no counterpart subsystem in algod-rust either (see `ENABLE_METRIC_REPORTING`'s doc comment in `crates/node/algo-config/src/lib.rs` and `issue_1188_enable_metric_reporting_round_trips_through_json_as_documented_no_op`). `NOT_YET_PORTED` shrank further from 11 to 10 entries. Issue #1190 then closed its slice: `TxBacklogSize`/`TxBacklogReservedCapacityPerPeer`/`EnableTxBacklogRateLimiting` ported as real, behavior-wired fields — `TxBacklogSize` (± `IncomingConnectionsLimit * TxBacklogReservedCapacityPerPeer` when `EnableTxBacklogRateLimiting` is set) now sizes the real bounded backlog-admission queue at both live `TxTagHandler::with_backlog_queue` call sites (WS-gossip and, if enabled, P2P) in `bin/algod-rust/src/commands/participate.rs`'s `tx_backlog_queue_capacity_from_local`, mirroring go's `MakeTxHandler` queue-capacity formula (`data/txHandler.go:154-158`) exactly; `TxBacklogSize` is also now the single source of truth for `RestConfig::async_backlog_size` (`POST /v2/transactions/async`'s admission window), reconciled via `resolve_async_backlog_size` with the REST-specific override layered on top. This ports go's queue-*sizing* formula only, not go's `util.NewElasticRateLimiter` per-peer fairness/reservation guarantee (`data/txHandler.go:189-199`), which has no algod-rust equivalent yet and remains deferred (tracked as a follow-up issue, not absorbed here) — see `tx_backlog_queue_capacity_from_local`'s doc comment; that fairness guarantee is filed as its own follow-up, #1195. `NOT_YET_PORTED` shrank further from 10 to 7 entries. Issue #1191 then closed the remaining 4-field slice: `ForceFetchTransactions` ported as a real, behavior-wired field — `algo_network::WebsocketNetworkConfig::force_fetch_transactions` already existed with full behavioral coverage (`force_fetch_transactions_always_receives_tx`/`want_tx_gossip_seeded_yes_for_force_fetch_transactions` in `ws_network.rs`, mirroring go's `wn.relayMessages || wn.config.ForceFetchTransactions` gate in `network/wsNetwork.go`/`p2pNetwork.go`); only the `config.json` -> `Local` plumbing was missing, now wired into `bin/algod-rust/src/commands/{participate,relay}.rs` the same way as `EnableRequestLogger`/`UseXForwardedForAddressField` (issues #1088/#1157) — see `issue_1191_force_fetch_transactions_round_trips_through_json`. `EnableVerbosedTransactionSyncLogging`/`TransactionSyncDataExchangeRate`/`TransactionSyncSignificantMessageThreshold` formally retired as documented no-ops: investigated and confirmed dead in go-algorand v5.0.0-stable itself — no `data/txnsync` package exists at this pin (that adaptive, bandwidth-tracking tx-sync protocol experiment was retired upstream before this version), so these fields have zero consumers anywhere in non-test Go source, only their `config/localTemplate.go`/`local_defaults.go` declarations — see `issue_1191_dead_transaction_sync_fields_round_trip_through_json_as_documented_no_ops`. `NOT_YET_PORTED` shrank further from 7 to 3 entries (`AccountUpdatesStatsInterval`/`EnableAccountUpdatesStats`/`NetAddress`). Issue #1187 then closed the last real field-content gap: `EnableAccountUpdatesStats`/`AccountUpdatesStatsInterval` ported as real, behavior-wired fields, gating a new periodic "AccountUpdates telemetry event" (`algo_ledger::acctupdates_stats`, a `tracing::info!` fired from `SqliteLedger::commit_block_uncleaned` on the configured interval) — algod-rust's synchronous, always-exactly-one-round-per-commit architecture has no equivalent of go's batched, multi-round deferred commit, so the event carries a reduced but real field set (accounts-writing/merkle-trie-update/database-commit durations, touched-accounts count) rather than go's full `AccountsUpdateMetrics` shape (no `OldAccountPreloadDuration`/`UpdatedResourcesCount`/`UpdatedCreatablesCount` equivalents exist to time or count yet) — see that module's doc comment for the full mapping and `SqliteLedger::configure_account_updates_stats`'s tests (`acctupdates_stats_disabled_emits_no_event`, `acctupdates_stats_enabled_emits_one_event_per_commit_when_interval_is_zero`, `acctupdates_stats_enabled_skips_events_within_the_configured_interval`). `NOT_YET_PORTED` shrank further from 3 to 1 entry — only the permanent `NetAddress` design exclusion remains. Still `partial`: this test's own methodology (field-by-field comparison against only the fixture keys `Local` has a field for, not go's full-struct comparison) remains a documented divergence from go's test regardless of field coverage. Re-investigated (batch 2, issue #1496): attempted to close the methodology gap directly by additionally asserting the fixture's key set and `Local`'s serialized key set are identical (a would-be full-struct-equivalent check) -- this fails as-is, because `Local` (a single struct spanning every config version, same as go's real `config.Local`) also carries every field introduced in versions after v27 (e.g. `EnableP2P`, `BlockDBDir`, `CatchpointDir`, `TrackerDBDir`, `P2PHybridNetAddress`, ~28 fields total), each serialized at its `default_at_version(27)` value. Reproducing go's exact full-struct comparison would require knowing, for every one of those ~28 later-introduced fields, the precise go struct-tag version at which each was introduced (so its v27-era value is provably the same zero/pre-introduction default go's real fixture-loaded struct would carry) -- that per-field introduction-version data isn't tracked anywhere in algod-rust today, and back-filling it accurately for ~28 fields purely to prove a methodology equivalence (not a behavioral gap -- every currently-ported field already matches value-for-value) is disproportionate cost for no behavioral payoff. Stays `partial` with this more precise note. |
| [TestLocal_ConfigLatestVersion](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L380) | [latest_version_matches_max_field_tag](../../crates/node/algo-config/src/lib.rs#L4808) | matched-1:1 | |
| [TestConsensusUpgrades](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L390) | [historical_approved_upgrades_chain_reaches_v42_from_every_version_v7_through_v41](../../crates/core/algo-types/src/consensus.rs#L2638) | matched-1:1 | Closed in the Phase-17 final push via the safer separated-data path a prior investigation identified: rather than backfilling the ~34 historical `(from, to, delay)` edges into `ConsensusParams::approved_upgrade` (which `algo_ledger::block_header::process_upgrade_params` reads as **live block-production logic** — auto-proposing/auto-voting-yes — so backfilling it would change real upgrade-proposal behavior for every historical pre-v42 consensus version), added a wholly separate, production-inert `pub const HISTORICAL_APPROVED_UPGRADES: &[(&str, &str, u64)]` table (`consensus.rs`, right after `KNOWN_PROTOCOL_VERSIONS`) that is never read by `process_upgrade_params` or any other production code path. Every one of its 34 entries (`v7`->`v8` through `v41`->`v42`, including the multi-source edges `v17`/`v18`->`v19` and `v21`/`v22`->`v23` that a single-`Option` `approved_upgrade` field can't represent) was read directly off go's real `config/consensus.go` `vN.ApprovedUpgrades[vM] = delay` assignment lines at `v5.0.0-stable` — not derived from `approved_upgrade` or guessed. The new test walks the table as a DAG from `v7` and confirms `v42` is reachable (proving the full historical chain go shipped is unbroken end to end), checks every `v7`..`v41` version has at least one recorded outbound edge, and cross-checks the table's `v41`->`v42` entry against the live `approved_upgrade` field to confirm the two never diverge on their one point of overlap. Zero risk to consensus behavior: `HISTORICAL_APPROVED_UPGRADES` and `approved_upgrade` are fully separate concerns. Not `matched-1:many` because go's test also checks upgrade-name provenance (URL/whitelist), which still has no Rust equivalent since `KNOWN_PROTOCOL_VERSIONS` names carry no separate provenance metadata — a narrower, acceptable residual gap the note flags but doesn't block closing this row on. |
| [TestConsensusLatestVersion](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L432) | [test_v42_has_no_further_approved_upgrade](../../crates/core/algo-types/src/consensus.rs#L2306) | matched-1:1 | |
| [TestLocal_DNSBootstrapArray](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L442) | [parse_array_multiple_entries](../../crates/node/algo-network/src/dns_bootstrap.rs#L545), [parse_array_single_entry](../../crates/node/algo-network/src/dns_bootstrap.rs#L537), [parse_array_skips_empty_segments](../../crates/node/algo-network/src/dns_bootstrap.rs#L554) | matched-1:many | |
| [TestLocal_ValidateDNSBootstrapArray_StopsOnError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L510) | [parse_array_stops_on_first_error](../../crates/node/algo-network/src/dns_bootstrap.rs#L561), [parse_array_error_propagates](../../crates/node/algo-network/src/dns_bootstrap.rs#L569) | matched-1:many | |
| [TestLocal_StructTags](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L524) | [empty_json_object_loads_every_field_at_its_versioned_default](../../crates/node/algo-config/src/lib.rs#L6096) | out-of-scope | Reclassified (issue #1498/#1496 final push): re-verified against current go source (`config/config_test.go#L523-563`) -- this is a `reflect.TypeFor[Local]()` structural check that every `Local` struct field carries well-formed, contiguous `version[N]` struct tags (no malformed/non-version tag content, no gaps). algod-rust's version-tag-equivalent logic lives as imperative match/if-chain code per field (`default_at_version`/`load_from_str`'s migration path), not as parseable struct-tag metadata -- there is no data structure of Rust struct tags to reflect over and validate the way go's test does, and none can exist without inventing a struct-tag-like annotation system purely to satisfy this test, which would be a large speculative addition with no other consumer. This is a permanent architecture gap (imperative-code vs reflection-based-declarative-metadata), not a closeable coverage gap -- `empty_json_object_loads_every_field_at_its_versioned_default` is the closest functional analogue (exercises the same per-field versioned-default table) and remains the citation. Moves from `partial` to `out-of-scope`. |
| [TestLocal_GetVersionedDefaultLocalConfig](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L568) | [default_at_version_replays_only_tags_up_to_that_version](../../crates/node/algo-config/src/lib.rs#L3699) | matched-1:1 | |
| [TestLocal_VersionField](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L579) | [latest_version_matches_max_field_tag](../../crates/node/algo-config/src/lib.rs#L4808) | out-of-scope | Reclassified (issue #1498/#1496 final push): re-verified against current go source (`config/config_test.go#L577-596`) -- same reflection-based `Version` field struct-tag contiguity check as the sibling `TestLocal_StructTags` row, same permanent architecture gap (no struct-tag data structure exists in the Rust representation to reflect over; algod-rust's version-dispatch is imperative code, not declarative metadata). `latest_version_matches_max_field_tag` is the closest functional analogue (checks the one concrete invariant substitutable here: latest-version constant == highest field tag actually used) and remains the citation. Moves from `partial` to `out-of-scope`, consistent with the sibling row. |
| [TestLocal_GetNonDefaultConfigValues](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L600) | [save_non_default_only_includes_version_and_overridden_fields](../../crates/node/algo-config/src/lib.rs#L3907), [save_non_default_round_trips_back_to_the_same_config](../../crates/node/algo-config/src/lib.rs#L3930) | matched-1:many | |
| [TestLocal_TxFiltering](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L629) | [tx_filter_enabled_helpers_decode_flags_like_go](../../crates/node/algo-config/src/lib.rs#L5573), [tx_incoming_filter_fields_round_trip](../../crates/node/algo-config/src/lib.rs#L3640) | matched-1:many | Batch 1 (issue #1496): added `Local::tx_filter_raw_msg_enabled`/`Local::tx_filter_canonical_enabled`, porting go's `TxFilterRawMsgEnabled`/`TxFilterCanonicalEnabled` bit-decode of `TxIncomingFilteringFlags` (`0x01`/`0x02`) verbatim, plus a test asserting the exact same 0/1/2/3-flag matrix go's test does. Like `tx_incoming_filtering_flags` itself (a documented no-op -- algod-rust has no per-message-hash dedup filtering stage to gate yet), these decode helpers have no runtime consumer yet, but the decode logic itself is pure and fully pinned. |
| [TestLocal_IsListenServer](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L656) | [is_ws_listen_server_matrix](../../crates/node/algo-config/src/lib.rs#L4858), [is_p2p_listen_server_matrix](../../crates/node/algo-config/src/lib.rs#L4879), [is_listen_server_is_the_or_of_ws_and_p2p](../../crates/node/algo-config/src/lib.rs#L4899), [is_hybrid_server_requires_both_addresses_and_the_mode_flag](../../crates/node/algo-config/src/lib.rs#L4913) | matched-1:many | Issue #949: `algo_config::is_ws_listen_server`/`is_p2p_listen_server`/`is_listen_server`/`is_hybrid_server` (plus `Local` method wrappers) port the full state matrix as free functions taking the WS/P2P listen addresses as explicit parameters, since algod-rust keeps those as CLI-flag concepts rather than `Local` fields (see the module doc note on `NetAddress`). Wired into `commands::participate::run`, replacing the prior ad hoc `effective_listen_address.is_some()` check. |
| [TestLocal_RecalculateConnectionLimits](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L736) | [adjust_connection_limits_matches_go_table](../../crates/node/algo-config/src/lib.rs#L5093) | matched-1:1 | Issue #949: `Local::adjust_connection_limits` ports go's `AdjustConnectionLimits` verbatim (same REST/incoming/P2P-hybrid-limit rebalancing algorithm, same `reservedRESTConns`/split-ratio logic), and the test ports go's own 14-row table exactly. Live FD accounting (go: `util.GetFdLimits`/`RaiseFdSoftLimit`, `getrlimit`(2)/`setrlimit`(2) on Unix, hard-coded "unlimited" on Windows in go itself) is ported in `algo_network::fd_limits` (`get_fd_limits`/`raise_fd_soft_limit`/`rebalance_connection_limits`; see [adjust_connection_limits_triggers_under_simulated_fd_pressure](../../crates/node/algo-network/src/fd_limits.rs#L273) for the simulated-FD-pressure case and [rebalance_lowers_limits_under_a_real_lowered_rlimit](../../crates/node/algo-network/src/fd_limits.rs#L302) for a real-rlimit-driven one, Unix-only), wired into `main.rs`'s `relay`/`participate` startup paths ahead of `config.json`'s connection-limit fields being captured into `RestOptions`/`NetworkOptions`. |
| [TestLocal_ValidateP2PHybridConfig](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L797) | [validate_p2p_hybrid_config_ok_when_both_addresses_and_public_address_set](../../crates/node/algo-config/src/lib.rs#L4938), [validate_p2p_hybrid_config_rejects_net_address_without_p2p_address](../../crates/node/algo-config/src/lib.rs#L4957), [validate_p2p_hybrid_config_rejects_missing_public_address](../../crates/node/algo-config/src/lib.rs#L4981) | matched-1:many | Issue #949: `algo_config::validate_p2p_hybrid_config`/`Local::validate_p2p_hybrid_config` port go's mismatched-address and missing-`PublicAddress` checks; wired into `commands::participate::run` as a startup-time rejection. |
| [TestEnsureAbsDir](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L839) | [create_genesis_ledger_dir_resolves_unclean_relative_data_dir](../../bin/algod-rust/src/commands/node.rs#L1134) | matched-1:1 | Closed (issue #1498): `create_genesis_ledger_dir` now resolves `data_dir` via a new `lexical_absolute` helper (lexical `..`/`.` cleaning atop the cwd, no filesystem access -- an MSRV-safe stand-in for `std::path::absolute`, which needs Rust 1.79 above this workspace's 1.75 pin) before joining `genesis_id`, mirroring go's `ensureAbsGenesisDir`. New test `create_genesis_ledger_dir_resolves_unclean_relative_data_dir` ports this go test's second case verbatim (`filepath.Join(testDirectory, "test2", "..")` -> clean absolute `<testDirectory>/myGenesisID`), alongside the pre-existing `create_genesis_ledger_dir_succeeds_and_is_idempotent` (first case) and `create_genesis_ledger_dir_fails_with_permission_denied_on_readonly_parent` (`TestMismatchingGenesisDirectoryPermissions`). |
| [TestEnsureAndResolveGenesisDirs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L869) | [resolve_resource_paths_defaults_to_ledger_directory / resolve_resource_paths_honors_hot_and_cold_data_dirs / resolve_resource_paths_honors_explicit_per_resource_overrides](../../bin/algod-rust/src/commands/participate.rs#L10227) | matched-1:many | Stale note: `resolve_resource_paths` (issues #953/#1030) is algod-rust's `EnsureAndResolveGenesisDirs` equivalent — resolves tracker/block/crash DB directories against `HotDataDir`/`ColdDataDir`/per-resource overrides, exactly mirroring go's fallback chain (`dir_or` helper). |
| [TestEnsureAndResolveGenesisDirs_hierarchy](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L899) | [resolve_resource_paths_honors_hot_and_cold_data_dirs](../../bin/algod-rust/src/commands/participate.rs#L10248) | matched-1:1 | Stale note: hot/cold data-dir hierarchy resolution is directly ported and tested. |
| [TestEnsureAndResolveGenesisDirs_migrate](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L939) | — | out-of-scope | `resolve_resource_paths`'s own doc comment documents this as a deliberate scope cut: unlike go's `EnsureAndResolveGenesisDirs`, it is a pure path-resolution function that does not move pre-existing DB files between old/new locations on a reconfigure — "an operator who changes these settings on an existing node is responsible for moving any existing files themselves." No automatic cold-to-hot migration exists to test. |
| [TestEnsureAndResolveGenesisDirs_migrateCrashErr](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L979) | — | out-of-scope | Same as `TestEnsureAndResolveGenesisDirs_migrate` above — no migration mechanism exists to have an error path. |
| [TestEnsureAndResolveGenesisDirs_migrateSPErr](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L1011) | — | out-of-scope | Same as `TestEnsureAndResolveGenesisDirs_migrate` above. |
| [TestEnsureAndResolveGenesisDirsError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L1044) | [open_resource_ledger_fails_with_permission_denied_on_readonly_tracker_dir](../../bin/algod-rust/src/commands/node.rs#L1273) | matched-1:many | `resolve_resource_paths` itself stays a pure, infallible path-string computation (go's "empty rootDir" constructor-error scenario has no direct equivalent), but the real fallible wiring step -- `create_dir_all` on the resolved directory in `open_resource_ledger` (the shared `node start`/`participate` ledger-open path) -- now has a Unix-only `chmod`-based permission-denied negative test, mirroring `create_genesis_ledger_dir_fails_with_permission_denied_on_readonly_parent`'s existing pattern for the sibling genesis-dir creation step. |
| [TestResolveLogPaths](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L1073) | [`log_file_dir_and_log_archive_dir_round_trip_through_json_as_documented_no_op`](https://github.com/xarmian/algod-rust/blob/main/crates/node/algo-config/src/lib.rs) | out-of-scope | Issue #1137 investigation and decision: algod-rust's `LOG_FILE_DIR`/`LOG_ARCHIVE_DIR` fields (`algo-config/src/lib.rs`) are **formally retired as permanent no-ops**, not an open gap. algod-rust logs exclusively to stdout/`tracing` subscribers, and every deployment tool this repo ships (`docker/docker-compose*.yml`, `ops/mixed-cluster*/`) already captures node output via `docker logs`/stdout redirection rather than an on-disk `node.log` — matching this node's containerized/systemd-supervised operational model. See `docs/PROJECT_SCOPE.md`'s Operational Readiness section for the recorded decision. The fields still round-trip through `config.json` for compatibility, pinned by the cited test; go's `ResolveLogPaths` path-resolution logic itself has no algod-rust analogue to test. |
| [TestStoresCatchpoints](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L1101) | [stores_catchpoints_false_when_interval_is_zero](../../crates/node/algo-config/src/lib.rs#L4095), [stores_catchpoints_false_for_untracked_mode](../../crates/node/algo-config/src/lib.rs#L4118), [stores_catchpoints_true_for_stored_mode_regardless_of_archival](../../crates/node/algo-config/src/lib.rs#L4131), [stores_catchpoints_automatic_and_tracked_modes_gate_on_archival](../../crates/node/algo-config/src/lib.rs#L4150), [stores_catchpoints_false_for_stock_default_config](../../crates/node/algo-config/src/lib.rs#L4184), [stores_catchpoints_unrecognized_tracking_value_falls_through_to_archival_gate](../../crates/node/algo-config/src/lib.rs#L4192) | matched-1:many | |
| [TestTracksCatchpointsWithoutStoring](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L1216) | [tracks_catchpoints_true_for_tracked_mode_without_storing](../../crates/node/algo-config/src/lib.rs#L4213), [tracks_catchpoints_false_when_untracked](../../crates/node/algo-config/src/lib.rs#L4227) | matched-1:many | |
| [TestEncodedAccountAllocationBounds](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/config_test.go#L1227) | — | out-of-scope | Cross-checks every consensus version's `MaxAssetsPerAccount`/`MaxAppsCreated`/`MaxAppsOptedIn`/`Max{Local,Global}SchemaEntries` against *fixed codec allocbound constants* that exist in go to cap how much a malicious msgpack length prefix can make the decoder pre-allocate. Investigated for issue #946: algod-rust's msgpack decode path (`crates/core/algo-codec`, `rmp_serde::from_slice`) decodes into ordinary growable `Vec`/`HashMap` collections rather than fixed-capacity allocbound constants (confirmed: no `EncodedMax*`/allocbound-style constant exists anywhere in `algo-types`/`algo-codec`), so there is no equivalent fixed-bound table in this crate for `ConsensusParams` to be cross-checked against — a genuine architectural difference, not a missing test. |
| [TestConsensusParams](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/consensus_test.go#L27) | [consensus_params_invariants_hold_across_every_known_version](../../crates/core/algo-types/tests/consensus_table_sweep_test.rs#L181), [consensus_params_checker_catches_synthetic_absolute_bound_regression](../../crates/core/algo-types/tests/consensus_table_sweep_test.rs#L127), [consensus_params_checker_catches_synthetic_grace_period_regression](../../crates/core/algo-types/tests/consensus_table_sweep_test.rs#L147) | matched-1:many | Issue #946: whole-consensus-table sweep across every `KNOWN_PROTOCOL_VERSIONS` entry asserting the four `MaxAbsolute* >= Max*` bounds (guarded by algod-rust's `0`-means-"not yet introduced" sentinel convention, since a single flat struct spans every version unlike go's per-version struct literals) and the challenge-grace-period bound, with TDD checks proving each invariant is actually enforced via a deliberately-broken synthetic `ConsensusParams`. (`SupportSignedTxnInBlock`/`ApplyData`/`PaysetCommitUnsupported` don't apply: algod-rust's pin starts at v7, where both are unconditionally true and payset commit is always supported.) Phase 17 consensus-gate sweep found and closed two fields that were flagged `(not modeled)` in `consensus.rs`'s own comments and never actually consulted anywhere: `MaxVersionStringLen` (issue #1278 — [test_max_version_string_len_activates_at_v12](../../crates/core/algo-types/src/consensus.rs), [apply_upgrade_vote_rejects_oversized_upgrade_propose](../../crates/core/algo-ledger/src/block_header.rs)) now bounds `UpgradePropose` string length in `apply_upgrade_vote`, matching go's `applyUpgradeVote`; `ForceNonParticipatingFeeSink` (issue #1279 — [test_force_non_participating_fee_sink_activates_at_v15](../../crates/core/algo-types/src/consensus.rs), [fee_sink_honors_declared_status_before_v15](../../crates/core/algo-ledger/src/genesis.rs)) now version-gates `genesis::effective_genesis_status`'s fee-sink-forcing branch instead of applying it unconditionally regardless of the genesis protocol version. A third finding, `NoEmptyLocalDeltas` (issue #1280), was investigated and found to require an architectural change (opt-in-without-write tracking through the AVM context boundary) rather than a local fix — filed for future work, not yet closed. |
| [TestConsensusUpgradeWindow](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/consensus_test.go#L64) | [test_upgrade_wait_rounds_bounds_before_v22_are_zero](../../crates/core/algo-types/src/consensus.rs#L2331), [upgrade_window_bounds_hold_for_every_known_version_with_an_approved_upgrade](../../crates/core/algo-types/src/consensus.rs#L2439) | matched-1:many | `upgrade_window_bounds_hold_for_every_known_version_with_an_approved_upgrade` sweeps every version in `KNOWN_PROTOCOL_VERSIONS` (matching go's map iteration) and checks the nonzero-delay/within-bounds invariant for whichever ones carry an approved upgrade — algod-rust models only the single outbound upgrade edge relevant per version (`ConsensusParams::approved_upgrade`) rather than a full historical `ApprovedUpgrades` map, so only v41 currently exercises the check, but the sweep methodology itself now matches go's and will catch a future version that violates the bound. |
| [TestConsensusUpgradeWindow_NetworkOverrides](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/consensus_test.go#L82) | [devnet_shortens_every_pending_upgrade_to_min_upgrade_wait_rounds](../../crates/core/algo-types/tests/consensus_devnet_upgrade_override_test.rs), [betanet_shortens_every_pending_upgrade_to_min_upgrade_wait_rounds](../../crates/core/algo-types/tests/consensus_devnet_upgrade_override_test.rs), [fnet_shortens_every_pending_upgrade_to_min_upgrade_wait_rounds](../../crates/core/algo-types/tests/consensus_devnet_upgrade_override_test.rs), [mainnet_is_a_no_op](../../crates/core/algo-types/tests/consensus_devnet_upgrade_override_test.rs), [testnet_is_a_no_op](../../crates/core/algo-types/tests/consensus_devnet_upgrade_override_test.rs) | matched-1:many | Issue #1138: `algo_types::consensus::apply_shorter_upgrade_rounds_for_dev_networks` ports `ApplyShorterUpgradeRoundsForDevNetworks` (`config/config.go:384-399`) field-for-field — shortens every version's pending-upgrade delay down to that version's own `MinUpgradeWaitRounds` (guarded by the same `MinUpgradeWaitRounds > 0` check) for the Devnet/Betanet/Fnet genesis networks, no-op elsewhere. Wired into `bin/algod-rust`'s `node start` (`commands/node.rs`) and `participate` (`commands/participate.rs`) startup, applied to the built-in consensus table before any `consensus.json` override is merged on top, matching go's exact call order in `cmd/algod/main.go:204-212`. |
| [TestConsensusStateProofParams](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/consensus_test.go#L178) | [consensus_state_proof_key_capacity_holds_across_every_known_version](../../crates/core/algo-types/tests/consensus_table_sweep_test.rs#L194), [state_proof_checker_catches_synthetic_key_capacity_regression](../../crates/core/algo-types/tests/consensus_table_sweep_test.rs#L160) | matched-1:1 | Issue #946: asserts `(max_keyreg_valid_period+1)/state_proof_interval == 1<<16` across every `KNOWN_PROTOCOL_VERSIONS` entry with a nonzero `state_proof_interval` (plus a non-vacuousness check that at least one known version enables state proofs), with a TDD check proving the invariant is actually enforced via a deliberately-broken synthetic value. |
| [TestParseDNSBootstrapIDBackupWithExpectedDefaultTemplate](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L47) | [parse_default_template_mainnet](../../crates/node/algo-network/src/dns_bootstrap.rs#L314), [parse_default_template_testnet](../../crates/node/algo-network/src/dns_bootstrap.rs#L328) | matched-1:many | |
| [TestParseDNSBootstrapIDBackupWithHardCodedNetworkBootstraps](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L69) | [hardcoded_devnet_override](../../crates/node/algo-network/src/dns_bootstrap.rs#L345), [hardcoded_betanet_override](../../crates/node/algo-network/src/dns_bootstrap.rs#L354), [hardcoded_alphanet_override](../../crates/node/algo-network/src/dns_bootstrap.rs#L363), [devnet_override_bypassed_when_overridden](../../crates/node/algo-network/src/dns_bootstrap.rs#L373) | matched-1:many | |
| [TestParseDNSBootstrapIDWithLegacyTemplate](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L89) | [legacy_template_no_backup](../../crates/node/algo-network/src/dns_bootstrap.rs#L385), [legacy_template_testnet](../../crates/node/algo-network/src/dns_bootstrap.rs#L394) | matched-1:many | |
| [TestParseDNSBootstrapIDNoBackup](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L108) | [no_backup_dedup_ignored](../../crates/node/algo-network/src/dns_bootstrap.rs#L405), [no_backup_with_https_prefix](../../crates/node/algo-network/src/dns_bootstrap.rs#L414) | matched-1:many | |
| [TestParseDNSBootstrapIDBackupNoDedup](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L138) | [backup_no_dedup](../../crates/node/algo-network/src/dns_bootstrap.rs#L426) | matched-1:1 | |
| [TestParseDNSBootstrapIDBackupWithSingleDomainDedup](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L157) | [backup_with_single_domain_dedup](../../crates/node/algo-network/src/dns_bootstrap.rs#L438) | matched-1:1 | |
| [TestParseDNSBootstrapIDEmptySpaceURLsRejected](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L180) | [empty_bootstrap_id_rejected](../../crates/node/algo-network/src/dns_bootstrap.rs#L454), [whitespace_only_rejected](../../crates/node/algo-network/src/dns_bootstrap.rs#L463) | matched-1:many | |
| [TestParseDNSBootstrapIDInvalidURLsRejected](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L190) | [invalid_url_rejected](../../crates/node/algo-network/src/dns_bootstrap.rs#L475) | matched-1:1 | |
| [TestParseDNSBootstrapIDInvalidQueryParamsRejected](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L198) | [invalid_query_params_rejected](../../crates/node/algo-network/src/dns_bootstrap.rs#L495) | matched-1:1 | |
| [TestParseDNSBootstrapIDInvalidNameMacroPosition](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L206) | [invalid_name_macro_position](../../crates/node/algo-network/src/dns_bootstrap.rs#L512) | matched-1:1 | |
| [TestParseDNSBootstrapIDInvalidDedupRegex](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/dnsbootstrap_test.go#L216) | [invalid_dedup_regex](../../crates/node/algo-network/src/dns_bootstrap.rs#L525) | matched-1:1 | |
| [TestAlgodVsUpdatedVersions](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/config/version_test.go#L29) | — | out-of-scope | Tests go-algorand's own S3-hosted-binary self-update version-naming/parsing scheme (`util/s3`). algod-rust ships no self-update mechanism (users build/update via cargo or binary releases directly), so there is no `Version`/S3-name-parsing equivalent to test. |
| [TestOmitEmpty](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L62) | [test_omitempty_skips_zero_fields](../../crates/core/algo-codec/src/canonical.rs#L2368) | matched-1:1 | Go tests the reflection-based codec's `omitempty`/`omitemptyarray` struct-tag machinery generically; Rust's serde derive achieves the same canonical-empty-map behavior, tested concretely on transaction encoding. |
| [TestEncodeOrder](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L70) | [test_keys_are_sorted](../../crates/core/algo-codec/src/canonical.rs#L2094) | matched-1:1 | Go verifies field-declaration-order and struct-embedding order don't affect wire order (canonical map-key sort wins); Rust directly asserts sorted-key canonical output. |
| [TestEncodeInline](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L129) | — | out-of-scope | Tests Go-reflection struct-embedding ("inline") encode equivalence, a go-codec-library-specific mechanism. Rust's serde derive model has no analogous embedding-flatten ambiguity to test. |
| [TestEncodeEmbedded](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L143) | [roundtrip_serde_encode_fast_decode_block_response](../../crates/core/algo-codec/tests/fast_decode_conformance.rs#L121) | out-of-scope | Re-verified (batch 1, issue #1496): reads go's `codec_test.go:143`. The test exists specifically because go-codec's generated `msgp` marshalers are defined per-receiver-type (value vs. pointer get separate generated methods that could in principle diverge), so `TestEncodeEmbedded` pins that `Encode(x) == Encode(&x)` for a struct embedding a msgp-custom type. Serde's `derive(Serialize)` has no such value/pointer split — a `&T` and a `T` always serialize through the exact same generated `Serialize` impl, so the property this test checks is a Rust-language invariant enforced by the compiler, not a runtime behavior with two independent code paths that could disagree. Same "no codegen-methodology ambiguity to test" rationale as the neighboring `TestEncodeInline`/`TestEncodeJSON` rows above. Round-trip correctness itself remains covered generically by the cited conformance test. |
| [TestEncodeJSON](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L161) | — | out-of-scope | Tests go-codec's non-strict `EncodeJSON` (unquoted int map keys, e.g. `0:` — technically invalid JSON per spec, but accepted by go's relaxed decoder) vs strict `EncodeJSONStrict` (`"0":`, spec-compliant). Checked go's own production call sites for the non-strict variant (`cmd/goal/clerk.go`, `cmd/goal/application.go`, `cmd/buildtools/genesis.go`) — all CLI pretty-printing/file-dump tooling, not REST API wire responses. algod-rust's `serde_json` always produces standards-compliant JSON (equivalent to go's *strict* mode), which is a strict superset of interoperability (parseable by any JSON tool, not just go-codec's own relaxed reader) — there is no consumer that specifically needs the non-standard unquoted-int-key format, so there is nothing of real value this distinction would add. |
| [TestMsgpDecode](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L208) | [decode_signed_txn_stream_rejects_too_deep_payload](../../crates/core/algo-codec/src/lib.rs#L441), [decode_signed_txn_stream_decodes_every_concatenated_value_then_stops_cleanly](../../crates/core/algo-codec/src/lib.rs#L455) | matched-1:many | `decode_signed_txn_stream_decodes_every_concatenated_value_then_stops_cleanly` covers the happy-path multi-value decode: three concatenated canonically-encoded `SignedTransaction`s all decode in order and the loop stops cleanly at exactly the end of the stream, alongside the existing depth-limit rejection test. |
| [TestRandomizeObjectWithPtrField](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L242) | [proposalvalue_canonical](../../crates/core/algo-agreement/tests/codec_proptest.rs#L292) | matched-1:many | Go's `RandomizeObject` reflection helper (incl. pointer-field non-nil-population) backs its randomized-encoding fuzz tests; Rust achieves equivalent randomized round-trip coverage via `proptest`-generated arbitrary values in `codec_proptest.rs` (also: rawvote_canonical, unauthenticated_vote_canonical, authenticated_vote_canonical, unauthenticated_bundle_canonical, authenticated_bundle_canonical). |
| [TestCheckMsgpAllocBoundDirectiveCrossPackage](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/codec_test.go#L273) | [decode_block_rejects_too_deep_payload](../../crates/core/algo-codec/src/lib.rs#L451) | out-of-scope | Re-verified (batch 1, issue #1496): reads go's `codec_test.go:273-278` -- the entire test body is one assertion, `hasMsgpAllocBoundDirective("github.com/algorand/go-algorand/protocol/test", "testSlice")`, which greps the `msgp` codegen tool's own source-comment directives (`//msgp:allocbound`) across a package boundary to prove the linter that enforces those directives at codegen time actually sees cross-package types. This is purely a self-test of go's `msgp` codegen/lint tooling operating on a synthetic fixture type (`testSlice`, itself out-of-scope per `TestMarshalUnmarshaltestSlice`/`TestRandomizedEncodingtestSlice` above) -- there is no source-comment-directive/codegen-linter tool in algod-rust's build to have an equivalent test for. The actual *safety property* the directive exists to enforce (bounded decode allocation) is a real, portable property and is separately covered by algod-rust's explicit depth/size-limited decoders (`decode_block_rejects_too_deep_payload` and siblings) -- but there is no tooling-completeness check to strengthen here. |
| [TestHashIDPrefix](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/hash_test.go#L29) | [hash_id_constants_match_go](../../crates/node/algo-network/src/identity.rs#L890) | matched-1:1 | |
| [TestPQSchemes](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/pq_scheme_test.go#L27) | [canonical_pq_address_salt_is_deterministic_and_pq_compliant](../../crates/core/algo-types/src/pq.rs#L209), [pq_scheme_falcon1024_literal_is_printable_ascii](../../crates/core/algo-types/src/pq.rs#L218) | out-of-scope | Re-verified (batch 1, issue #1496): `pq_scheme_falcon1024_literal_is_printable_ascii` already ports go's printable-ASCII invariant directly. The remaining half of go's test (`PQSchemeFalcon1024 != PQSchemeFalcon512`, a uniqueness/collision check across go's two declared PQ schemes) has no closeable gap to strengthen into -- algod-rust's `pq.rs` module deliberately models only the one PQ scheme go-algorand's `v5.0.0-stable` pin actually issues addresses under (`PQ_SCHEME_FALCON1024`); there is no second wire-scheme constant anywhere in algod-rust for a uniqueness assertion to compare against. Confirmed by reading `pq.rs`: adding a second, never-otherwise-used constant purely to give this comparison something to compare against would test nothing real. A permanent, by-design architectural difference, not missing coverage. |
| [TestTagList](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/tags_test.go#L115) | [roundtrip_all_tags](../../crates/node/algo-network/src/tag.rs#L212) | matched-1:1 | |
| [TestMaxSizesDefined](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/tags_test.go#L144) | [max_sizes_match_go](../../crates/node/algo-network/src/tag.rs#L245), [every_active_tag_has_a_nonzero_max_message_size](../../crates/node/algo-network/src/tag.rs#L270) | matched-1:many | `every_active_tag_has_a_nonzero_max_message_size` iterates `Tag::ACTIVE_TAGS` directly (matching go's exhaustive-iteration methodology) and asserts each has a nonzero max message size, alongside the existing per-tag spot-check values. |
| [TestMaxSizesTested](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/tags_test.go#L154) | [max_sizes_match_go](../../crates/node/algo-network/src/tag.rs#L245), [every_active_tag_has_a_nonzero_max_message_size](../../crates/node/algo-network/src/tag.rs#L270) | out-of-scope | Re-verified (batch 1, issue #1496): reads go's `tags_test.go:154-186` -- an AST meta-test that `go/parser`-parses `../node/node_test.go`'s source text and walks its declarations to check that a *separate, hand-written* `TestMaxSizesCorrect` test has one assertion line per tag, catching a reviewer who added a new `Tag` but forgot to add its line to that other test. This is a coverage-completeness lint over go source text, with no Rust equivalent (no AST-parser-over-test-source tooling in this repo, and no analogous "assert one line per tag exists in file X" test to check). The underlying property it protects -- every tag has a real, asserted max-message-size value -- is already fully covered a different way in algod-rust: `every_active_tag_has_a_nonzero_max_message_size` iterates `Tag::ACTIVE_TAGS` exhaustively (so a newly added tag is automatically included, with nothing to forget), which is strictly stronger than go's must-remember-to-list-it-by-hand mechanism, not weaker. |
| [TestLockdownTagList](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/tags_test.go#L273) | [lockdown_tag_list](../../crates/node/algo-network/src/tag.rs#L313) | matched-1:1 | Issue #949: a hardcoded, spelled-out tag list (not `Tag::ACTIVE_TAGS` compared against itself) pinning algod-rust's own advertised tag set, mirroring go's rationale exactly (a silent addition/removal must force a reviewer to touch this test). |
| [TestMarshalUnmarshaltestSlice](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/test/msgp_gen_test.go#L16) | — | out-of-scope | Tests go's generated `msgp` codec for a synthetic fixture type (`testSlice`) used only to validate the codegen tool itself. algod-rust uses serde derive, not a generated-codec toolchain, so there is no analogous synthetic-type codegen test. |
| [TestRandomizedEncodingtestSlice](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/test/msgp_gen_test.go#L37) | — | out-of-scope | Same synthetic-codegen-fixture rationale as above. |
| [TestIdempotence](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/transcode/core_test.go#L180) | [roundtrip_double_serde_fast_decode](../../crates/core/algo-codec/tests/fast_decode_conformance.rs#L146) | out-of-scope | Re-verified (batch 1, issue #1496): confirmed via `go-algorand/protocol/transcode/{core.go,core_test.go}` -- `protocol/transcode` is a standalone generic msgp<->JSON transcode *CLI tool* (`msgptojson`/`jsontomsgp`-style conversion of arbitrary go-codec objects), not a library function any production go-algorand path calls. algod-rust has never built an equivalent generic transcode tool and none is planned -- there is no `crates/tools/*` entry point that reads/writes arbitrary msgp<->JSON like this one does, only fixed-type codec round-trip tests for the concrete wire types algod-rust actually uses (blocks, transactions, etc.). Closing this gap would mean building a new, currently-nonexistent CLI tool, which is new-feature scope, not a test-coverage gap in existing code -- out of scope for a test-parity audit. The idempotence *property* itself (double round-trip is stable) is separately and meaningfully covered for the wire types algod-rust does support, via the cited conformance test. |
| [TestIdempotenceMultiobject](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/transcode/core_test.go#L194) | [roundtrip_double_serde_fast_decode](../../crates/core/algo-codec/tests/fast_decode_conformance.rs#L146) | out-of-scope | Same rationale as TestIdempotence above, extended to multi-object streams -- no generic transcode tool exists to test. |
| [TestIdempotenceStruct](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/protocol/transcode/core_test.go#L231) | [roundtrip_double_serde_fast_decode](../../crates/core/algo-codec/tests/fast_decode_conformance.rs#L146) | out-of-scope | Same rationale as TestIdempotence above, for a single struct value. |
| [TestDbSchemaUpgrade1](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/db_test.go#L52) | [db_add_and_sig_exists_roundtrip](../../crates/core/algo-ledger/src/stateproof_worker.rs#L732) | matched-1:many | Issue #814: `stateproof_worker::db::install_sigs_table` ports go's `sigs` table schema (`CREATE TABLE ... UNIQUE (sprnd, signer)` plus the `from_this_node` index) exactly; every `db_*` test in this file exercises a freshly-installed table via the `fresh_db()` helper. The `provers`-table cache (`TestProversDB`/`TestDbProverAlreadyExists` below) is not ported. |
| [TestPendingSigDB](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/db_test.go#L95) | [db_get_pending_sigs_for_round_returns_only_that_round](../../crates/core/algo-ledger/src/stateproof_worker.rs#L763), [db_get_pending_sigs_respects_threshold_and_max_round](../../crates/core/algo-ledger/src/stateproof_worker.rs#L800), [db_get_pending_sigs_only_from_this_node_filters](../../crates/core/algo-ledger/src/stateproof_worker.rs#L825), [db_delete_pending_sigs_before_round](../../crates/core/algo-ledger/src/stateproof_worker.rs#L857) | matched-1:many | Issue #814: `stateproof_worker::db::{add_pending_sig, get_pending_sigs, get_pending_sigs_for_round, delete_pending_sigs_before_round}` port `db.go`'s pending-signature CRUD exactly, including the `sprnd<=threshold OR sprnd=maxRound` query shape. |
| [TestSigExistQuery](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/db_test.go#L160) | [db_add_and_sig_exists_roundtrip](../../crates/core/algo-ledger/src/stateproof_worker.rs#L732) | matched-1:1 | Issue #814: `stateproof_worker::db::sig_exists_in_db`. |
| [TestProversDB](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/db_test.go#L217) | [round_trips_through_ledger_state_into_a_verifiable_proof](../../crates/core/algo-ledger/tests/voters_participants_prover_integration_test.rs#L178), [round_trips_through_sqlite_ledger_into_a_verifiable_proof](../../crates/core/algo-ledger/tests/voters_participants_prover_integration_test.rs#L183) | out-of-scope | Reclassified (issue #1498/#1496 final push): re-verified against `stateproof/db_test.go#L217-260` -- go's test exercises a worker-local `provers` sqlite table that caches a fully-built, serialized `spProver` (participants + tree + message + VotersHdr) keyed by state-proof round, in the `stateproof` package's own db. No such object-cache DB exists in algod-rust, and none is needed: the underlying data-retention requirement (retaining voter data long enough to build a proof) is already met a different way, via issue #912's ledger-level `votersparticipants` sqlite table, which persists the raw `Vec<Participant>` array and rebuilds the tree on read instead of caching a serialized prover object -- a deliberate, safety-justified simplification (see `stateproof_worker.rs`'s module doc, also cited by the sibling `TestDbProverAlreadyExists` row's `out-of-scope` classification just below this one). Building a worker-local prover-object cache DB purely to satisfy this test's own db-shape assumption, with no independent behavioral payoff, is not a closeable coverage gap. Moves from `partial` to `out-of-scope`, consistent with `TestDbProverAlreadyExists`. |
| [TestDbProverAlreadyExists](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/db_test.go#L273) | — | out-of-scope | Confirmed (see `stateproof_worker.rs`'s module doc, cited across the `parity_crypto.md`'s `TestMarshalUnmarshalProver` row) this is a deliberate, safety-justified simplification: algod-rust's `StateProofRuntime` cheaply rebuilds its in-progress prover state from the persisted per-signature `sigs` table on restart rather than caching a serialized prover object, so there is no "prover already cached" upsert-conflict scenario for this test to exercise — the underlying data-retention need is met by issue #912's `votersparticipants` table (`TestProversDB` row above). |
| [TestMarshalUnmarshalsigFromAddr](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/msgp_gen_test.go#L16) | [sig_from_addr_round_trips_through_msgpack](../../crates/core/algo-ledger/src/stateproof_worker.rs#L2257) | matched-1:1 | Reclassified (batch 2, issue #1496): the previous citation/note were stale (already flagged as such by the sibling `TestRandomizedEncodingsigFromAddr` row). `SigFromAddr::to_msgpack`/`from_msgpack` (`stateproof_worker.rs`) *is* a dedicated whole-struct canonical-msgpack encoder/decoder (field tags `"a"`/`"r"`/`"s"`, `omitempty`, lexicographic ordering -- matching go's generated `MarshalMsg`/`UnmarshalMsg` exactly), not just the embedded `merklesig::Signature` codec. `sig_from_addr_round_trips_through_msgpack` exercises it directly with a fixed populated instance, matching go's `TestMarshalUnmarshalsigFromAddr` one-to-one; `sig_from_addr_rejects_garbage` additionally covers malformed input rejection. |
| [TestRandomizedEncodingsigFromAddr](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/msgp_gen_test.go#L37) | [sig_from_addr_randomized_roundtrip_over_field_combinations](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1709) | matched-1:1 | `SigFromAddr::to_msgpack`/`from_msgpack` (`stateproof_worker.rs`) *is* a whole-struct canonical-msgpack encoder/decoder (the sibling `TestMarshalUnmarshalsigFromAddr` row's "no whole-struct encoder" note appears stale) -- round-tripped here across the cross product of several signer addresses, rounds (including `0` and `u64::MAX`), and both a populated and an empty signature, covering the field-omission combinations go's randomized fuzzer exercises. |
| [TestMarshalUnmarshalspProver](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/msgp_gen_test.go#L76) | [sig_collector_end_to_end_builds_a_verifiable_proof](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1905) | out-of-scope | Reclassified (issue #1498/#1496 final push): re-verified `SigCollector` still has no `to_msgpack`/`from_msgpack` (unlike the sibling `SigFromAddr`, whose whole-struct codec was found and reclassified separately). This test round-trips go's `spProver`'s msgpack codec purely to support the worker-local prover-cache DB (`TestProversDB`, reclassified `out-of-scope` above) -- with that cache DB itself now confirmed a permanent, safety-justified design difference (issue #912's `votersparticipants` table meets the same underlying need without ever needing to serialize a prover object to disk), there is nothing left for a `SigCollector` msgpack codec to serve; building one solely to satisfy this test would be dead code with no caller. Moves from `partial` to `out-of-scope`, consistent with `TestProversDB`. Issue #814: `stateproof_worker::SigCollector` (wrapping `algo_consensus_crypto::stateproof::Prover`) remains the Rust equivalent of go's `spProver` and is exercised end-to-end via `sig_collector_end_to_end_builds_a_verifiable_proof`. |
| [TestRandomizedEncodingspProver](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/msgp_gen_test.go#L97) | — | out-of-scope | Same deliberate no-worker-local-prover-cache-DB reasoning as `TestDbProverAlreadyExists` above — no `spProver`-equivalent disk snapshot ever exists to encode. |
| [TestStateProofMessage](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/stateproofMessageGenerator_test.go#L39) | [generate_state_proof_message_builds_expected_fields](../../crates/core/algo-ledger/src/stateproof_message.rs#L232) | matched-1:1 | Issue #814 (follow-on session): `algo_ledger::stateproof_message::generate_state_proof_message` ports `GenerateStateProofMessage` -- block-headers commitment, voters commitment, `LnProvenWeight`, attested-round range -- end-to-end tested against a real ledger. |
| [TestGenerateStateProofMessageForSmallRound](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/stateproofMessageGenerator_test.go#L103) | [fetch_light_headers_returns_the_full_interval_in_order](../../crates/core/algo-ledger/src/stateproof_message.rs#L211) | matched-1:many | Issue #814: `fetch_light_headers`/`create_header_commitment` cover the small-round light-header-fetch path the same way. |
| [TestMessageLnApproxError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/stateproofMessageGenerator_test.go#L124) | [ln_int_approximation_rejects_zero](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1099), [calculate_ln_proven_weight_zero_weight_is_illegal_ln_input](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1157) | matched-1:many | The `Ln` integer-approximation primitive and its `calculateLnProvenWeight` caller (issue #814 follow-on) are both ported and tested. |
| [TestMessageMissingHeaderOnInterval](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/stateproofMessageGenerator_test.go#L153) | [fetch_light_headers_errors_when_a_round_is_missing](../../crates/core/algo-ledger/src/stateproof_message.rs#L199) | matched-1:1 | Issue #814: `fetch_light_headers` errors (rather than panicking) when a needed header isn't retained, matching go's missing-header-in-interval detection. |
| [TestGenerateBlockProof](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/stateproofMessageGenerator_test.go#L179) | [get_light_block_header_proof_happy_path](../../crates/node/algo-rest-api/tests/integration.rs#L5541) | out-of-scope | Reclassified (issue #1498/#1496 final push): re-verified against `stateproof/stateproofMessageGenerator_test.go#L179-244` -- go's `GenerateBlockProof` builds a Merkle proof of a block header against the state-proof-covered light-header commitment specifically for the *worker* to attach when submitting a state proof, as one step of an internal proof-submission pipeline. algod-rust builds the identical Merkle proof (same underlying array-proof mechanism, verified via the small-array edge case shared with `TestGenerateBlockProofOnSmallArray`, `matched-1:1` above) but exposes it as a standalone, read-only REST API response (`GET /v2/blocks/{round}/lightheader/proof`) rather than as an internal pipeline step with no external caller. This is a permanent design-shape difference (different caller/purpose for the same mechanism), not a coverage gap -- the mechanism itself is already fully tested. Moves from `partial` to `out-of-scope`. |
| [TestGenerateBlockProofOnSmallArray](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/stateproofMessageGenerator_test.go#L244) | [get_light_block_header_proof_small_array](../../crates/node/algo-rest-api/tests/integration.rs#L5574) | matched-1:1 | Closed (batch 2, issue #1496): added a REST-endpoint test with a minimal 2-round state-proof interval (mirroring `TestGenerateBlockProofOnSmallArray`'s 2-participant/small-array setup), exercising the same `GET /v2/blocks/{round}/lightheader/proof` path as the sibling `TestGenerateBlockProof` row's `get_light_block_header_proof_happy_path`, now specifically through the small-array edge case (single-level tree, minimal reveal set) go's test targets. |
| [TestValidateStateProof](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/verify/stateproof_test.go#L49) | [genuine_state_proof_verifies](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1283), [genuine_state_proof_rejects_wrong_message](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1292), [genuine_state_proof_rejects_forged_falcon_signature](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1306), [genuine_state_proof_rejects_tampered_sig_commit_root](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1343), [genuine_state_proof_rejects_tampered_participant_weight](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1359) | matched-1:many | |
| [TestAcceptableStateProofWeight](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/verify/stateproof_test.go#L111) | [verify_weights_rejects_zero_signed_weight](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1107), [verify_weights_rejects_too_many_reveals](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1115), [verify_weights_rejects_insufficient_reveals](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1123), [verify_weights_accepts_enough_reveals](../../crates/core/algo-consensus-crypto/src/stateproof.rs#L1134) | matched-1:many | |
| [TestStateProofParams](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/verify/stateproof_test.go#L157) | [rejects_round_mismatch](../../crates/core/algo-ledger/src/apply_stateproof.rs#L630), [rejects_when_state_proof_next_is_zero](../../crates/core/algo-ledger/src/apply_stateproof.rs#L655) | matched-1:many | go's `GetProvenWeight` round-validation logic maps to algod-rust's equivalent state-proof-round-mismatch/next-round-zero rejection checks in `apply_stateproof.rs`. |
| [TestWorkerAllSigs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L564) | [multi_node_simulated_gossip_all_signers_converge_on_a_verifiable_proof](../../crates/core/algo-ledger/src/stateproof_worker.rs#L2036) | matched-1:1 | Closed (batch 2, issue #1496): the remaining gap after batch 1's tightened note was specifically go's *multi-node* property (weight gathered from peers over gossip, not just locally inserted) -- this didn't actually need real network/thread infrastructure to reproduce, since every node's `SigCollector` is already a pure, synchronous, in-memory object and every node derives byte-identical `participants`/`part_tree`/`addr_to_pos` from the same ledger snapshot in reality. `build_multi_node_collectors` builds N independent `SigCollector`s sharing that same deterministic setup, and `gossip_sig` simulates "broadcast this signature to every peer" with a plain in-process loop -- faithfully reproducing the multi-node convergence property go's test drives with real goroutines/fake-network plumbing, without needing either. |
| [TestWorkerPartialSigs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L628) | [multi_node_simulated_gossip_partial_signers_still_converge_via_gossip](../../crates/core/algo-ledger/src/stateproof_worker.rs#L2077) | matched-1:1 | Closed (batch 2, issue #1496): same simulated-gossip mechanism as `TestWorkerAllSigs` above, with only 3 of 5 nodes actually signing -- asserts every node, *including the two that never produced a signature themselves*, converges on a verifiable proof purely from what it received over simulated gossip, matching go's partial-signer scenario. |
| [TestWorkerInsufficientSigs](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L690) | [multi_node_simulated_gossip_insufficient_signers_never_converge](../../crates/core/algo-ledger/src/stateproof_worker.rs#L2124) | matched-1:1 | Closed (batch 2, issue #1496): same simulated-gossip mechanism, with only 1 of 5 nodes signing (below the proven-weight threshold) -- asserts every node, having received every signature that will ever arrive over gossip, correctly stays not-ready and `create_proof` rejects with `SignedWeightLessThanProvenWeight`, matching go's insufficient-signers scenario. |
| [TestWorkerRestart](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L724) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerHandleSig](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L773) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerIgnoresSignatureForNonCacheProvers](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L808) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestKeysRemoveOnlyAfterStateProofAccepted](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L882) | [prune_round_none_before_any_state_proof_confirmed](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1745), [prune_confirmed_state_proof_keys_noop_before_confirmation](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1821), [run_loop_prunes_state_proof_keys_only_after_ledger_confirms_acceptance](../../bin/algod-rust/src/commands/stateproof_service.rs#L781) | matched-1:many | Issue #1139: `ParticipationStore::delete_state_proof_keys_before` is now wired into the live state-proof daemon's commit-reaction path via `algo_ledger::stateproof_worker::prune_confirmed_state_proof_keys`/`state_proof_keys_prune_round`, gated on the ledger's own confirmed `StateProofNextRound` (read from an already-committed block header, matching go's `Worker.deleteProverData` being invoked only from a just-committed block) — never a merely built/broadcast round. |
| [TestKeysRemoveOnlyAfterStateProofAcceptedSmallIntervals](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L911) | [prune_round_small_interval_keeps_reused_bucket](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1774), [prune_confirmed_state_proof_keys_keeps_reused_bucket_small_interval](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1857) | matched-1:many | Issue #1139: covers the `KeyLifetime > StateProofInterval` case where a confirmed proof round still falls inside the currently-live key-lifetime bucket, so nothing is pruned yet. |
| [TestKeysRemoveOnlyAfterStateProofAcceptedLargeIntervals](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L952) | [prune_round_large_interval_crosses_bucket_boundary](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1791), [prune_confirmed_state_proof_keys_deletes_only_past_bucket](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1833) | matched-1:many | Issue #1139: covers the `KeyLifetime < StateProofInterval` case where a confirmed proof round has moved past the first key-lifetime bucket, so that bucket's keys are now pruned. |
| [TestWorkersProversCacheAndSignatures](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L989) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestSignatureBroadcastPolicy](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1065) | [meets_broadcast_policy_accepts_within_threshold_or_at_latest](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1221), [handle_sig_ignores_a_round_the_ledger_already_has_a_complete_proof_for](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1281), [handle_sig_ignores_an_externally_sourced_signature_beyond_the_broadcast_policy](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1296), [handle_sig_does_not_apply_the_broadcast_policy_to_this_nodes_own_signature](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1321), [pending_sigs_to_broadcast_recovers_lost_broadcast_in_second_half](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1907), [pending_sigs_to_broadcast_returns_empty_when_state_proofs_disabled](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1969), [pending_sigs_to_broadcast_skips_rounds_later_than_brnd](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1987), [find_and_broadcast_pending_sigs_recovers_lost_broadcast_across_daemon_ticks](../../bin/algod-rust/src/commands/stateproof_service.rs#L986) | matched-1:many | Phase 17 deep-pass finding: go's test actually exercises two distinct mechanisms under one name — `Worker.meetsBroadcastPolicy` (the anti-spam gate inside `handleSig` that rejects an externally-received signature for an untracked, far-out-of-window round) and `Worker.broadcastSigs` (a periodic re-announcement of still-pending signatures). Issue #814 had ported `meets_broadcast_policy`'s formula but never actually called it from `StateProofRuntime::handle_sig`; a follow-on pass wired it in. Issue #1231 then ported the second mechanism, `Worker.broadcastSigs` (`builder.go:493`) itself: `algo_ledger::stateproof_worker::pending_sigs_to_broadcast` selects, for a single round, which pending signatures are due for re-announcement (first-half-of-period = own signatures only, second-half = any seen signature, further gated by the per-signer address-based schedule `addr64 % (interval/2) == brnd % (interval/2)` for load-balancing, matching `builder.go:524-527` exactly rather than simplifying it away), and `bin/algod-rust`'s `stateproof_service::find_and_broadcast_pending_sigs` wires it into `run_loop`'s per-round batch scan (mirroring go's `builder()` loop iterating `nextBroadcastRnd..newLatest`, `builder.go:470-474`). A signature broadcast lost to a one-shot gossip-propagation failure is now retried on a later daemon tick. |
| [TestWorkerDoesNotLimitProversAndSignaturesOnDisk](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1125) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestSigBroacastTwoPerSig](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1259) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestProverGeneratesValidStateProofTXN](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1312) | [runtime_signs_gathers_builds_and_verifies_a_real_state_proof](../../crates/core/algo-ledger/tests/stateproof_runtime_integration_test.rs#L74) | matched-1:1 | Issue #814 (follow-on session): full end-to-end path -- sign with real MSS/Falcon keys, gather over the wire `SigFromAddr` round trip, `StateProofRuntime::try_build`, `stateproof_worker::build_state_proof_transaction`/`state_proof_body_from_crypto` -- verified against an independent `stateproof::Verifier`. Not covered: this test builds the transaction but doesn't submit it through a running node's pool/broadcast path the way go's test's mocked `TransactionSender` observes (see `bin/algod-rust`'s `stateproof_service::run_loop`, which does that live). |
| [TestForwardNotFromThisNodeSecondHalf](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1348) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestForwardNotFromThisNodeFirstHalf](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1370) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerHandleSigOldRounds](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1429) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerHandleSigRoundNotInLedger](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1447) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerHandleSigWrongSignature](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1465) | [handle_sig_rejects_forged_signature_through_the_full_handler](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1487), [sig_collector_rejects_forged_signature_when_verifying](../../crates/core/algo-ledger/src/stateproof_worker.rs#L681) | matched-1:many | Batch 1 (issue #1496): a live state-proof daemon now exists (`bin/algod-rust`'s `stateproof_service`, registering `StateProofSigHandler::handle` on `Tag::StateProofSig`), so `TestWorkerHandleSig*`'s actual target -- go's `Worker.handleSig` entrypoint -- has a direct Rust equivalent: `StateProofRuntime::handle_sig`. `handle_sig_rejects_forged_signature_through_the_full_handler` drives a real ledger with one registered voter, forces prover creation, and submits a signature over the wrong message through `handle_sig` itself (not just the lower `SigCollector::insert_sig`), asserting the same `VerificationFailed` rejection go's test does. |
| [TestWorkerHandleSigAddrsNotInTopN](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1483) | [handle_sig_rejects_address_not_in_voters_through_the_full_handler](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1508), [sig_collector_rejects_unknown_address](../../crates/core/algo-ledger/src/stateproof_worker.rs#L654) | matched-1:many | Batch 1 (issue #1496): same rationale as `TestWorkerHandleSigWrongSignature` above -- `handle_sig_rejects_address_not_in_voters_through_the_full_handler` submits a signature from an address outside the round's selected voters through `handle_sig` itself, asserting `AddressNotInVoters`. |
| [TestWorkerHandleSigAlreadyIn](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1527) | [handle_sig_ignores_already_gathered_signature_through_the_full_handler](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1523), [sig_collector_rejects_duplicate_signature](../../crates/core/algo-ledger/src/stateproof_worker.rs#L667) | matched-1:many | Batch 1 (issue #1496): same rationale as `TestWorkerHandleSigWrongSignature` above -- `handle_sig_ignores_already_gathered_signature_through_the_full_handler` submits the same validly-signed message twice through `handle_sig`, asserting `Broadcast` then `Ignore` (not an error) on redelivery, matching go's assertion exactly. |
| [TestWorkerHandleSigExceptionsDbError](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1566) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerHandleSigCantMakeProver](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1599) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerHandleSigIntervalZero](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1644) | [handle_sig_ignores_signature_when_state_proofs_disabled_via_full_handler](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1550), [is_eligible_signing_round_requires_interval_multiple](../../crates/core/algo-ledger/src/stateproof_worker.rs#L505) | matched-1:many | Batch 1 (issue #1496): `handle_sig_ignores_signature_when_state_proofs_disabled_via_full_handler` submits a signature through `handle_sig` itself against a ledger whose active protocol has `state_proof_interval == 0`, asserting `SigOutcome::Ignore` and no prover entry created -- go's actual assertion target (`handleSig`'s own gate), not just the standalone `is_eligible_signing_round` helper one layer down. |
| [TestWorkerHandleSigNotOnInterval](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1670) | [handle_sig_ignores_signature_for_round_not_on_interval_via_full_handler](../../crates/core/algo-ledger/src/stateproof_worker.rs#L1564), [is_eligible_signing_round_requires_interval_multiple](../../crates/core/algo-ledger/src/stateproof_worker.rs#L505) | matched-1:many | Batch 1 (issue #1496): same rationale as `TestWorkerHandleSigIntervalZero` above, for a round that isn't a multiple of the active interval. |
| [TestWorkerHandleSigCorrupt](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1689) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerCacheAndDiskAfterRestart](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1726) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerInitOnlySignaturesInDatabase](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1784) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerLoadsProverAndSignatureUponMsgRecv](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1848) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestWorkerCreatesProversOnCommit](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1915) | — | out-of-scope | Stale note: a live state-proof daemon does exist (issue #814) and does react to ledger commits (`voters_tracker.rs`/`apply_stateproof.rs`'s tracker bookkeeping, plus `StateProofRuntime`'s lazy per-round `SigCollector` construction from the voters snapshot). The specific "create/cache a serialized prover object per round" mechanism this test targets doesn't exist by the same deliberate no-provers-table design documented on the other worker rows above — the observable requirement (react to commits, build proofs for eligible rounds) is met via a different mechanism. |
| [TestSignerUsesPersistedProverLatestProto](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1952) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |
| [TestRegisterCommitListener](https://github.com/algorand/go-algorand/blob/v5.0.0-stable/stateproof/worker_test.go#L1999) | — | out-of-scope | A live state-proof daemon exists (issue #814, `bin/algod-rust`'s `stateproof_service` + `algo_ledger::stateproof_worker::StateProofRuntime`); this specific scenario drives go's mocked worker-local `provers`-table cache/restart mechanics, which `stateproof_worker.rs`'s module doc documents skipping by deliberate, safety-justified design (cheap re-verification from the persisted `sigs` table on restart, rather than trusting a cached prover snapshot — see the `parity_crypto.md` `TestMarshalUnmarshalProver` row for the full reasoning). With no cached-prover-object mechanism to exist, there is no equivalent internal state for this test's scenario to construct. |

## Gaps summary

Status counts: matched-1:1 = 31, matched-1:many = 41, missing-test = 0, not-implemented = 0, out-of-scope = 46, partial = 1 (119 total).

- ~~**No state-proof worker/signing daemon (44 rows, the largest single gap
  group).**~~ **Core algorithm landed in issue #814** (`crates/core/algo-ledger/src/stateproof_worker.rs`):
  round-eligibility gates (`nextStateProofRound`, `signStateProof`'s
  interval check, `onlineProversThreshold`, `meetsBroadcastPolicy`),
  per-account signing (`signStateProofMessage`, reusing the existing
  `merklesig::Secrets`/`Signer` Falcon primitives), signature gathering
  (`SigCollector`, wrapping `algo_consensus_crypto::stateproof::Prover` —
  see `parity_crypto.md`'s state-proof-proving-role note) with go's
  `spProver.insertSig` exactly, and the `sigs` pending-signature table
  (`db.go`) via `rusqlite`.
  **The live daemon itself also now exists** (same issue, follow-on
  session, unblocked by issue #912's voters-participant-array
  persistence): `algo_ledger::stateproof_message::generate_state_proof_message`
  ports the signer-side `GenerateStateProofMessage`/`createHeaderCommitment`/
  `calculateLnProvenWeight`/`FetchLightHeaders` (a new
  `algo_consensus_crypto::light_block_header::LightBlockHeader` type
  supplies the SHA-256 vector-commitment-tree leaf go's
  `bookkeeping.LightBlockHeader` is); `stateproof_worker::StateProofRuntime`
  (`create_prover_entry`, `handle_sig`, `try_build`) is the in-memory
  equivalent of `Worker`'s `provers` map + `tryBroadcast`; and
  `bin/algod-rust/src/commands/stateproof_service.rs` wires it all into a
  background thread (mirroring `heartbeat_service.rs`'s established
  pattern, issue #820) that signs eligible rounds, gossips/gathers
  `SigFromAddr` messages over a new `Tag::StateProofSig` handler, and
  submits the resulting `StateProofTx` via `LocalTxBroadcaster` — gated
  behind `config.json`'s `EnableStateProofWorker` (default `false`, no go
  equivalent since go's `Worker` is always-on) rather than being wired
  into default node startup. **Deliberately still not ported**: a
  disk-persisted `provers` table (`stateproof/db.go`'s `provers` table,
  `TestProversDB`/`TestDbProverAlreadyExists`) — the runtime is in-memory
  only, rebuilt from the ledger's own persisted voters snapshot on
  restart — and go's `AcceptableStateProofWeight` incremental
  build-earlier-with-a-smaller-proof schedule (this runtime always
  targets the full proven-weight threshold, strictly safe but slower to
  produce the very first proof of a round). The remaining
  `not-implemented` `worker_test.go` rows are go's daemon-internal
  scenario tests this simplified in-memory implementation doesn't
  replicate one-for-one (DB-error injection, disk-cache-after-restart,
  `OnPrepareVoterCommit`/ledger-commit-triggered prover creation) — see
  each row's note. This is a distinct, previously-undocumented
  architectural gap — not covered by Phase 16 (which scoped only
  `config.Local`/`ConsensusParams`, not node subsystems).
- **`config.Local` node-runtime derivations — closed by issue #949.**
  Phase 16's original finding ("~58 of ~97 `Local` fields have no
  equivalent at all") is significantly narrowed by
  `crates/node/algo-config` (76+ tests covering versioned defaults,
  migration, JSON load/save, catchpoint-tracking gates, etc.).
  [Issue #949](https://github.com/xarmian/algod-rust/issues/949) closed
  the `IsListenServer`/`IsWsListenServer`/`IsP2PListenServer`/
  `IsHybridServer` state matrix, `ValidateP2PHybridConfig`, the
  `phonebook.json` loader, the `TestLockdownTagList`-equivalent, and (in
  a follow-up pass) `AdjustConnectionLimits`'s FD-pressure connection-limit
  rebalancing — `Local::adjust_connection_limits` (pure rebalancing
  algorithm, table-tested against go's own 14 cases) plus
  `algo_network::fd_limits` (live `getrlimit`/`setrlimit` FD accounting on
  Unix, go's own hard-coded Windows "unlimited"), wired into `main.rs`'s
  `relay`/`participate` startup paths. **Superseding this bullet's
  earlier "architectural non-goal" note (issue #749)**: issues #953 and
  #1030 (the most recent work on `main` as of this pass) actually
  implemented `HotDataDir`/`ColdDataDir`/per-resource directory
  overrides after all — `bin/algod-rust/src/commands/participate.rs`'s
  `resolve_resource_paths` (with `create_dir_all` at its call site) is
  the working equivalent of go's `EnsureAndResolveGenesisDirs`/
  `ensureAbsGenesisDir`, closing most of the "7 config-package rows"
  this bullet used to describe as fully open — see those rows' updated
  status above. Genuinely still open: `ResolveLogPaths` (`LogFileDir`/
  `LogArchiveDir` are accepted-but-documented-no-op fields, since
  algod-rust has no file-based log-archival subsystem at all — issue
  #1137) and go's automatic cold-to-hot DB-file migration-on-reconfigure
  (`resolve_resource_paths` is deliberately a pure function that doesn't
  move existing files, per its own doc comment — an operator-responsibility
  scope cut, not a gap).
- **`phonebook.json` loader closed by issue #949; `config.json.example`
  still missing.** go-algorand loads an optional phonebook include-list
  file (`{"Include": [...]}`) and ships a `config.json.example`
  regression-checked against compiled defaults. `algo_network::
  phonebook_file` now ports the loader/saver, wired into `Phonebook`
  construction in both `commands::participate::run` and
  `commands::relay::run`; no example config file exists to check (still
  not-implemented, unrelated to #949's scope).
- **No self-update mechanism.** `TestAlgodVsUpdatedVersions` tests
  go-algorand's S3-hosted-binary version-naming scheme; algod-rust ships no
  self-update mechanism at all (out-of-scope, architectural).
- **Whole-consensus-table invariant sweeps.** go's `TestConsensusParams`,
  `TestConsensusStateProofParams`, and the allocbound-vs-consensus-limit
  cross-check (`TestEncodedAccountAllocationBounds`) each iterate *every*
  known protocol version asserting a structural invariant holds throughout
  the table. [Issue #946](https://github.com/xarmian/algod-rust/issues/946)
  closed the first two: `crates/core/algo-types/tests/consensus_table_sweep_test.rs`
  now sweeps every `KNOWN_PROTOCOL_VERSIONS` entry asserting
  `TestConsensusParams`'s absolute-bound and grace-period invariants and
  `TestConsensusStateProofParams`'s state-proof key-capacity invariant, each
  backed by a TDD check proving the sweep catches a deliberately-introduced
  synthetic regression before trusting it against the real table.
  `TestEncodedAccountAllocationBounds` stays out-of-scope: it cross-checks
  fixed codec allocbound constants that have no counterpart in algod-rust's
  architecture (`rmp_serde` decodes into ordinary growable `Vec`/`HashMap`
  collections, not fixed-capacity allocbounds), so there is nothing for it
  to cross-check against. (Tracked together with the parallel AVM
  opcode/field table-completeness gap in `parity_txn_logic.md`.)
- **Go-reflection/codegen-specific test machinery has no Rust analogue
  (out-of-scope, `protocol`/`protocol/test`/`protocol/transcode`).** Tests
  like `TestEncodeInline`, `TestRandomizeObjectWithPtrField`,
  `TestCheckMsgpAllocBoundDirectiveCrossPackage`, and the `protocol/test`
  and `protocol/transcode` packages entirely test go-codec's
  reflection-based struct-tag machinery, its `msgp` codegen tool's
  `allocbound` directives, and a generic msgp↔JSON transcode CLI tool.
  algod-rust uses serde derive with hand-written canonical-encoding logic
  and explicit depth/size-limited decoders instead, so these have no
  meaningful port target; the underlying *properties* they protect
  (bounded allocation, round-trip correctness) are covered by different,
  more targeted Rust tests noted inline above.
- **PQ scheme registry only partially modeled.** go-algorand registers two
  PQ schemes (`PQSchemeFalcon1024`, `PQSchemeFalcon512`); algod-rust's
  `pq.rs` models only Falcon-1024, with no dedicated ASCII-printability/
  uniqueness invariant test for the scheme-tag byte pair.
- **`TestLockdownTagList` (backward-compat-locked message-of-interest tag
  allow-list) closed by issue #949** — `algo_network::tag::tests::
  lockdown_tag_list` pins a hardcoded, spelled-out tag list against
  `Tag::ACTIVE_TAGS`, matching go's rationale that a silent tag addition/
  removal must force a reviewer to touch this test.
