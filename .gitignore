/target/
**/target/
.claude/worktrees/
.claude/scheduled_tasks.lock
.codex
# Compiled vector-capture binaries (built via `go build` in their dirs)
tools/*-vector-capture/*-vector-capture
# Bench output JSON — produced by `trie_replay` and `go-trie-replay-bench`.
bench-results/
**/bench-results/
/fixtures/
reports/
docker/data/
*.swp
*.swo
.DS_Store
.env
wire_fixtures/
benchmarks/go-decode/go.sum
# Dual-node REST conformance harness scratch state (issue #129).
/.validate-api-rust-data/
/.validate-api-rust-data.log
/.validate-api-rust.pid
# Issue #100 stress-test netroot: goal-generated genesis + participation keys
# (private key material), regenerated by docker/scripts/stress-bootstrap.sh.
docker/stress-netroot/
docker/.stress-template.rendered.json
# Mixed-cluster consensus conformance output (issue #488). The nightly
# workflow pins consensus-conformance.sh's OUT_DIR here before uploading
# it as a workflow artifact; a local run with the same OUT_DIR lands here
# too. See .github/workflows/consensus-cluster.yml.
/consensus-artifacts/
